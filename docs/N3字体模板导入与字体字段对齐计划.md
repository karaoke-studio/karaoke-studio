# N3 字体模板导入与字体字段对齐计划

> 状态校准：2026-07-13  
> 逆向基准：NicoKaraMaker3 10.74.80.0 x64  
> 范围：N3 `LyricsFontModel` / `TemplateFont/*.tpl`，以及现有 `.n3proj` 导入中对
> `LyricsFonts` 的应用情况。布局、歌词时间、素材和输出参数继续以
> [`N3项目导入兼容性与实施计划.md`](N3项目导入兼容性与实施计划.md) 为准。

## 1. 结论

N3 字体模板的序列化字段已经可以全部识别，但当前实现还不能宣称“任意 N3 字体
模板无损导入”。需要同时处理两条入口：

1. **现有 `.n3proj` 项目导入**：已经覆盖主字体、主/ruby 字号、描边、8 层配色、
   阴影和发光，但仍有部分 N3 fallback 被错误折算为本项目的全局方案继承，英数和
   假名字体页也没有完整应用。
2. **新增 `.tpl` 模板导入**：文件读取本身简单，但必须额外复刻 N3 应用模板时的
   `SizeAndRatio` 换算，并明确模板进入样式预设库后的跨分辨率语义。

实施时必须先把 `LyricsFontModel → SubtitleStyleScheme` 转换抽成两条入口共用的纯函数，
不能为 `.tpl` 再写一套与 `.n3proj` 不同的字段映射。

### 1.1 2026-07-13 描边字段对齐进度

本轮已经完成以下数据归属调整：

- 描边宽度、描边 2 开关、描边 2 宽度已经从颜色卡片移到字体卡片；
- 字体卡片现在提供主文字/注音 × 日文/英数四组独立描边参数；
- `UseEdge2` 与 `EdgeSize2` 分开保存，关闭二重描边不会丢失保存的宽度；
- 新字段已接入 `Style`、`SubtitleStyleScheme`、项目/预设序列化、属性面板、角色方案、
  N3 `.n3proj` 导入和横排 Painter；
- 主文字字符分类已按 N3 扩展到 ASCII 与 `À..ÿ`；
- 装饰类型、阴影、发光和浓度仍位于颜色卡片，并改为整个配色方案共用；N3 导入不再
  为 ruby 制造一套重复装饰覆盖。旧工程已有 ruby 装饰覆盖仍可读取，用户重新编辑共用
  装饰参数时会清除对应旧覆盖。

尚未因此自动完成的事项：竖排主文字仍以整条路径绘制，不能在同一竖排内应用不同的
日文/英数描边；混合脚本 ruby 仍以整段读音绘制，只有整段为英数时应用 ruby 英数描边。
这些限制必须继续保留在兼容矩阵中，不能把本轮 UI/模型完成等同于所有渲染路径无损。

## 2. 状态标记

| 标记 | 含义 |
|---|---|
| ✅ | 当前字段已应用，已知样例语义正确 |
| ⚠️ | 当前结果在已有样例中可能正确，但存在未覆盖或错误 fallback |
| ❌ | 字段存在，但当前导入没有应用或会丢失 |
| 🚫 | 产品明确不支持，需要检测并给出确定降级提示 |
| — | N3 同步/编辑元数据，不影响单向视觉导入 |

## 3. N3 文件与对象结构

### 3.1 `.tpl` 保存位置

非 MSIX 版：

```text
%APPDATA%\SHINTA\NicoKaraMaker3\TemplateFont\<GUID>.tpl
```

MSIX 版：

```text
%LOCALAPPDATA%\Packages\<PackageFamilyName>\Settings\TemplateFont\<GUID>.tpl
```

`.tpl` 是 ZIP，内部固定读取名为 `0` 的 UTF-8 JSON。N3 选择窗口只显示
`Synchronize == true` 的模板，并按 `SettingsName` 排序。

### 3.2 顶层 `LyricsFontModel`

```text
LyricsFontModel
  BrushInfos[8]
  FontInfos[6]
  DecorKind
  DecorSize { Size, Reference, Ratio }
  BlurLevel
  SettingsName / Index
  Synchronize / SynchronizedTime
  CreateAppVer / ModifyAppVer
  Guid / LastModified
```

其中视觉字段是 `BrushInfos`、`FontInfos`、`DecorKind`、`DecorSize` 和
`BlurLevel`。其余字段用于模板名称、同步、排序和修改记录。

### 3.3 `FontInfos` 六个固定槽位

| 索引 | N3 `FontFacePage` | fallback |
|---:|---|---|
| 0 | 歌词/汉字 | N3 默认字体与默认数值 |
| 1 | 歌词/假名 | 0 |
| 2 | 歌词/英数 | 0 |
| 3 | ruby/汉字 | 0 |
| 4 | ruby/假名 | 3 |
| 5 | ruby/英数 | 3 |

每个槽位保存：

```text
FontName
FontFaceName
CharSize  { Size, Reference, Ratio }
EdgeSize  { Size, Reference, Ratio }
UseEdge2  bool | null
EdgeSize2 { Size, Reference, Ratio }
```

N3 的字符分类不是简单的 Python `isascii()`：

- 假名：平假名、片假名、`ー`、全角空格；
- 英数：ASCII `U+0020..U+007E`，以及 `À..ÿ`；
- 其他字符：汉字页；
- ruby 再整体偏移到索引 3～5。

### 3.4 `BrushInfos` 八个固定槽位

| 索引 | 含义 |
|---:|---|
| 0 | 走字后/文字 |
| 1 | 走字后/描边 |
| 2 | 走字后/二重描边 |
| 3 | 走字后/装饰（阴影或发光颜色） |
| 4 | 走字前/文字 |
| 5 | 走字前/描边 |
| 6 | 走字前/二重描边 |
| 7 | 走字前/装饰（阴影或发光颜色） |

每个笔刷同时保存所有编辑分支，但只有 `SelectedBrushTypeIndex` 指定的分支生效：

```text
0 Solid          -> SolidColor.DxColor RGBA
1 Gradient       -> GradientStops[{ Position, Color RGBA }]
2 MilleFeuille   -> GradientStops，N3 在边界位置复制前一颜色形成硬切
3 Bitmap         -> BitmapPath + BitmapScale
```

单向视觉导入只需保留当前生效分支；若未来要回写 N3 或在本项目中切换填充类型后恢复
N3 的未选中编辑值，则必须额外保存原始分支数据。

## 4. N3 的实际解析规则

### 4.1 字体族与字体面是成对 fallback

N3 从当前 `FontFaceInfoModel` 开始：

1. 如果 `FontName` 非空，直接使用该槽位的字体族和该槽位的 `FontFaceName` 所对应字重；
2. 只有 `FontName` 为空时，才沿 `Fallback` 继续查找；
3. 整条链字体族都为空时，使用 N3 默认字体并使用 Bold。

因此不能分别执行“找第一个非空 `FontName`”和“找第一个非空 `FontFaceName`”。当前
导入器的 `_resolve_font_name()` 与 `_resolve_face_name()` 是独立解析，存在如下错误场景：

```text
英数 FontName = Comic Sans MS
英数 FontFaceName = 空
主文字 FontFaceName = Bold
```

N3 使用 Comic Sans MS Normal；当前导入器会把主文字 Bold 错误套给英数。

N3 的字体面列表只枚举 DirectWrite `FontStyle.Normal`，`FontFaceName` 在这里主要代表
字重，而不是斜体或 stretch。导入 N3 时应精确解析字重，并把斜体明确设为关闭，避免
项目全局斜体污染 N3 方案。

### 4.2 数值字段独立 fallback

`CharSize`、`EdgeSize`、`EdgeSize2` 分别沿各自 fallback 链查找第一个大于 0 的
`Size`。整条链都没有有效值时，N3 默认值为：

| 字段 | N3 最终默认值 |
|---|---:|
| `CharSize` | 100 |
| `EdgeSize` | 5 |
| `EdgeSize2` | 5 |

当前 `_resolve_font_size()` 在整条链无有效值时返回 0。对于全局 `Style`，部分字段可能
碰巧落到相同默认值；对于自定义 `SubtitleStyleScheme`，0 或 `None` 可能继承另一个
全局方案，因此必须在 N3 方案转换阶段物化上述最终值。

### 4.3 `UseEdge2` 是 nullable fallback

1. 当前槽位是 `true/false` 时立即使用；
2. `null` 时沿 fallback 链继续查找；
3. 整条链都是 `null` 时为 `false`。

当前 `_resolve_use_edge2()` 与这项规则一致，不能因为 `EdgeSize2.Size > 0` 就强制开启。

### 4.4 `.n3proj` 与 `.tpl` 的 `SizeAndRatio` 语义不同

#### `.n3proj`

工程文件中的 `Size` 已经由 N3 按工程画面高度更新。现有导入器直接读取 `Size` 是正确
行为，不应再次按 `Ratio` 缩放。

真实工程 `異世界ガールズ♡トーク/1.n3proj` 的画面高度为 1080，保存值也是：

```text
主文字 100 @ 1080
描边 15 @ 1080
ruby 45 @ 1080
装饰 5 @ 1080
```

所以当前读取结果与 N3 一致，并不能证明导入器已经实现了比例换算。

#### `.tpl`

N3 应用模板时会对 `DecorSize` 和全部六个 `FontInfos` 调用 `UpdateSizes(height)`：

```text
if target_height != 0 and Ratio != 0:
    Size = int(target_height * Ratio)
else:
    Size = 原值
```

例如现有“标准配色”模板保存 `CharSize=133, Reference=1440,
Ratio=0.092592...`，应用到 1080 高度后应得到 100，而不是 133。

### 4.5 模板跨分辨率复用

本项目 `StylePreset` 当前主要保存绝对像素值。如果 `.tpl` 在导入当前工程时直接折算为
绝对像素，再把预设应用到另一种画面高度，就不再具备 N3 模板的动态缩放语义。

需要明确选择并测试其中一种方案：

1. **完整对齐方案**：为导入的 N3 预设保留各尺寸的 `Ratio`，每次应用到项目时按当前
   画面高度解析；
2. **固定像素方案**：导入时按当前项目高度解析，后续跨分辨率不再缩放，并在导入界面
   明确提示。

本计划以完整对齐方案为目标。不能把模板原始 `Reference` 当作当前项目高度直接使用。

## 5. 现有 `.n3proj` 字体字段应用矩阵

### 5.1 主文字与脚本字体

| N3 字段/语义 | 当前目标字段 | 状态 | 需要调整 |
|---|---|---|---|
| 汉字 `FontName` | `font_family` | ✅ | 保持 |
| 汉字 `FontFaceName` | `font_weight` | ⚠️ | 从“多语言关键字猜测”改为按字体族+字体面查询真实字重；失败时提示并 fallback |
| 汉字斜体 | N3 不提供 | ⚠️ | 导入完整 N3 方案时显式 `italic=False` |
| 汉字 `CharSize` | `font_size_px` | ✅ | `.n3proj` 继续读 `Size` |
| 汉字 `EdgeSize` | `stroke_width_px` | ✅ | 补全“全链无值→5”默认 |
| 汉字 `UseEdge2/EdgeSize2` | `stroke2_enabled` + `stroke2_width_px` | ✅ | 已分开保存和渲染；仍需补全 EdgeSize2 全链默认 5 |
| 假名字体族 | 无独立轨 | 🚫 | 按既定产品决策沿用汉字字体，并在任意假名覆盖生效时提示 |
| 假名字号/字重/描边 | 无独立轨 | ❌ | 当前仅检查假名 `FontName`，其余差异会静默丢失；需决定扩模型或统一降级并完整提示 |
| 英数字体族 | `font_family_latin` | ⚠️ | 已读取非空字体，但必须物化“空→本方案汉字字体”，不能让自定义方案继承全局英数字体 |
| 英数字号 | `latin_font_size_px` | ✅ | `.n3proj` 已按英数 fallback 链写入 |
| 英数字重 | `latin_font_weight` | ⚠️ | 已写入并渲染；字体族+字体面仍需改为成对 fallback 和真实字重查询 |
| 英数描边/二重描边 | `latin_stroke_width_px` + `latin_stroke2_enabled/width_px` | ⚠️ | 模型/UI/保存/N3 导入/横排 Painter 已接入；竖排混合脚本仍待拆分路径 |
| `À..ÿ` 英数字符路由 | N3 Latin 分类 | ✅ | 主文字字体与描边路由已接入 |

### 5.2 ruby 字体

| N3 字段/语义 | 当前目标字段 | 状态 | 需要调整 |
|---|---|---|---|
| ruby 汉字字体族 | `ruby_font_family` | ✅ | 当前已沿 3→0 链解析并物化 |
| ruby 汉字字号 | `ruby_font_size_px` | ✅ | 补全“全链无值→100”默认 |
| ruby 汉字字重 | `ruby_font_weight` | ⚠️ | 改为按实际字体面解析 |
| ruby 汉字描边/二重描边 | ruby stroke + enabled 字段 | ✅ | 已分开保存开关和宽度；补全 N3 数值默认仍待办 |
| ruby 假名字体族 | 无独立轨 | 🚫 | 按既定产品决策沿用 ruby 汉字字体，并完整检测提示 |
| ruby 假名字号/字重/描边 | 无独立轨 | ❌ | 与主假名相同，需要明确扩展或降级 |
| ruby 英数字体族 | `ruby_font_family_latin` | ⚠️ | 已读取非空字体；空值必须物化为本方案 ruby 汉字字体，不能继承全局 ruby 英数字体 |
| ruby 英数字号 | `ruby_latin_font_size_px` | ⚠️ | `.n3proj` 已写入，模型/UI/保存已接入；需补齐 Painter 的逐脚本 ruby 字体应用 |
| ruby 英数字重 | `ruby_latin_font_weight` | ⚠️ | `.n3proj` 已写入，仍受字体面解析与 ruby Painter 路径限制 |
| ruby 英数描边/二重描边 | `ruby_latin_stroke_width_px` + enabled/width2 | ⚠️ | 模型/UI/保存/N3 导入已接入；横排整段英数 ruby 已应用，混合脚本及竖排仍待办 |

### 5.3 自定义方案内部 fallback 污染

N3 每个 `LyricsFontModel` 都是独立完整方案；它的 1/2/3/4/5 槽位只在该方案内部
fallback。当前 `SubtitleStyleScheme` 中的 `None` 表示继承项目全局 `Style`，两者语义
不同。

当前存在如下错误风险：

```text
全局 N3 方案：
  主字体 = 教科书体
  英数字体 = Comic Sans MS

自定义 N3 方案：
  主字体 = 游明朝
  英数字体 = 空（N3 语义：继承该方案的游明朝）

当前导入结果：
  自定义方案 font_family = 游明朝
  自定义方案 font_family_latin = None
  渲染合并后错误继承全局 Comic Sans MS
```

修复要求：从 N3 导入的每个 `LyricsFontModel` 必须先完成 N3 内部 fallback，生成一个
完整、独立、可直接渲染的方案快照。不能用 `None` 表达 N3 的槽位 fallback。

必须物化的字段至少包括：

- 主/英数/ruby/ruby 英数的有效字体族、字号、字重；
- 主/ruby 描边宽度与二重描边有效状态；
- 装饰类型、半径/偏移、浓度；
- 主文字与 ruby 的完整 before/after 配色矩阵；
- N3 不提供但可能从项目全局泄漏的字段，例如 `italic=False`。

## 6. 颜色与笔刷字段应用矩阵

| N3 字段/语义 | 当前状态 | 需要调整 |
|---|---|---|
| 8 个笔刷槽位顺序 | ✅ | 保持 |
| `SelectedBrushTypeIndex=0` 纯色 | ✅ | `DxColor` RGBA 已完整导入 |
| `SelectedBrushTypeIndex=1` 平滑渐变 | ✅ | RGBA、浮点 stop 与 N3 字符渲染目标坐标均已对齐 |
| `SelectedBrushTypeIndex=2` MilleFeuille | ✅ | 转为真正硬边色带模型，不再使用“边界前 1%”近似 |
| `SelectedBrushTypeIndex=3` 图片 | ⚠️ | 路径与缩放已接入；需对齐平铺、缩放方向和纹理锚点 |
| 未知笔刷类型 | ✅ | 回退纯色并提示，保持 |
| 缺失图片素材 | ✅/有限 | 当前回退纯色并提示；`.tpl` 需延迟到应用项目时按歌词目录解析相对路径 |
| `DxColor.A` | ✅ | `0..1` 转 8 位 Alpha；不透明规范化为 `#RRGGBB`，其余保存为 `#AARRGGBB` |
| `GradientStop.Position` float | ✅ | 百分比保留到浮点模型、项目保存、编辑器和 Painter，不再取整 |
| 同位置重复 stop | ✅ | 通用渐变模型稳定保序；MilleFeuille 使用不会被 Qt 合并的硬色带模型 |
| 渐变坐标 | ✅ | IL 确认 N3 为 `(0,0) → (0, renderTarget.Height)`；本项目映射为字符/同样式 run 局部矩形的 top→bottom |
| 图片坐标 | ⚠️ | N3 纹理以渲染目标原点平铺；当前按字形/运行 rect 平移锚定，需要固定帧对照 |

### 6.1 Alpha 表示

本项目 `QColor`、属性面板和 Painter 已能处理 `#AARRGGBB`；N3 导入器也已使用同一
转换函数覆盖纯色、渐变 stop、MilleFeuille 和装饰色。颜色选择窗口另有可见的棋盘格
透明度条，并与 Qt Alpha 数值双向同步。转换规则为：

```text
A/R/G/B float 0..1
→ round(channel * 255)
→ #AARRGGBB（A=255 时可规范化为 #RRGGBB）
```

纯色、渐变 stop、MilleFeuille 和装饰色都使用同一转换函数。

### 6.2 渐变坐标、硬边与缓存

10.74.80.0 的 `BrushInfoModel.CreateBrush()` IL 已确认：Gradient 和 MilleFeuille
都建立从 `(0, 0)` 到 `(0, renderTarget.Size.Height)` 的纵向笔刷；调用点位于
`DrawOneCharBodyText`、`DrawOneCharEdgeText` 和 `DrawOneCharEdge2Text`。本项目因此以
字符或同样式 glyph run 的局部矩形 top→bottom 建刷，缓存 run 内字体度量相同，坐标
语义不会因合并缓存而变化。

MilleFeuille 的 IL 会在每个源 stop 后插入 `(next.Position, current.Color)`，并忽略
最后一个源 stop 的颜色。Qt 会合并同位置的 `QGradientStop`，所以不能直接照搬重复
stop；当前改为 `split_vertical` 真硬色带模型，并使用 1×目标高度的纹理逐像素保存
边界。基础纹理进入独立 LRU，缓存键包含目标高度、完整浮点位置和 RGBA；字符位置仅
对缓存笔刷副本做平移。`clear_before_layer_cache()` 会与文字层、发光层和图片笔刷一起
清除此缓存，`_fill_signature()` 也包含完整节点列表，样式变更不会命中旧烘焙层。

## 7. 装饰字段应用矩阵

| N3 字段 | 当前状态 | 需要调整 |
|---|---|---|
| `DecorKind=0 None` | ✅/表达差异 | 当前以 0 偏移 shadow 表示，视觉等价；导入方案必须显式物化，避免继承全局 glow |
| `DecorKind=1 Shadow` | ✅ | `DecorSize` 同时映射 X/Y 偏移 |
| `DecorKind=2 Blur` | ✅ | 发光半径已映射 |
| `BlurLevel=0/1/2` | ✅ | 主/ruby/角色/标题均已接入 |
| `DecorSize` in `.n3proj` | ✅ | 直接读取当前 `Size` |
| `DecorSize` in `.tpl` | ❌ | 按目标高度和 `Ratio` 解析 |
| before/after 装饰颜色 | ✅ | 来自 BrushInfos 3/7，Alpha 已完整导入；仍需填充精度对齐 |

## 8. `.tpl` 导入功能清单

### 8.1 共享转换层

新增独立于 Qt UI 的 N3 字体方案转换模块，职责包括：

1. 读取/校验 `LyricsFontModel` 字典；
2. 按 N3 规则解析六个字体槽位；
3. 按入口类型处理尺寸：
   - `project_snapshot`：读取 `Size`；
   - `template`：按目标高度解析 `Ratio`；
4. 转换 8 个笔刷；
5. 输出完整 `SubtitleStyleScheme`，同时返回结构化 warning；
6. 由 `.n3proj` 导入器和 `.tpl` 导入器共同调用。

不要继续把所有逻辑保留在 `n3proj_import.py::_scheme_changes()` 内。

### 8.2 文件发现与读取

1. 自动发现普通版和 MSIX 版 `TemplateFont`；
2. 同时允许用户手动选择 `.tpl` 文件或目录；
3. ZIP 内只读取条目 `0`；
4. JSON 错误、缺少必要字段、GUID 与文件名不一致时分别给出中文错误；
5. 默认只列出 `Synchronize == true`；删除态模板可在诊断模式显示；
6. 按 `SettingsName` 排序；
7. 支持多选批量导入。

### 8.3 样式预设库接入

推荐入口是现有“样式预设库”，新增“从 N3 导入”操作：

1. 每个 `.tpl` 转为一个 `StylePreset`；
2. 默认分组可设为 `N3`；
3. 名称来自顶层 `SettingsName`；
4. 同名时提供覆盖、自动改名、跳过，并支持批量统一策略；
5. 导入预设不自动改动当前角色；导入完成后由现有“应用到当前目标”或“导入为项目角色”
   流程处理；
6. 单个模板失败不应取消整批，结束后显示成功/跳过/失败汇总。

### 8.4 预设持久化边界

当前 `_style_presets` 随 `.yurika` 项目保存。如果产品目标是“导入一次后所有项目可用”，
需要把个人样式预设库持久化到应用设置，并保留项目内快照；如果目标仅是当前项目，则保持
现状。该边界必须在实现前固定，避免用户导入后新建项目发现模板消失。

### 8.5 图片填充的延迟解析

N3 的 `BitmapPath` 优先相对歌词源文件目录解析，然后尝试绝对路径。应用级预设导入时
可能还没有歌词源，因此不能在导入模板时永久回退纯色。完整方案应保存原始 N3 图片路径，
应用到具体项目后再按该项目歌词目录解析；确实不存在时才提示并回退。

## 9. 不参与视觉导入的字段

以下字段默认不进入 `SubtitleStyleScheme`：

| 字段 | 处理 |
|---|---|
| 顶层/子对象 `Guid` | 本项目创建自己的预设身份；可作为 source metadata 保留用于重复导入识别 |
| `Synchronize` | 仅用于过滤 N3 删除态模板 |
| `SynchronizedTime` | 不参与视觉 |
| `CreateAppVer/ModifyAppVer` | 可记录在诊断信息，不参与视觉 |
| `LastModified` | 不参与视觉 |
| `Index` | N3 UI 顺序字段，不参与视觉 |
| 子对象 `SettingsName` | 仅用于错误/警告上下文和 8 槽位诊断 |
| `DxColor.SumRGB/Average/Luma` | 派生值，只读取 R/G/B/A |
| 未选中笔刷分支 | 单向视觉导入可忽略；需要 N3 round-trip 时才保留 |

## 10. 本机真实数据审计

### 10.1 `TemplateFont` 四个模板

当前四个模板为：

- `【1配色】`
- `【2配色】`
- `【3配色】`
- `標準配色`

共同特征：

- 没有假名独立字体；
- 没有英数独立字号/描边；
- 所有颜色 Alpha 为 1；
- 没有 Bitmap 和 MilleFeuille；
- 主字体面是 Bold；
- `UseEdge2` 全链为 null，因此二重描边关闭；
- 装饰是 Blur，`BlurLevel=0`；
- `【1/2/3配色】` 的走字后文字使用平滑渐变；
- `【1/2/3配色】` 的参考高度为 1088，`標準配色` 的参考高度为 1440。

因此这四个模板的首批阻塞项是 `.tpl` 读取、`SizeAndRatio`、渐变浮点位置，以及预设库
接入；其他字段仍必须用合成 fixture 锁定，不能因为四个样例没使用就省略检测。

### 10.2 三个真实 `.n3proj`

本机扫描：

```text
工程数：3
LyricsFont 方案数：35
含英数字体族：16
含英数字体面：16
含英数 UseEdge2/描边覆盖：10
含浮点百分比渐变 stop：8
假名独立覆盖：0
ruby 英数独立覆盖：0
Alpha < 1：0
Bitmap：0
MilleFeuille：0
Size.Reference 与工程高度不一致：0
```

三个工程是：

- `Dark spiral journey/1.n3proj`
- `メフィスト/1.n3proj`
- `異世界ガールズ♡トーク/1.n3proj`

这些样例中英数 `CharSize=0`，所以 UI 显示主文字字号是正确的；英数字体面
`Negreta` 与主文字 `Bold` 最终都对应 700，真实样例本身不会暴露独立数值差异。本轮已用
合成 `.n3proj` fixture 覆盖英数/ruby 英数的独立字号、描边宽度、二重描边开关与保存宽度；
字体族与字体面成对 fallback、真实字重查询仍不能只凭这些样例判定完成。

## 11. 实施顺序

### P0：先修共享解析与现有 `.n3proj`

1. 为 N3 六字体槽建立纯数据解析器；
2. 修正字体族/字体面成对 fallback；
3. 补 N3 数值最终默认 100/5/5；
4. 为每个 N3 `LyricsFont` 物化完整独立方案，消除全局方案 fallback 污染；
5. ~~写入已有 Latin/ruby Latin 字号和字重字段；~~ 已完成写入，ruby Painter 仍待补齐；
6. ~~按 N3 规则识别 Latin-1；~~ 主文字已完成，ruby 混合脚本仍待拆分；
7. 对当前模型无法表达的假名和脚本独立描边检测并提示，不允许静默丢失；
8. 保持 `.n3proj` 直接使用 `Size`。

### P1：实现 `.tpl` 导入

1. 读取器与目录发现；
2. 模板 `SizeAndRatio` 按目标高度解析；
3. 样式预设库“从 N3 导入”；
4. 批量、同名和错误汇总；
5. 持久化边界；
6. 图片路径延迟解析。

### P2：颜色和填充精度

1. ~~RGBA；~~ 已完成（`.n3proj` 纯色、渐变节点、MilleFeuille、装饰色）；
2. ~~浮点 stop；~~ 已完成；
3. ~~同位置 stop 与 MilleFeuille；~~ 已完成，硬边纹理按高度、节点和 RGBA 缓存；
4. ~~渐变坐标；~~ 已按 N3 字符 render target 局部坐标完成；
5. 图片平铺、缩放和锚点。

### P3：扩展当前模型无法表达的字段

根据真实样例和产品决策处理：

1. ~~英数独立描边/二重描边的数据模型、UI、保存、N3 导入与横排渲染；~~ 已完成；
2. 竖排主文字按日文/英数拆分描边路径；
3. ruby 混合脚本与竖排的英数独立字体和描边；
4. 假名字号、字重、描边；
5. ruby 假名字号、字重、描边。

假名独立字体族继续遵守现有“不支持，沿用日文字体”的产品约束。

## 12. 测试清单

### 12.1 共享 fallback 单测

- 六个 `FontInfos` 的完整链；
- 字体族与字体面成对 fallback；
- 当前字体族非空、字体面为空时使用 Normal，不取 fallback Bold；
- `CharSize/EdgeSize/EdgeSize2` 独立 fallback；
- 全链无值时 100/5/5；
- `UseEdge2 true/false/null` 全组合；
- N3 字符分类：平假名、片假名、`ー`、全角空格、ASCII、`À..ÿ`、汉字；
- 自定义方案英数空值不继承全局英数字体。

### 12.2 `.n3proj` 回归

- `異世界ガールズ♡トーク`：Comic Sans MS、100px、700、描边继承；
- 合成工程：主文字/注音 × 日文/英数四组描边宽度、二重描边开关和保存宽度；
- 属性面板：四组控件均位于字体卡片，颜色卡片不再持有描边尺寸；
- 方案共用装饰：切换主文字/注音不改变装饰值，编辑时清除旧 ruby 装饰覆盖；
- 自定义方案主字体与全局英数字体不同时，空英数槽回退到本方案主字体；
- 合成工程显式设置英数字号/字重；
- 合成工程显式设置不同英数描边时必须应用或给出降级提示；
- `.n3proj` 的 `Size` 不重复缩放；
- 主/ruby/标题/逐字符角色均使用同一转换结果。

### 12.3 `.tpl` 回归

- ZIP 条目 `0` 与 UTF-8/BOM；
- 133@1440 应用到 1080 得 100；
- `Ratio=0` 保留原始 `Size`；
- Decor、主字体、ruby、全部描边一起缩放；
- 1080/1088/1440 三种目标高度；
- 同名覆盖/改名/跳过；
- `Synchronize=false` 默认不导入；
- 批量中单个坏文件不影响其他模板。

### 12.4 笔刷回归

- 纯色和 gradient RGBA；
- `Position=0.334/0.376` 不丢精度；
- 同位置重复 stop；
- MilleFeuille 硬边；
- Bitmap 相对/绝对路径、平铺、缩放、锚点；
- before/after × text/stroke/stroke2/decor 八层逐一断言；
- N3 与本项目固定分辨率、固定字体、固定时间点 PNG 对照。

## 13. 验收门槛

1. `.n3proj` 和 `.tpl` 使用同一个 N3 字体转换器；
2. 支持字段必须实际写入 Style、项目保存、UI 和 Painter，不能只解析不应用；
3. 不支持字段必须按字段值检测并给出中文提示，不能只检查 `FontName`；
4. N3 方案内部 fallback 不得泄漏到本项目全局方案；
5. `.n3proj` 不重复缩放，`.tpl` 必须按目标高度缩放；
6. `UseEdge2` 全链 null 时保持关闭；
7. 现有 N3 导入测试、模型/项目保存、属性面板和 Painter 测试全部通过；
8. 四个真实模板完成导入预览；三个真实 N3 工程完成回归；
9. 对渐变、MilleFeuille、Bitmap、Alpha 和脚本独立设置使用合成 fixture 补足真实样例空白；
10. 文档中的 ⚠️/❌ 项未完成前，不对外宣称“任意 N3 字体模板完全无损导入”。
