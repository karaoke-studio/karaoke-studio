# 字幕渲染管线优化调研报告

> 面向接手开发者的方案调研与路线规划。
> 目标:**在完整保留现有全部特效的前提下**,优化「预览」与「导出渲染」两条管线的性能,
> 并让特效体系（Sayatoo 信号模块、ユートピア 及未来新增特效）可持续扩展。
>
> 约束（硬性，先写在最前面；★ 标注的是 2026-06-23 与产品方确认的口径）：
> 1. **不能砍特效**。Sayatoo 信号灯/音量条、ruby 注音、竖排、glow、双行、分色(roles)、
>    入退场/逐字动画，全部要保留，且未来还会加新的特效族。
>    - ★ **ユートピア(Utopia) = 内部特效，"字体动态跳动"**（逐字随时间做位移/缩放/旋转等仿射抖动；
>      入场放大→收缩、扫光过冲、退场淡出）。**代码里已存在**（`_UTOPIA_*` 常量、entry/exit 的 `"utopia"`、
>      `_paint_line_with_character_transition`）。**★ 该特效可能视觉上"跨行"**（过冲/位移可能侵入相邻行区域），
>      但当前产品决策为**按单行拆分 scope**，见 §5.2。
>      （注：早前文档里的 "コドビーア" 系打字笔误，实为 ユートピア，全文已订正。）
> 2. 跨平台（Windows 优先，但实现选型不要锁死单平台）。
> 3. ★ **性能目标**：
>    - **导出**：在性能允许范围内尽可能快（无固定倍率硬指标，越快越好）。
>    - **预览**：稳定 **24–60 fps**，并**实时显示当前帧率**；**不要求 4K**，**输出分辨率允许用户可调**（低分辨率换流畅）。
> 4. ★ **一致性**：允许导出与预览存在差异，但**差异要尽可能小**；**预览可以不用 GPU**。
>    → 直接结论：**优先保留"单一共享 CPU 渲染器"**（预览与导出走同一套 layout+render，靠同构天然保证一致），
>      **GPU 从主线降级为"可选的导出加速"**（见 §4 方案 C 调整）。
> 5. ★ **句内逐段多段文字风格（字体/字号/颜色混排）的具体视觉规则待定**（§4.5 的对齐策略等留作后续设计）。
> 6. 渐进式：能 A/B 切换、能回退，不要一次性大重写上线。

---

## 0. TL;DR（结论先行）

- 当前两条管线：**导出**是单线程 Python 逐帧 `QPainter` 软件光栅化 → `constBits` 拷字节 → ffmpeg pipe；
  **预览**已经是较优形态（`QGraphicsView` + 原生 `QGraphicsVideoItem` 视频 + 透明 `QPainter` 字幕层）。
- **真正的逐帧热点**：`painter.py` 已把「未唱层」(阴影+描边+底色) 和「glow」烘焙成位图缓存，
  但**「已唱层」(扫光 fill + stroke + stroke2) 仍每帧用 `addText`+`strokePath` 重新光栅化**，
  只是套了个 clip rect（见 [`_paint_after_path`](../krok_helper/subtitle_render/engine/painter.py)）。这是最大、最易摘的果子。
- 业界共识（libass / AE / SDF 文献）高度一致：**渲染成本由「总位图尺寸 × 每帧需要重新光栅化的量」支配**，
  优化的核心是**「把与时间无关的内容烘焙成位图缓存，逐帧只做位图合成 + clip/transform」**。
- 推荐分三期推进，且**互不冲突、可叠加**：
  - **P0 增量优化**（不改架构，纯收益）：烘焙已唱层、只渲染字幕条带、多进程导出、缓冲复用。导出预计 **5–15×**。
  - **P1 layout/render 解耦 + 图层合成器重构**（中改架构，换可扩展性 + 缓存友好 + 一致性）：
    把渲染拆成 **layout（纯几何）→ animation（逐帧变换）→ paint（位图合成）** 三段，每个特效抽象成「图层/节点」。
    这是让 Sayatoo / ユートピア（字体跳动，视觉跨行但按单行拆分）/ 句内混排 / 未来特效**长期可维护**、且**预览=导出一致**的关键（见 §4.6、§5）。
  - **P2 GPU（可选，且仅导出加速）**：因约束 4「预览可不用 GPU + 要一致」，**不把预览 GPU 化**；
    GPU 仅作为**导出端可选加速**（skia-python(Ganesh) 优先）。**非必需**——CPU 路径缓存做到位通常已够（libass 即无 GPU）。
- libass/ASS 旁路只能覆盖「无 Sayatoo 信号」的普通卡拉OK，**不能作为主路径**（表达不了信号模块），
  但可作为「快速导出」可选项。
- ★ **关于"layout 是否必须与 render 解耦"**（新增确认点）：**P0 不必须**；但要同时满足"预览=导出一致 + 字体跳动 +
  句内混排 + 缓存 + 并行"，**解耦是总枢纽，P1 实质必须**。详见 §4.6。

---

## 1. 现状盘点

### 1.1 导出管线 `engine/renderer.py`

```
for index in range(total_frames):              # 单线程 Python 循环
    t_ms = round(index * 1000 / fps)
    image = QImage(1920×1080, RGBA8888)         # 每帧新分配 ~8MB
    image.fill(0); paint_frame(image, …)        # QPainter 软件光栅化
    process.stdin.write(bytes(image.constBits)) # 拷 8MB → 写 ffmpeg pipe
```

- ffmpeg 侧：rawvideo rgba pipe 作为 overlay 叠加在缩放后的背景视频上，音频从背景视频拷贝
  （[`build_render_command`](../krok_helper/subtitle_render/engine/renderer.py)）。
- 默认 60fps、1920×1080。60fps 下每秒：60 次全幅光栅化 + 60×8MB≈**470MB/s** 内存拷贝/pipe 带宽。
- 瓶颈：**单核 Python 串行**；`QMediaPlayer` 不参与导出，纯 `QPainter`。

### 1.2 预览管线 `frontend/preview_graphics.py`（默认）/ `preview_view.py`（raster 回退）

- 默认 `PreviewGraphicsView`：`QGraphicsScene` 里放
  - `QGraphicsVideoItem`（视频走 Qt 原生多媒体 → GPU 呈现，**不经过你的 QPainter**）；
  - `SubtitleGraphicsItem`（透明层，`paint()` 调 `paint_frame_to_painter`）。
- 环境变量 `KROK_SUBTITLE_PREVIEW=raster` 回退到老的 `PreviewCanvas`（手画视频帧 + 字幕，更慢）。
- 播放时钟：`TransportBar` 用 `QElapsedTimer` 60/120Hz tick 推进，`QMediaPlayer` 只负责音频
  （Windows 后端 `positionChanged` 粒度粗，所以自己插值）。
- 评价：**视频部分已经不是瓶颈**（原生 GPU）。预览的痛点只剩「字幕层每 tick 全量 `QPainter` 重绘」，
  与导出热点同源。

### 1.3 渲染核心 `engine/painter.py`（~4756 行，特效全在这里）

已有的缓存（做得不错）：

| 缓存 | 内容 | Key（与时间无关） | 失效 |
|---|---|---|---|
| `_BEFORE_LAYER_CACHE` | 未唱层：阴影+描边+底色 烘焙成透明 QImage | line text/font/style/颜色/描边/装饰 | LRU 64 |
| `_AFTER_GLOW_CACHE` | 整行 glow 模糊位图 | 同上 | LRU 64 |
| `_IMAGE_FILL_CACHE` / `_IMAGE_BRUSH_CACHE` | 图片填充 brush | 图片+尺寸 | LRU 16 |

逐帧仍然重算的部分（**热点 + 优化空间**）：

1. **已唱层 fill+stroke+stroke2**：`_paint_line_static` 每帧 `line_path = _line_text_path(...)`
   （`addText` 重建整行轮廓），再 `_paint_after_path` 在 clip band 下 `strokePath`/`fillPath`。
   → **几何逐帧不变，变的只有扫光 clip 边界**。这正是 libass 的 "clip gradient" 技巧适用处，
   而 before-layer/glow 已经这么做了，**唯独 after 主层没做**。
2. **Sayatoo 信号模块** `_paint_signal_lits`：逐帧画若干 `drawRect/Ellipse/RoundedRect`。
   形状简单、量小，但 countdown 状态逐帧变（active_index/phase/flash），属于「天然动态」，
   可优化空间有限（小图集化即可，优先级低）。
3. **入退场/逐字动画** `_paint_line` / `_paint_line_with_character_transition`：opacity/translate/缩放，
   属于「动态变换」，本就便宜（变换是几乎免费的，见 libass 结论）。
4. **每帧重建 `QFont`/`QFontMetrics`/path 对象**：Python 对象分配开销，量大时不可忽略。

### 1.4 分色(roles) 现状

- 当前 `_style_for_line(style, line)` 按 **`line.singer_id`（整行一个歌手）** 覆盖 font_family/
  font_size_px/颜色/描边/glow 等 → **字体/字号可「按行(按歌手)」不同**。
- 句内：`_paint_line_static` 整行只建**一个** `font` + 一个 `metrics`；唯一的句内字体变化是 `font_for`
  的「日文↔英数」family 切换，**且同一字号**。
- 关键：**分色颜色在烘焙位图时就画进像素了，扫光仍只是横向 clip**。所以只要把已唱层也烘焙，
  **分色不会增加任何逐帧成本**，cache key 带上 role 颜色/字体签名即可。这点对 P0 很重要。
- ⚠️ **当前不支持「同一句内逐段不同字体/字号」**（详见 §4.5 专题，这是已确认的新增需求）。

---

## 2. 瓶颈量化建议（先量再优化）

动手前务必建一个**基准测量脚手架**，否则无法评估收益、也无法防回退：

- 导出侧：固定一段样例字幕（含分色 + 信号 + glow + ruby），统计
  - 纯 `paint_frame` 单帧耗时（不含 ffmpeg）；
  - end-to-end 导出 fps（含 pipe + ffmpeg）；
  - 拆分：光栅化 / `constBits` 拷贝 / pipe 写入 各占比。
- 预览侧：`paintEvent`/`paint()` 单次耗时，播放时实际刷新率、掉帧率。
- 建议做成 `pytest -m bench` 或一个 `scripts/bench_render.py`，输出 CSV，方便 A/B。

经验先验（基于代码结构，待实测校准）：导出端**光栅化**和**8MB/帧 字节搬运**大概率是两座大山；
其中光栅化里「已唱层逐帧重描边」占大头。

---

## 3. 业界做法调研（可借鉴点）

### 3.1 libass / Aegisub（ASS 卡拉OK 的事实标准）

- Aegisub 只是编辑器；真正渲染器是 **libass**（C 写，mpv/VLC/ffmpeg 内置，跨平台，比 VSFilter 快很多）。
- **铁律一**："渲染性能的支配因素是**总位图尺寸**(total bitmap size)" —— 不是路径复杂度、不是动画数量。
- **铁律二**：多级缓存依赖链 **Font → Outline → Bitmap → Composite**，逐层复用；
  在某个时间戳取帧时，**改颜色/变换/淡入不增加成本**（底层位图被复用）。
- 卡拉OK：每个 shape 拆成最多 4 张**单色 alpha 位图**（`\1c` 填充 / `\3c` 描边 / `\4c` 阴影 /
  `\2c` 卡拉OK高亮），逐帧只做 **alpha 混合 + clip**。分色超过 4 色就拆多 run，但每 run 仍只光栅化一次。
- **"clip gradient" 技巧**：整行渲染一次缓存，再用 N 个不同 clip rect 复用 → 逐帧只是输出不同。
  **= 我们 P0 要补的「已唱层烘焙 + clip」**。
- libass **没有 GPU 后端**（[issue #704 至今 open]），它靠自研快速光栅器 + 激进缓存达到高性能。
  → 启示：**CPU 路径只要缓存做到位，就能很快**，不必为了性能强上 GPU。

### 3.2 After Effects / Premiere（图层合成器架构）

- AE 用 **effect graph + cross-render caching（跨渲染缓存）**：图里没变化的节点保留缓存，
  只有变化的节点重算；"靠近图顶的调整比靠近底部快"。
- 图层 buffer 只在单次渲染 pass 内持有，控制显存/内存占用。
- GPU 加速合成（混合、模糊、变换在 GPU 做）。
- **启示**：把"每个特效 = 一个图层/节点"，框架统一判定「该节点是否随时间变化」→ 决定缓存或重算。
  这正是让 **Sayatoo / ユートピア / 未来特效共存且各自可优化**的工程范式（见 §5 架构建议）。

### 3.3 SDF / MSDF GPU 文字渲染（GPU 路线的关键技术）

- 把字形预生成**(多通道)有符号距离场**纹理；着色器里 **一次纹理采样 + 简单阈值算术**即可得到
  填充 / 描边 / 阴影 / **柔光**，且**任意缩放都清晰**、动态改色零成本。
- msdfgen / msdf-c（基于 stb_truetype 的单文件实现）可离线/运行时生成图集。
- **启示**：若走 GPU，卡拉OK 扫光 = 着色器里按音节移动一个 UV 阈值；分色 = 每 quad 顶点色；
  描边+glow = 距离场算术（省掉多遍描边）。是「重特效 + 4K + 实时」的终极答案，但要重写"观感"。

### 3.4 Skia（skia-python）/ Qt RHI（中间路线）

- **Skia**：跨平台 2D 库（Chrome/Android/Flutter 引擎），有 **GPU 后端(Ganesh, GL/Vulkan/Metal)**、
  自带**字形图集缓存**、可**headless 离屏 GPU 渲染**。`skia-python` 提供 Python 绑定。
  绘制模型与 QPainter 高度同构（`QPainterPath→SkPath`、`strokePath→SkPaint stroke`、
  `QGraphicsBlurEffect→SkMaskFilter blur`），**移植成本远低于裸 GL**。
- **Qt RHI**：Qt 自己的硬件抽象（OpenGL/D3D/Metal/Vulkan）。`QRhiWidget`/`QCanvasPainter` 可做
  GPU 加速 2D。但 PyQt 暴露面有限、生态资料少。
- 注意：**`QPainter` 当前是软件光栅器**（Qt 官方明确："QPainter 目前为软件渲染设计，图形密集场景会成瓶颈"）。
  把 QPainter 画进 `QOpenGLWidget` 也只是合成走 GPU、光栅化仍在 CPU，提升有限。

### 3.5 Python 离屏并行渲染

- `QGuiApplication` + `QT_QPA_PLATFORM=offscreen` 可在**无显示器/worker 进程**里跑 `QImage`+`QPainter`
  （字体需要 QGuiApplication）。→ **多进程导出**可行：N 个 worker 各渲一段帧返回 raw RGBA，
  主进程按序喂 ffmpeg。近线性加速。
- `moderngl.create_standalone_context()` / skia-python 也支持 headless GPU，便于未来 GPU 导出。

---

## 4. 方案评估

> 评估维度：性能收益 / 改动量 / 风险 / **是否保留全部特效** / 扩展性 / 跨平台。
> 几个方案**不是互斥**的，P0 是所有路线的共同地基。

### 方案 A — P0：增量优化现有 QPainter 管线 ★推荐先做

不改架构，只补缓存与并行。**零特效损失**。

| 子项 | 做法 | 预计收益 | 风险 |
|---|---|---|---|
| **A1 烘焙已唱层** | 把 fill+stroke+stroke2 的「完整一行」烘焙成透明 QImage（与 before-layer 同构的 LRU 缓存，key 含 roles 颜色签名），逐帧把 `_paint_after_path` 改成 `drawImage`+`setClipRect(扫光带)` | 单帧光栅化大降（两次描边+一次填充 → 一次 blit）；**分色零额外成本** | 低，改动集中在一处，有 `test_subtitle_render_painter.py` 兜底 |
| **A2 只渲染字幕条带** | 按 style 布局算出文字 y 范围，只生成/序列化窄条（如 1920×360），ffmpeg `overlay=0:Y` 贴回 | 光栅化/拷贝/pipe 带宽按条带高度线性下降（常见 3×+） | 低-中，需正确处理竖排/双行/信号的纵向范围 |
| **A3 多进程导出** | `QT_QPA_PLATFORM=offscreen` worker 池，各渲一段帧 → 主进程按序写 ffmpeg | 近线性（8 核≈6–7×） | 中，需处理字体一致性、worker 生命周期、取消/进度 |
| **A4 缓冲复用 + 空帧短路** | 复用单块 QImage（`fill(0)`）；无活跃行帧直接写预存全零字节，跳过 paint | 减分配 churn；空段几乎免费 | 低 |
| **A5 对象缓存** | 缓存 `QFont/QFontMetrics`/line path（按 style+text），减少逐帧 Python 分配 | 中等，量大时明显 | 低 |
| **A6 预览脏区** | 预览只 `update(dirtyRect)`（字幕实际区域），降低每 tick 重绘面积 | 预览 CPU 降 | 低 |

- **优点**：风险最低、收益大、不动特效、各子项可独立上线/回退。
- **缺点**：painter.py 仍是 4756 行的巨石，新增特效仍要在里面塞逻辑（扩展性没改善）。
- **跨平台**：✅ 全平台。
- **开发路径**：A1 → A4/A5（同区域顺手）→ A2 → A6 → A3。建议每步配 §2 基准对比。

### 方案 B — P1：图层合成器 + 分层缓存重构 ★推荐作为中期主线

把渲染抽象成「图层栈 + 统一缓存框架」，参考 AE cross-render caching / libass composite cache。

核心模型：

```
Frame = compose([
    Layer.render(t)  for layer in active_layers(t)
])
# 每个 Layer 自述：
#   - static_key(style, content)         # 与时间无关的烘焙 key（None = 不可烘焙）
#   - bake(key) -> Bitmap                # 一次性烘焙（框架缓存）
#   - dynamic(t) -> {clip, transform, opacity}  # 逐帧只产出"怎么贴"，不重光栅化
```

- 现有特效逐个落为 Layer：`TextBeforeLayer` / `TextAfterLayer`(clip 扫光) / `GlowLayer` /
  `RubyLayer` / `SignalLitsLayer`(Sayatoo) / `TitleOverlayLayer` / 未来的 `KodbieaLayer` …
- 框架统一负责：LRU 缓存、失效（style/content hash）、烘焙/合成调度、条带裁剪、并行。
- 新增特效 = 实现一个 Layer 接口，**不用再碰巨石函数**，且自动获得缓存/并行红利。

- **优点**：扩展性质变（Sayatoo/ユートピア/未来特效即插即用）；缓存策略集中、可测；为 P2 GPU 铺平
  （Layer 接口可换 GPU 实现）。
- **缺点**：重构工作量中-大；需把 painter.py 现有逻辑搬迁、保证逐特效像素一致（要做回归对比图测试）。
- **风险**：中。务必**逐特效迁移 + 像素回归**（渲染参考帧 diff），用 env 开关 A/B 与旧路径并存。
- **跨平台**：✅。
- **开发路径**：先定义 Layer 接口与 compositor + 缓存框架 → 迁移 before/after 文本层（已是 P0 形态，最易）
  → glow/ruby → Sayatoo 信号 → title → 收口删旧路径。

### 方案 C — P2：GPU 后端（MSDF 图集 / skia-python） 按需，长期

两条子路线：

- **C1 skia-python(Ganesh)**：用 Skia 替换 QPainter 作为 2D 后端。绘制模型同构，移植成本相对低；
  自带字形图集 + GPU；可 headless GPU 导出。**中间路线**，性价比最高的 GPU 方案。
- **C2 裸 GPU（QRhi/OpenGL + MSDF）**：字形图集 + 着色器，扫光/描边/glow/分色全在 shader。
  天花板最高（4K/实时/超重特效），但要**重写所有特效的观感**，工作量最大。

- **优点**：实时 4K、重特效无压力、导出可 GPU 并行。
- **缺点**：大重写；需重做描边/glow/信号的视觉以达成与现有一致或更好；GPU 驱动/跨平台坑。
- **风险**：高。仅当 P0+P1 后仍不满足（例如要做实时 4K 预览或海量特效叠加）才上。
- **前置条件**：必须先有 P1 的 Layer 接口，GPU 才能作为"另一种 Layer 后端"渐进替换，而非推倒重来。

### 方案 D — libass/ASS 导出旁路 可选加速档，非主路径

普通卡拉OK（无 Sayatoo 信号、无超出 ASS 表达力的特效）时，由 `TimingTrack` 生成 `.ass`
（`\k` 逐音节、`\1c/\2c/\3c` 上色、分色用多 `\c` run、glow≈`\blur`+border、ruby 用定位文本），
交给 `ffmpeg -vf ass=xxx.ass` 一遍硬烧，**没有 Python 逐帧循环**。

- **优点**：导出可快一个数量级；渲染器是全球最经验证的 libass。
- **缺点（致命限制）**：**表达不了 Sayatoo 信号模块、ユートピア 等自定义特效**，无法与现有观感像素一致。
- **定位**：仅作"当前工程不含信号/自定义特效时"的**可选快速导出**，主路径仍是 QPainter/未来 GPU。
- **风险**：低（旁路，不影响主路径）。但要做好"能否走 ASS 旁路"的特性探测与 fallback。

---

## 4.5 专题 · 句内逐段不同字体/字号（已确认的新增需求）

> 背景问题："现在的缓存方式不允许在一句话里面字体、字号不一样,是真的吗?" —— **半真,但归因要纠正。**

### 4.5.1 事实核实（基于代码）

- **现象属实**：目前一句话**做不到任意逐段不同字体/字号**。
  - `_paint_line_static` 整行只建**一个** `font = _build_font(style)` + 一个 `metrics`
    （[painter.py 约 2009 行]）。
  - 该 `style` 来自 `_style_for_line(style, line)`，按 **`line.singer_id`（整行一个歌手）** 覆盖
    `font_family/font_size_px/...`（[painter.py 约 3939 行]）。→ **字体/字号能"按行(按歌手)"不同,句内不行。**
  - 句内唯一的字体变化是 `font_for` 的「日文↔英数」family 切换（`_make_font_for`），**且同一字号**。
- **但"是缓存不允许"这个归因不准确**：真正的限制在**布局/绘制路径假设了「一行一种字体一个字号」**；
  缓存只是**被动按这个模型建 key**——`_before_layer_cache_key` 里只放了单个 `font_sig` + 单个
  `latin_sig`（[painter.py 约 3464 行]），`_build_before_layer` 也只用单 font 烘焙。

### 4.5.2 关键结论：缓存方案与"句内混排"不冲突，反而是其理想底座

烘焙整行（P0 的 A1）本质是"把**实际字形**画进位图一次"。无论这些字形用了几种字体/字号，烘焙完都只是
**一张位图**，卡拉OK 扫光仍然只是**横向 clip**。所以：

> **"句内逐段混排字体/字号" 与 "管线优化" 是叠加、相互增强的，不是二选一。**
> bake-the-line（A1）天然支持混排；只需把布局改成逐段、并把 per-run 字体签名并入 cache key。

### 4.5.3 要做的工作（与缓存无关，缓存只需扩 key）

1. **布局逐段化**：per-char/per-run 用各自 `QFontMetrics` 取步进（`char_widths` 本就是 list，易改）；
   path 烘焙循环本就逐字，可逐字传不同 `QFont`。
2. **统一行盒与基线**（混排字号的真正难点）：句内字号不同会打破"单 `metrics.height()/ascent()`"的假设。
   需要改成**取各段的 `max(ascent)` / `max(descent)`** 来定：
   - before/after 烘焙位图的高度与基线偏移；
   - 卡拉OK 扫光 clip 的纵向范围（现用 `y - metrics.ascent()` / `metrics.height()`）；
   - `line_rect`、`_resolve_baseline_y`、双行/竖排几何。
   并确定**基线对齐策略**（底对齐 / 居中 / 视觉中线），作为风格项暴露。
3. **缓存 key 加 per-run 字体签名**：把单 `font_sig` 换成"逐段 `(family, size, weight, italic)` 的元组签名"，
   不同混排自然落不同缓存项；其余缓存逻辑（LRU、失效、blit+clip）不动。
4. **关联模块同步**：ruby 注音、信号模块的纵向锚点依赖行盒高度，需跟随统一行盒重算。

### 4.5.4 与路线图的关系

- 这本质是 roles 的 **Phase 3（逐段渲染）** 再叠加"字号也可变"。
- **建议在 P1（图层合成器重构）一并落地**：P1 把"行布局"抽成独立步骤后，逐段化与统一行盒是自然的扩展点，
  且 Layer 接口的 `static_key` 直接纳入 per-run 字体签名。
- 若要在 P0 阶段先行支持，也可行（A1 的烘焙天然兼容），但 §4.5.3 第 2 点的"统一行盒/基线"改动会散落在
  多处旧函数里，回归面较大——故**优先放 P1**。
- **验收**：在像素回归测试集中**新增"句内混排字体+字号"参考帧**（含分色、ruby、双行组合）。

### 4.5.5 评估小结

| 维度 | 说明 |
|---|---|
| 现状 | 句内仅 jp/latin family 切换且同字号；字体/字号只能整行(按歌手)变 |
| 根因 | 布局假设单字体单字号；缓存只是跟随，非根因 |
| 与优化的关系 | **正交且互促**——bake-the-line 是混排的理想底座 |
| 主要难点 | 句内混字号下的**统一行盒 + 基线对齐**（不是缓存） |
| 建议落点 | P1 图层合成器重构内一并实现（roles Phase 3 的超集） |
| 风险 | 中（纵向几何改动面广，靠像素回归兜底） |

---

## 4.6 专题 · layout 与 render 是否必须解耦（新增确认点）

> 问题："layout 和 render 是不是必须要解耦?现在项目内似乎是一个 layout 直接画。"

### 4.6.1 现状核实

属实，**当前 layout 与 render 是耦合的**：`_paint_line_static` 在同一个函数里**既算布局**
（`char_widths`、`x0`、`baseline`、`char_x_ranges`、`intervals`、`fill_segments`）**又直接绘制**
（建 path、画 before/after）。只有少数布局（如 `_resolve_sayatoo_line_layouts` 的信号 union）被单独抽出。
即"一次布局直接画"。

### 4.6.2 是否"必须"解耦？——分阶段回答

- **P0（增量优化）：不必须。** A1 烘焙已唱层、A2 条带、A4 缓冲复用都能在现有耦合结构里就地做。
- **P1 及以后：实质必须。** 你这次确认的四个诉求**全部**指向"layout 必须独立于 render"：

| 诉求（你的澄清） | 为什么需要解耦 |
|---|---|
| **预览=导出一致**（约束 4） | 预览与导出调**同一个 layout**（纯几何，确定性），只有最后 paint 的画布不同 → **一致性由构造保证**，而不是靠两边各写一遍对齐 |
| **ユートピア 字体跳动 + 视觉跨行**（约束 1★） | 跳动 = 逐帧给字形一个**仿射变换**(位移/缩放/旋转)。layout 先算"静止盒子"，animation 段叠加变换，paint 段 blit 已烘焙字形 + 变换 → **不必逐帧重光栅化**（现状是每帧重光栅化，见 §5.2）。视觉跨行按单行 group 的保守 bounds 覆盖，不做跨行联合 scope（见 §5.2.4） |
| **句内混排字体/字号**（§4.5） | layout 段统一算 per-run 盒子 + max(ascent/descent) 行盒；render 段无需关心混排 |
| **缓存 + 多进程并行** | layout（几何）与 bake（位图）都可按"静态签名"缓存；导出时一行只 layout 一次，各帧只跑 animation+paint，天然适合并行 |

> 结论：**P0 可以先不解耦拿快钱；但要达成你要的最终形态，P1 必须把 layout 抽成独立的纯函数阶段。**
> 这也正是 libass（shaping/layout → bitmap → composite 三层）和 AE（effect graph）的通用结构。

### 4.6.3 建议的三段式（layout → animation → paint）

```
# 1) layout：纯几何，无 QPainter，确定性 → 可缓存、预览/导出共用、可并行
FrameLayout = layout(track, style, t_window)
#   每行: [GlyphBox(x, y, font, advance, run_id), ...]，line_box(max_ascent/descent)，
#         fill_segments，ruby_boxes，signal_geometry …

# 2) animation：给定 layout + t，产出"逐帧怎么变"，仍是纯数据（无光栅化）
FrameAnim = animate(FrameLayout, t_ms)
#   per-glyph transform(跳动/入退场)、opacity、karaoke 扫光 clip、信号 active 状态 …

# 3) paint：消费 layout + anim，blit 已烘焙位图 + 套 transform/clip（后端可换）
paint(device, FrameLayout, FrameAnim)   # device = 预览 widget / 导出 QImage / (未来)Skia
```

- **一致性**：预览与导出 1、2 段**完全相同**，只有第 3 段的 device 不同 → 差异天然最小。
- **后端可换**：第 3 段抽象成接口后，未来要给"导出端"接 Skia/GPU，只换 paint 实现，layout/anim 不动，
  **观感不漂移**（直接满足约束 4）。
- **预览不用 GPU**：第 3 段在预览用现有 QPainter 即可；性能靠 layout/bake 缓存而非 GPU。

### 4.6.4 与既有代码的迁移关系

- 把 `_paint_line_static` 拆成 `layout_line()`（返回 GlyphBox/行盒/fill_segments）+ `paint_line()`（消费）。
- `_resolve_sayatoo_line_layouts`、`_signal_lit_groups` 已是"半 layout"，并入 layout 段。
- before/after/glow 的 bake 函数保持不变，只是改由 layout 结果驱动。
- 风险：纵向几何（baseline/行盒/clip 带）改动面广 → 靠 §7 像素回归兜底，env 开关与旧路径并存。

---

## 5. 针对「特效可扩展性」的架构建议（回应核心诉求）

### 5.1 Effect/Layer 插件接口（建立在 §4.6 三段式之上）

你明确说"还要 Sayatoo + ユートピア + 未来更多特效"。所以**比单点提速更重要的是把特效做成可插拔**。
建议在 P1 落地一个 **Effect/Layer 插件接口**，落在 §4.6 的 `layout → animation → paint` 三段上，
并把"静态可烘焙 / 逐帧动态"二分作为一等公民：

```python
class SubtitleLayer(Protocol):
    def active_window(self, ctx) -> list[tuple[int,int]]: ...    # 该层何时出现（决定可见性/裁剪）
    def layout(self, ctx) -> LayoutData: ...                     # 纯几何（无 QPainter）：盒子/行盒/分段
    def static_key(self, ctx) -> Hashable | None: ...            # 与 t 无关的烘焙 key（含 per-run 字体签名）；None=纯动态
    def bake(self, ctx, key) -> BakedBitmap: ...                 # 一次性烘焙（框架缓存+LRU）
    def animate(self, ctx, layout, t_ms) -> AnimState: ...       # 逐帧只产 clip/transform/opacity（无光栅化）
    def vertical_bounds(self, ctx) -> tuple[int,int]: ...        # 供条带裁剪(A2)与脏区(A6)
```

- 卡拉OK 文本 = `bake(整行) + animate(扫光 clip)`；
- Sayatoo 信号 = 多个 `bake(单个灯/列)` + `animate(active_index/phase 选哪些 + 闪烁 alpha)`；
- **ユートピア（字体跳动，视觉跨行但按单行拆分）= `bake(字形一次)` + `animate(逐字仿射变换)`**，作用域为单行内 group（见 §5.2）；
- 句内混排 = `layout` 段算 per-run 盒子，`bake` 把混排字形画进一张位图，`animate` 仍只做扫光 clip；
- 未来特效 = 各自实现接口，**自动获得缓存、条带裁剪、并行、一致性、(未来)GPU 后端**。
- 框架集中管：缓存与失效、合成顺序(z)、条带范围聚合、worker 调度。

这样"加特效"从"改 4756 行巨石"变成"加一个 Layer 类 + 测一张参考帧",这是长期可维护性的根本。

### 5.2 专题 · ユートピア(Utopia)「字体动态跳动 + 跨行」如何融入 bake 缓存模型

> 已是现有特效。现状：`_paint_line_with_character_transition` 对 `transition.effect=="utopia"` 用
> `_transition_char_state` 产出逐字 **仿射变换**(opacity/dx/dy/rotation/scale_x/scale_y/skew_y)，
> 然后**每帧 `addText` 重建 path + `QTransform.map` + 重绘**（见 painter.py 约 2379–2409 行）——即**每帧重光栅化**。
> 这正是 §5.2.1 要改成"bake 一次 + 逐帧变换"的典型例子。

#### 5.2.1 把"逐帧变化"分四类（绝大多数落 a/b/c，只有 d 才需逐帧重光栅化）

| 类别 | 逐帧变化 | 处理 | 例子 |
|---|---|---|---|
| (a) **裁剪** | 只是显示范围推进 | bake 一次 + 逐帧 `setClipRect` | 卡拉OK 扫光 |
| (b) **变换/透明** | 位移/旋转/缩放/淡入淡出 | bake 一次 + 逐帧 `QTransform`/`opacity` | 入退场动画、**字体位移/旋转跳动** |
| (c) **离散状态选择** | 在少数预烘焙状态间切换 | bake 一组 + 逐帧选 index | 信号灯亮/灭 |
| (d) **连续重光栅化** | 字形轮廓本身连续改变 | 见下方缓解策略 | **字号连续脉动**、字重 morph |

ユートピア 现用的是 **位移+旋转+skew（属 b）+ 入场/扫光的缩放过冲（1.3×/1.15×，属 b 含缩放）**：

- **位移/旋转/skew** → 纯 (b)：字形 bake 一次，`animate` 每帧给该字一个 `QTransform`，
  paint 段 `drawImage` 带变换，**逐帧成本≈一次 blit**。
- **缩放过冲（放大→收缩）** → 走"**超采样 bake + 逐帧缩放**"：bake 在**过冲后的最大字号**（如 1.3×），
  逐帧缩小（清晰、放大不糊）。这样把现状的"每帧 addText 重光栅化"降为"一次 blit + transform"。
- 若未来出现"字号/字重**连续脉动**"这种真 (d)，缓解：(1) 量化成 N 档字号各 bake 一份（退化成 c，逐帧选档）；
  (2) 超采样 bake 缩放；(3) 只对"正在跳动的少数字"逐帧重 bake（跳动通常只作用于当前唱到的字，量很小）。

> 设计要点：跳动是 **animation 段**的职责（产出 per-glyph transform/选档），**不污染 layout 段的静止盒子**；
> 这样跳动与"句内混排/缓存/并行/一致性"全部解耦。**这也是为什么 §4.6 的解耦对 ユートピア 是刚需。**

#### 5.2.2 现状 vs 目标

| | 现状 | P1.c 目标 |
|---|---|---|
| 逐帧 | `addText` 重建 path + `transform.map` + 重绘 | bake 一次 + 逐帧 `QTransform` blit |
| 缩放过冲 | 每帧重光栅化 | 超采样 bake（最大字号）+ 逐帧缩放 |
| 作用域 | 单行内的字符组（ruby group） | **单行内 group；视觉跨行只扩 bounds**（见 §5.2.4） |

#### 5.2.3 关于精度/观感

bake+变换 与 每帧重光栅化 会有**极小的边缘抗锯齿差异**（放大时尤其）。用超采样 bake（按最大过冲尺寸）可把差异
压到肉眼不可见。务必纳入 §7 像素回归（utopia 入场/扫光/退场各取若干帧对比）。

#### 5.2.4 ★ "跨行"对架构的影响（重点）

**2026-06-23 产品决策更新**：如果一个短语因为排版视觉上跨到两行，当前不把它作为一个跨行动画单元合并处理；
按**单行拆分**。也就是说，utopia 的 `group` scope 只在单行内聚合 ruby/主字组，`scope_id`
必须包含行边界（如 `line_start` / `line_end`），防止两行里相同短语被合成一个跨行 group。
放大过冲侵入相邻行只作为该单行 group 的保守 bounds 覆盖，不驱动跨行动画同步。

早期方案曾按"跨行联合 group"设计；当前决策改为**视觉跨行但按单行拆分**。仍然需要在合成器里引入
**"特效作用域(scope)"** 概念，但 utopia 的 `group` 只聚合单行内的 ruby/主字组：

- **scope 级别**：`glyph`（单字）/ `line`（单行）/ `group`（单行内短语/熟字训/ruby 组）/ `frame`（全屏）。
  ユートピア 的动画单元是单行内 `group`，普通卡拉OK 扫光是 `line`，标题 overlay 是 `frame`。
- **layout 段**：产出单行内 group 的联合盒子；`scope_id` 必须包含行边界，避免相邻行相同短语被误合并。
- **bake/clip**：不做跨行联合裁剪；放大过冲或位移侵入相邻行时，用当前行 group 的保守 bounds 覆盖。
- **active_window/可见性**：按行内 group 判定；短语折到两行时拆成两个 group，各自跟随所在行。
- **条带渲染(A2) 与脏区(A6)**：纵向范围聚合当前行 group 的保守 bounds；不再计算跨行 group 联合高度。

> 这进一步印证 §4.6：**layout 必须独立于 render，且要有明确作用域**。Layer 接口的
> `active_window/layout/vertical_bounds` 都带 `scope` / `scope_id`，框架按 scope 聚合几何与裁剪范围。

### 5.3 待你确认（ユートピア 行为细节）

1. 缩放过冲的最大倍率是否就是现有常量（入场 `1.3×`、扫光 `1.15×`）？决定超采样 bake 的目标尺寸。
2. "跨行"具体形态：短语折到两行时按单行拆分；上一行过冲视觉侵入相邻行时，只扩当前行 group 的保守 bounds。
当前假设：跳动以 (b) 仿射 + 超采样 bake 处理；视觉跨行按单行 group 拆分，
不做跨行联合盒子。

### 5.2.6 实测与决策：ユートピア bake 化（P1.c-1）

**实测（1080p、glow+stroke 的 utopia 行、入场+扫光阶段帧）**：单帧 ≈ **4.0 ms**。
逐项验证发现：

- 先尝试的"安全优化"——缓存 `_glyph_run_path` 的字形轮廓（避免每帧 `addText` 重取轮廓）——
  **无可测收益**（4.005 → 3.971 ms，噪声内）。说明 utopia 的逐帧成本由**光栅化**
  （`strokePath` / `fillPath` / glow 模糊）支配，而非轮廓提取。该缓存已回退（不留死代码）。
- 因此 utopia 唯一真实的提速是 **bake 一次 + 逐帧变换**（把每帧的两次描边+填充+glow 光栅化
  降为一次 blit+QTransform，预计 ~4×）。但 bake 出的位图经 `QTransform` 缩放会引入**边缘抗锯齿
  软化**（放大相位尤甚）——即**改变 flagship 特效的像素**。

**张力**：这与产品方既定约束「**保留当前特效功能** + 导出/预览**差异尽量小**」直接冲突——
bake 化是 perf 与 视觉保真 的取舍，**不是纯透明优化**。故 P1.c-1 **挂起待确认**，不擅自改 flagship 观感。

**若确认要做，推荐实现路径（可逐步、每步像素/行为回归）**：
1. **超采样 bake**：按固定 `S=2×`（覆盖 1.3× 入场过冲且始终降采样→锐利）烘焙 group 的
   before/after/(glow) 位图。新增 `_build_glyph_run_layer` 的 `supersample` 变体（字号×S、
   坐标×S、画进 S² 面积位图）。
2. **逐帧合成**：`painter.save(); setClipRect(屏幕系扫光带)` →（Qt clip 在设时即落 device 空间，
   后续 transform 不动它）→ `setTransform(group_transform ∘ scale(1/S), combine=True)` →
   `drawImage(before/after/glow)`。before 全画、after+glow 按扫光带 clip——复刻
   `_paint_char_karaoke_stack` 的 before→glow→after 顺序与裁剪。
3. **回归**：utopia 入场/扫光/退场各取帧，behavior 哈希断言保持；新增 bake-vs-vector 的
   容差像素对比（SSIM / 允许小阈值），并请产品方目视签收一次"软化是否可接受"。
4. **作用域**：复用 §5.2.4 已落定的"单行 group"（不跨行）。倍率沿用现有常量（§5.3 第 1 项）。

**已实现范围（opt-in 当前版本）**：覆盖 utopia **主文本**（role 与 plain 两条 transition 路径）。utopia
**ruby** 仍走逐帧 vector（小、glyph 少，perf 可忽略）；开关开启时主文本为 bake（边缘略软）、ruby 为 crisp
vector，存在极轻微观感不一致——属已知的次要 follow-up，待确有需要时再把 ruby transition 也 bake 化。

---

## 5.4 专题 · GPU 与 CPU 渲染的差异（你要的：到底差在哪，写进文档）

> 你问"GPU 和 CPU 预览/渲染/导出的差异是什么"。下面分**(A) 性能差异**和**(B) 观感/正确性差异**两面讲清，
> 并给出**为什么本项目当前结论是"预览与导出统一用 CPU(QPainter)，GPU 只作可选导出后端"**。

### 5.4.1 名词先对齐（本项目语境）

- **CPU 渲染** = 现状：`QPainter` **软件光栅器**把矢量/字形扫描转换成像素（Qt 官方明确 QPainter 为软件渲染设计）。
  预览的字幕层、导出的每帧都走它（预览的**视频**另由 `QGraphicsVideoItem` 走原生 GPU，但那只是"播放视频"，
  不是"渲染字幕"）。
- **GPU 渲染** = 候选：把字形做成**纹理图集**，用着色器画带纹理的四边形（OpenGL/Vulkan/Metal/D3D，
  或经 Skia Ganesh / Qt RHI 抽象）。

### 5.4.2 (A) 性能差异

| 维度 | CPU(QPainter 软件光栅) | GPU(图集 + 着色器) |
|---|---|---|
| 单帧光栅化 | 受字形数/描边/blur 影响，单核 | 并行像素填充，量大时快很多 |
| 扩展到 4K / 海量特效 | 吃力 | 优势明显 |
| **本项目实际瓶颈** | 主要是"**每帧重光栅化**"，靠 bake 缓存即可大幅缓解（见 P0/P1） | — |
| 并行导出 | 多进程即可近线性（约束已满足） | 需 headless GPU 上下文，复杂 |
| 结论 | **缓存做到位通常已够**（libass 无 GPU 也快） | 仅当 CPU 路径仍不够时才需要 |

> 关键认知：本项目的慢**不是"CPU 比 GPU 慢"那么简单**，而是"该缓存的没缓存、单核串行"。先把 P0/P1 做了，
> CPU 路径大概率就满足你的目标（导出尽快、预览 24–60fps）。GPU 是"还不够时的下一档"，不是必选。

### 5.4.3 (B) 观感 / 正确性差异（**这是要统一渲染器的根本原因**）

同一套矢量+样式，CPU 与 GPU 画出来**不会逐像素相同**，常见差异：

| 差异点 | 说明 | 后果 |
|---|---|---|
| **抗锯齿算法** | CPU 用高质量覆盖率扫描转换 AA；GPU 常用 MSAA / 着色器覆盖近似 | 文字/描边**边缘软硬不同**，细字尤其明显 |
| **字形光栅化与 hinting** | CPU 经 FreeType/系统字体光栅器（带 hinting、子像素定位）；GPU 图集多为"某尺寸 bake 一次再缩放" | 字形**粗细/清晰度/对齐**不同，缩放时 GPU 略糊 |
| **渐变插值色彩空间** | sRGB 还是 linear 空间插值，两端实现常不一致 | **渐变中段颜色/条带**不同（你的 fill 渐变、split 渐变会受影响） |
| **模糊/glow** | CPU `QGraphicsBlurEffect`（高斯）vs GPU 着色器 blur，半径语义/衰减不同 | **glow 大小与柔和度**不同 |
| **混合与 premultiplied alpha 舍入** | 半透明叠加的取整方式不同 | 多层叠加后**累积 1–2 级色差**，半透明边缘可见 |
| **裁剪边缘 AA** | 矩形 clip：CPU 多为硬边，GPU 可能 MSAA 软边 | 扫光交界、条带边缘**锯齿/羽化**不同 |
| **浮点精度** | GPU fp32/fp16 vs CPU double | 亚像素**定位舍入**差异，动画时轻微抖动 |
| **跨厂商不确定性** | 同一 GPU 代码在 NVIDIA/AMD/Intel/驱动版本上结果可能不同 | **导出在不同机器上不可复现**；预览(本机 GPU) vs 导出(另一路径)更难一致 |

> 一句话：**CPU 渲染是确定性的、跨机一致的；GPU 渲染受后端/驱动影响、且与 CPU 有系统性观感差异。**

### 5.4.4 对"预览 vs 导出一致性"的直接结论（呼应约束 4）

- **若预览用 GPU、导出用 CPU（或反之）** → 必然出现 §5.4.3 的系统性差异，且 GPU 端还跨机不稳定 →
  违背"预览=导出尽量一致"。
- **若预览与导出都用同一套 CPU(QPainter) 渲染** → **一致性由构造保证**（同一代码、同一光栅器），
  这也正是你说"预览可不用 GPU"所允许的最优解。
- **若将来一定要 GPU 加速导出** → 在 §4.6 三段式下**只替换 paint 后端**，但要付出"重新对齐观感 + 像素回归"的成本；
  且**预览仍保持 CPU**，意味着此时预览与导出会有 §5.4.3 的小差异 → 需产品方接受。
- 折中（若 P2 走 Skia）：让**预览与导出都改用 Skia**（Skia 可 CPU 也可 GPU），至少"同一渲染库"能把差异收窄；
  但仍受 CPU/GPU 后端差异影响，不如纯 CPU 共享来得稳。

### 5.4.5 给接手者的执行性建议

1. **默认**：预览 + 导出共用 CPU QPainter 渲染器（三段式 layout/anim 完全共享，paint 都用 QPainter）。
2. GPU 仅在"P0+P1 后导出仍不够快"时，作为**导出专用可选后端**引入，并：
   - 固定一个参考后端做"基准观感"，其余后端对齐到它；
   - 把 §5.4.3 各项纳入像素回归阈值；
   - 文档标注"GPU 导出与 CPU 预览存在已知小差异"。
3. 预览侧若要更快，优先"**bake 缓存 + 脏区**"，而不是上 GPU。（注：原"降输出分辨率"档位已于 2026-06-24 移除，预览恒满分辨率；提速改走 §9 缓存方案。）

---

## 6. 推荐路线图与工作量评估

| 阶段 | 内容 | 相对工作量 | 预计收益 | 依赖 | 状态 |
|---|---|---|---|---|---|
| **P0.0** | 基准测量脚手架（§2） | S | 让后续可量化 | — | ✅ 完成 |
| **P0.1** | A1 烘焙已唱层 + A4 缓冲复用/空帧 + A5 对象缓存 | M | 单帧光栅化大降 | P0.0 | ✅ 完成（A1✅ / A4✅ / A5 跳过） |
| **P0.2** | A2 条带渲染（方案 A 单条并集 + 方案 B 多条分离带） | M | 拷贝/pipe/光栅化按高度线性降 | P0.1 | ✅ 完成（A=单条；B=多条分离带 ✅，打包单 pipe + split/crop/overlay） |
| **P0.3** | A3 多进程导出 | M–L | 近线性 ×核数 | P0.1 | ✅ 完成 |
| **P0.4** | A6 预览脏区 + ~~预览输出分辨率可调~~ + 实时帧率显示（约束 3★） | S–M | 预览满足 24–60fps 目标、可读数 | — | ✅ 完成（分辨率档位控件 2026-06-24 移除，见 §6.1）|
| **P1.a** | **layout/render 解耦**（§4.6 三段式：layout→animation→paint） | M–L | 一致性/缓存/并行/跳动的总枢纽 | P0 | ✅ 完成（横排统一 glyph-run / 信号 geometry / 竖排 layout） |
| **P1.b** | 图层合成器 + 插件接口（§5），逐特效迁移 + 像素回归 | L | 扩展性质变 + 缓存集中 | P1.a | ✅ 完成（b.0✅ 骨架 / b.1✅ 文本主体 / b.2✅ glow/ruby / b.3✅ 标题/信号 / b.4✅ bounds 接线 / b.5✅ **竖排整条路径（主文本+ruby）迁入合成器，7.4× 提速**） |
| **P1.b'** | **特效作用域(scope)抽象**（glyph/line/group/frame，支持 ユートピア group）（§5.2.4） | M | 特效作用域地基 | P1.a | ✅ 完成（b'.0✅ scope box 地基 / b'.1.1✅ scope_id 聚合 / b'.1.2✅ utopia bounds 接线 / b'.1.3✅ 跨行按单行拆分） |
| **P1.c-2** | **句内混排字体/字号**（§4.5，roles Phase 3 超集） | M | 满足新增特效需求 | P1.a | ✅ 完成（`_layout_role_line` 逐段字体/字号 + 统一行盒 max(ascent/descent) + per-run bake；静态/卡拉ok/transition 三路径均 honor 逐字 font；测试 `test_paint_frame_applies_inline_role_styles_with_mixed_font_sizes` 等）。基线对齐策略仍按底/居中/顶三档（`_resolve_role_baseline_y`），逐段自定义对齐为 §8 预留项 |
| **P1.c-1** | **ユートピア 跳动改 bake+变换、缩放走超采样**（§5.2） | M | utopia 帧 perf | P1.a, P1.b' | ✅ 完成（**opt-in，默认关**）。`KROK_SUBTITLE_UTOPIA_BAKE=1` 开启：bake 一次（2× 超采样）+ 逐帧 `QTransform` blit，**实测 utopia 单帧 4.09→1.09ms（3.75×）**。默认关→走原逐帧光栅化，像素不变（保留特效）。详见 §5.2.6 |
| **P2（可选）** | **仅导出加速**：方案 C1 skia-python(Ganesh) headless GPU；**预览不 GPU 化**（约束 4★） | XL | 导出更快；非必需 | P1 | ⬜ 待做 |
| **可选** | 方案 D ASS 旁路（仅无信号工程的快速导出） | M | 导出 ×10+（受限场景） | 独立 | ⬜ 待做 |
| **P1.d** | **特效字幕缓存重构**：动画路径并入 LayerCompositor + glow 缓存复用（A 档）→ utopia 量子化重栅（B 档）→ 原语契约。详见 **§9（含 §9.7 执行清单）** | A:M / B:L | 解决 utopia+glow 掉帧 + 延展性 + 消除软化 | P1.b | ⬜ **待做（下一步主线）** |

> 工作量：S≈1–2 天，M≈3–5 天，L≈1–2 周，XL≈数周（含视觉对齐与调优）。仅供排期参考，需结合实测。

### 6.1 实施进度日志

> 持久进度记录（session 任务面板是临时的，以此为准）。每条标注 commit + 实测。

- **2026-06-23 · P0.0 完成**（`f5ba70a`）：建 `scripts/bench_render.py`——offscreen、无 GUI、可 A/B
  的单帧光栅化基准，覆盖 plain/roles/glow/ruby/signals/full 六场景，输出 CSV（`.bench/` 已 gitignore）。
  1080p 基线：plain 1.82ms、roles 4.29ms、glow 1.62ms、signals 2.25ms、**full 15.97ms（仅 54fps）**。
- **2026-06-23 · A1 烘焙已唱层 完成**（普通路径 `cd0984e` + 分色路径 `561ccc5`）：把"已唱"主体层
  （阴影(非glow)+stroke2+stroke+底色）像未唱层一样烘焙成 LRU 缓存的透明 QImage，逐帧只做一次
  `drawImage` + 在扫光锋面做半平面 clip，取代逐帧重建整行/逐段 `addText` path 并多次
  `strokePath/fillPath`。分色路径按 run 烘焙（run 内多字体/字号，key 带逐字形字体签名 + 角色颜色），
  已唱 glow 仍逐帧（与普通路径一致）。**普通与分色现共用同一套烘焙 + clip 机制，扫光锋面行为一致**
  （彻底统一仍待 P1.a 解耦）。1080p 光栅化实测：plain 1.82→0.40ms、roles 4.29→2.00ms、glow
  1.62→0.48ms、signals 2.25→0.77ms、**full 15.97→4.05ms（fps 54→153）**。像素回归
  `tests/test_subtitle_render_painter.py` 105/105 通过。
- **2026-06-23 · A4 缓冲复用 + 空帧短路 完成**（`93a81b0`）：renderer 导出循环复用单块 QImage
  （不再每帧分配 ~8MB），无可见内容的帧直接写预存全透明字节（省 fill + 光栅化 + 拷贝）。新增
  `frame_has_content()`，与 `paint_frame_to_painter` 早退条件共用 `_resolve_visible_content()`。
  导出循环微基准（1080p60 含静默段）：2.57→1.15 ms/帧（2.2x）。**P0.1 完成**（A1+A4；A5 跳过）。
- **2026-06-23 · A2 条带渲染（方案 A）完成**（`f36df03`）：导出前预扫整段，求所有可见字幕内容的
  纵向并集（采样=均匀网格 + 每行起止动画极值，numpy alpha 行扫描），只把窄条作为 rawvideo 输入
  喂 ffmpeg、`overlay=0:y_top` 贴回全幅背景。**条带由实际渲染像素决定，自适应任意行数 / 多字幕来源 /
  信号注音，不写死行数**（3 行布局、上2下2 双来源都自然覆盖）；并集 ≥85% 全高或全空退回整帧；
  yuv420p 友好（top 下取偶 / height 上取偶）；`KROK_SUBTITLE_RENDER_STRIP=0` 可关。回归测试断言
  条带像素 = 整帧对应区域逐像素一致。1080p60 单行居中：条带 124/1080 行，字节/帧 8100→930KB(8.7x)、
  循环 1.79→0.51 ms/帧(3.5x)。
- **2026-06-23 · A3 多进程导出 完成**（`230734a`）：帧数 ≥240 时用 spawn worker 池并行渲帧，
  主进程 imap 按序收回喂 ffmpeg；否则单进程。**worker 继承父 QT_QPA_PLATFORM（不强制 offscreen），
  保证字体与预览/单进程一致**；进程数=CPU 核数(封顶 8)或 `KROK_SUBTITLE_RENDER_WORKERS`；
  chunk 按 64MB 封顶控内存。新增测试断言**多进程拼接输出与单进程逐字节一致**（含 worker 字体一致性）。
  收益随核数 / 工作负载：重负载(4K/密集特效/慢 CPU)接近线性加速；典型 1080p 因 A1+A2 后光栅化已很轻、
  导出瓶颈转向 ffmpeg 编码，提升有限（见下「优先级提示」）。
- **★ 优先级提示**：A1+A2 后 Python 光栅化已不是典型 1080p 导出的瓶颈（ffmpeg 编码才是、且已并行）。
  **P0.4 预览优化** 与 **P1.a 解耦**（架构枢纽 + 彻底统一普通/分色两条路径）价值更高，建议优先于继续压榨导出。
- **2026-06-23 · P0.4 预览优化完成**（`0e776b7`）：默认 `PreviewGraphicsView`
  的字幕层时间更新使用 `frame_vertical_bounds()` 做旧/新 dirty rect 合并刷新（不可判定路径回退整层 update）；
  `TransportBar` 新增实时 `FPS` 读数，按实际 subtitle layer paint 次数滚动统计；新增预览分辨率档位
  `1/1` / `1/2` / `1/4`，只降低预览字幕层光栅化分辨率，仍按完整输出坐标布局并缩放贴回，不影响导出宽高。
  raster fallback 同样支持预览分辨率档位，但仍按整 widget paintEvent 刷新。回归：
  `python -m pytest tests/test_subtitle_render_transport.py tests/test_subtitle_render_layers.py tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
  → 158 passed。
- **2026-06-23 · P1.a.1 普通行 layout/paint 解耦 完成**（`348f74e`）：把 `_paint_line_static` 普通行
  分支拆成纯几何 layout 段（`_layout_plain_line` → `_PlainLineLayout`：字符几何/基线/fill_segments，
  **不接收 t_ms、可缓存**）+ paint 段（`_paint_plain_line_layers` 消费 layout）；transition/扫光带是
  animation 段。纯结构重构，输出逐像素不变（painter 106/106）。这是三段式的雏形与模板。

- **P1.a 分步计划（持久化，逐步推进 + 每步像素回归）**：
  - **a.1 ✅** 普通行 layout/paint 解耦（数据模型 `_PlainLineLayout` + 纯几何 layout 函数）。
  - **a.2 ✅**（`03dc997`）分色路径也三段化：`_layout_role_line` → `_RoleLineLayout`（逐段多字体 glyph
    列表 + 基线 + fill_segments，不依赖 t_ms）+ `_paint_role_line_layers`。字段与 a.1 同名同义。测试断言
    分色 layout 是纯几何、glyph 逐段带自身 font（句内混排地基）。
  - **a.3 ✅**：把 `_PlainLineLayout` 与 `_RoleLineLayout` 合并成**单一 glyph-list 模型**
    （普通行=所有 glyph 同 style 的特例），统一 paint 段消费，**退役并行的 `_paint_role_line_static`**；
    竖排（縦書き）/ 信号几何也归入 layout 段。这一步真正落地「一条路径」。
    - **a.3.1 ✅**（`1b40cc6`）合并横排 layout 数据模型：删除并行的 `_PlainLineLayout` /
      `_RoleLineLayout`，统一为 `_LineLayout` + `_TextLayout` + `_GlyphLayout`；普通行也产出
      glyph-list layout（单 style 特例），分色行继续保留逐 glyph 字体/字号。纯结构增量，现有 paint
      行为保持不变。回归：`python -m pytest tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
      → 123 passed。
    - **a.3.2 ✅**（`f1eb716`）统一横排静态 paint 段与 bake cache key：普通/分色都按连续同视觉
      style 的 glyph run 消费 `_LineLayout`，统一 run 级烘焙 + 半平面 clip blit；删除旧
      `_paint_plain_line_layers` / `_paint_role_line_static` 双 dispatch，以及旧整行 before/after/glow cache。
      回归：`python -m pytest tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
      → 123 passed。bench（本轮基线 → a.3.2）：plain paint 0.675→0.667ms；roles 2.545→1.559ms；
      full 4.475→3.760ms。
    - **a.3.3 ✅** 竖排（縦書き，`_paint_line_vertical`）与 Sayatoo 信号几何（`_signal_*` /
      `_resolve_sayatoo_line_layouts`）归入 layout 段。此项涉及不同几何坐标系与动态信号状态，
      从 a.3.2 拆出，避免破坏横排逐像素回归。
      - **a.3.3.1 ✅**（`d8c0c13`）Sayatoo 信号几何归入 layout：`_SayatooLineLayout`
        增加 `line_style` / `metrics` / `total_w` / `signal_y`，`_signal_lit_groups` 有 layout
        时复用这些几何产物，不再重复排字；动态倒计时状态仍留在 paint 阶段。回归：
        `python -m pytest tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
        → 123 passed。bench：signals paint 1.093→1.001ms；full 3.760→3.802ms（同量级波动）。
      - **a.3.3.2 ✅**（`adc4cf4`）竖排 layout：新增 `_VerticalLineLayout` /
        `_layout_vertical_line`，把 `_paint_line_vertical` 的字体、列位置、字符格、字形路径、
        注音目标等几何抽为不依赖 `t_ms` 的 layout；paint 阶段只消费 layout 与当前扫光带。
        为控制风险，暂不强行并入横排 glyph-run paint。回归：`pytest ... -k vertical`
        → 8 passed；完整相关回归 → 124 passed。bench：plain 0.666ms；roles 1.346ms；
        signals 0.908ms；full 3.386ms。
  - 之后接 **P1.b**：把各特效（文本/glow/ruby/信号/标题）落成 Layer 接口 + scope 抽象（utopia 单行 group）。
- **下一步**：进入 **P1.b 图层合成器 + 插件接口**：先定义 Layer/Compositor 骨架，再迁移横排文本 before/after/glow 与 ruby。
- **2026-06-23 · P1.b.0 图层合成器骨架完成**（`7199189`）：新增
  `engine/layers.py`，定义 `LayerContext` / `BakedLayer` / `LayerAnimation` /
  `SubtitleLayer` / `LayerCache` / `LayerCompositor`。当前支持 z-order、`active_window`、
  静态 bake LRU 缓存、动态 layer 分支与 `vertical_bounds` 聚合；尚未接管 `painter.py`
  的具体特效输出。回归：`tests/test_subtitle_render_layers.py` → 6 passed；原指定回归
  → 124 passed。
- **P1.b 分步计划（持久化，逐步迁移 + 每步像素回归）**：主体已完成。剩余未完全 layer 化的
  竖排 ruby / utopia transition 不再塞回 P1.b，转入 P1.b' scope 与 P1.c 动态特效阶段。
  - **b.0 ✅** Layer/Compositor 骨架 + 缓存框架。
  - **b.1 ✅** 横排文本静态层迁移：先把 before/after 主体 run layer 包成 Layer，保持现有
    `_paint_line_layers` 输出逐像素不变。
    - **b.1.1 ✅**（`c97e409`）横排 glyph run 的 before/after 主体层接入
      `LayerCompositor + LayerCache`；after glow 暂作为动态 layer 保持原绘制逻辑。回归：
      `python -m pytest tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py tests/test_subtitle_render_layers.py -q`
      → 130 passed。bench：plain paint 0.704ms；roles 1.744ms；signals 1.037ms；
      full 3.710ms。相对 `adc4cf4` 存在少量 Layer 调度成本，后续 b.1.2 做批处理优化。
    - **b.1.2 ✅**（`de9dd5c`）优化 Layer 调度：`LayerCompositor` 增加
      `paint_ordered`，横排文本 run 使用已排序路径；after 扫光 band 每帧只算一次；同时移除
      `tests/test_subtitle_render_layers.py` 中不必要的 `qapp` fixture，避免与 painter 模块连跑时
      QApplication 生命周期冲突。回归：layers+painter+renderer 连跑 → 130 passed。bench：plain
      0.725ms；roles 1.733ms；signals 1.105ms；full 3.895ms。
  - **b.2 🔄** glow/ruby 迁移。
    - **b.2.1 ✅**（`16f8aec`）横排 glyph run 的 after glow 迁移为
      `_GlyphRunAfterGlowLayer`：完整 glow 图静态 bake，逐帧只按扫光 band clip blit；非 glow /
      无需 after glow 的 run 不创建 layer。回归：`pytest -k glow` → 3 passed；
      layers+painter+renderer 连跑 → 130 passed。bench 稳定样本：plain 0.729ms；roles
      1.667ms；glow 0.728ms；full 3.807ms。
    - **b.2.2 ✅** ruby 迁移：横排普通 ruby 已完成；竖排 ruby 与 utopia ruby group 后续单独评估。
      - **b.2.2.1 ✅**（`bc71481`）横排 ruby layout：新增 `_RubyLayout` /
        `_layout_rubies`，把目标字符范围、基线、`target_width` / `reading_width`
        从 paint 循环抽到不依赖 `t_ms` 的 layout 段；绘制路径暂不变。回归：
        `pytest -k ruby` → 25 passed；layers+painter+renderer 连跑 → 131 passed。
        bench：plain 0.627ms；roles 1.705ms；ruby 0.961ms；full 3.758ms。
      - **b.2.2.2 ✅**（`b3bc3c1`）横排 ruby layer：新增 `_RubyTextLayer`，
        无入退场 transition 的横排 ruby before/after 主体进入 `LayerCompositor`；bake key
        不含 `t_ms`，逐帧仅按 ruby 进度 clip blit。utopia / transition ruby 仍走原动态路径，
        竖排 ruby 继续单独评估。回归：`pytest -k ruby` → 26 passed；
        layers+painter+renderer 连跑 → 132 passed。bench：plain 0.687ms；roles
        1.636ms；ruby 0.534ms；full 3.109ms。
  - **b.3 🔄** Sayatoo 信号 / 标题 overlay 迁移。
    - **b.3.1 ✅**（`f3d025b`）标题 overlay 迁移：新增
      `_TitleOverlayLayout` / `_TitleOverlayLayer`，标题静态文字块进入 `LayerCompositor`；
      layout 负责文本块、行宽与屏幕锚点，bake key 不含 opacity，淡入淡出只走
      `LayerAnimation.opacity`。回归：`pytest -k title` → 110 passed；
      layers+painter+renderer 连跑 → 132 passed。bench：plain 0.658ms；roles
      1.576ms；ruby 0.441ms；full paint 3.136ms。
    - **b.3.2 ✅**（`0e776b7`）Sayatoo 信号 layer：新增 `_SignalLitsLayer`，
      `volume` / `circle` / `square` 都进入 `LayerCompositor` 的动态 layer 分支；绘制仍复用原
      `_draw_volume_lit_group` / `_draw_lit_shape`，保持 opacity / extinguish / flash 行为逐像素兼容。
      同时实现信号 `vertical_bounds`，可覆盖 signal-only 时间窗。回归：
      `python -m pytest tests/test_subtitle_render_layers.py tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
      → 135 passed。bench 抽查（1080p / 80 frames）：signals paint 0.913ms；full paint 2.914ms。
      后续若还要压榨信号性能，可再把 shape/volume 的静态底图拆成 cached baked layer；当前收益主要是架构统一与 bounds 覆盖。
  - **b.4 ✅**（`0e776b7`）用 Layer 的 `vertical_bounds` 接入条带/脏区聚合：新增
    `frame_vertical_bounds()` 作为共享查询入口，横排文本 / 横排 ruby / 标题 overlay 的 layer bounds
    已参与聚合；bounds 会计入描边、阴影、glow 外扩与整体 viewport 平移/缩放。导出条带预扫优先走
    layer bounds，遇到未迁移路径（竖排、viewport 旋转、逐字 transition / utopia）
    自动回退到原 alpha 扫描，保证不漏像素；`PreviewGraphicsView` 的字幕层时间更新改为旧/新 dirty rect
    合并刷新。新增回归断言普通横排 / 信号条带不再触发整帧 scratch `paint_frame`。回归：
    `python -m pytest tests/test_subtitle_render_layers.py tests/test_subtitle_render_painter.py tests/test_subtitle_render_renderer.py -q`
    → 135 passed。bench 抽查（1080p / 80 frames）：plain paint 0.677ms；full paint 2.914ms。
  （穿插项 P0.4 已完成；后续若要进一步优化预览，可细化 raster fallback dirty rect 或给 FPS 读数加历史曲线。）
- **P1.b' 分步计划**：
  - **b'.0 ✅**（本轮完成）scope 地基：新增 `LayerScopeBox` 与
    `LayerCompositor.scope_boxes()`，把 `glyph/line/group/frame` 从字符串标记升级为可查询的作用域盒子；
    现有 `vertical_bounds()` 复用同一套 scope 聚合口径；utopia 按单行 group 查询联合盒。
    纯框架增量，不改视觉输出。
  - **b'.1 ✅** 为 utopia / transition 的单行 group 准备 layout 分组与保守 bounds。
    - **b'.1.1 ✅**（本轮完成）`LayerScopeBox` 增加 `scope_id`，
      `LayerCompositor` 按 `(scope, scope_id)` 聚合，并新增 `scope_rect()` 查询具体 group
      的联合盒；同一 scope 下多个短语 group 不会互相吞并。回归：
      `tests/test_subtitle_render_layers.py` → 9 passed。
    - **b'.1.2 ✅**（本轮完成）为现有 utopia 动态路径新增 bounds-only group layer：
      `scope_id=("utopia", kind, line_start, line_end, indices, ruby.kanji, ruby.reading)`，
      主字/ruby 的变换后矩形进入 `LayerCompositor.scope_boxes()`，`frame_vertical_bounds()`
      遇到 `transition.effect=="utopia"` 时优先走保守 group bounds，不再无条件退回 alpha scan。
      纯 bounds 接线，不改变绘制像素。回归：
      `tests/test_subtitle_render_painter.py` → 114 passed。
    - **b'.1.3 ✅**（本轮完成）跨行语义落定：短语折到两行时按单行拆分，不生成跨行联合
      `scope_id`；视觉过冲只由当前行 group 的保守 bounds 覆盖。新增回归锁定：
      两行中相同短语/ruby 的 utopia `scope_id` 必须因 `line_start` / `line_end` 不同而分开。
      回归：`tests/test_subtitle_render_painter.py` → 115 passed。
  - **b'.2 ⬜** 接入 P1.c 的 bake+transform / 超采样动态特效路径。
- **✅ A2 方案 B（多条分离带）完成**：内容劈成相隔很远的两块（典型：顶部标题 + 底部歌词）时，单条并集
  会退化成近全高、不省。B = 用 `frame_content_intervals`（新增：返回逐源 lyric/title 的**未合并**纵向区间）
  跨帧汇总 → `_merge_intervals` 按 `_BAND_MERGE_GAP_PX=64` 合并成不相交带 → 各带竖向**打包进同一条 rawvideo
  pipe**（避免多输入/多 pipe），ffmpeg 侧 `split→crop→overlay` 还原到各自原始 y。`_compute_content_bands`
  少于 2 条或打包高度 ≥ `_STRIP_MIN_GAIN_RATIO×全高` 时返回 None 退回方案 A；任一帧落未迁移路径（竖排/
  viewport 旋转/逐字 transition）也退回。env `KROK_SUBTITLE_RENDER_BANDS=0` 可关。单/多进程两路径均支持。
  回归：`tests/test_subtitle_render_renderer.py` 新增 6 例（命令组装 / bands 计算 / **打包逐像素 == 整帧对应区域**
  / **多进程字节一致**）→ 24 passed；trio 148 passed。
- **✅ P1.c-1 ユートピア bake 化完成（opt-in，默认关）**：产品方选「做成开关、默认关」。实现：
  `_build_glyph_run_layer` 加 `supersample`（`p.scale(S,S)` 复用现有绘制逻辑，S=2）；新增
  `_utopia_baked_run`/`_utopia_baked_glow`（LRU 缓存）+ `_paint_utopia_group_baked`（复刻
  `_paint_char_karaoke_stack` 的 未唱→已唱发光→已唱描边/填充 分层与扫光带，但用烘焙位图+`QTransform` 贴出）；
  role/plain 两条 transition 路径在 utopia 分支按 `_utopia_bake_enabled()` 切换。**默认关→逐帧光栅化、像素
  不变**；`KROK_SUBTITLE_UTOPIA_BAKE=1` 开启→**实测 4.09→1.09ms（3.75×）**，像素差集中在边缘 AA
  （均值 0.27/255）。回归：painter 新增 4 例（几何接近≤5px / 飞出保持空 / 缓存填充与清理 / 默认关不变）；
  trio 152 passed。**实测确认**：utopia 瓶颈是光栅化非 `addText`（轮廓缓存无收益，已回退）。
- **✅ 竖排（縦書き）整条路径迁入 LayerCompositor（主文本 + ruby 一并）**：产品方定「不单独迁移、和竖排
  主文本一起做」。新增通用 `_BakedPathStackLayer`（把一次 `_paint_text_layer_stack` 烘焙成位图缓存，逐帧
  只 blit + 纵向扫光带 clip；主文本与 ruby 共用，因二者几何都是 QPainterPath + clip）+ `_build_baked_path_stack`
  （整数对齐 → blit 不重采样）。`_paint_line_vertical` 默认走 `_paint_line_vertical_layers`，旧逐帧直绘
  保留为 `_paint_line_vertical_direct`（`KROK_SUBTITLE_VERTICAL_LAYER=0` 回退 + A/B oracle）。
  **实测 7.4×（4.70→0.64ms）**——竖排原来每帧 before+after 都重光栅化，bake 后两者都缓存，收益比横排更大。
  与直绘**几何完全一致**，仅 ≤1/255 premultiplied 取整差异（与横排迁移同性质）。回归：painter 新增 6 例
  （5 帧 A/B 容差≤1 + 缓存填充清理）；trio 158 passed。utopia ruby 走 §5.2.6 的 opt-in transition bake，
  此处不重复。
- **已知遗留（与本优化无关）· 已缓解**：`tests/test_subtitle_render_transport.py` 在 Python 3.14 退出期
  段错误（PyQt6 multimedia C++ 析构与 Python GC 顺序竞争）。根因：各测试懒创建的 `QMediaPlayer`/`QAudioOutput`
  泄漏到解释器退出才被销毁。修法：模块级 `qapp` fixture teardown 里 `_release_media_objects` 趁 app 存活时
  显式 `stop()` + 解绑 source/audio/video output + `deleteLater()` + `processEvents()`，确定性销毁残留媒体对象。
  本机为 Python 3.13.11（该版本本就不崩、加修复后仍 23 passed）；**3.14 上的修复效果待在 3.14 环境复验**。
- **走字对齐墨水宽度（correctness，对齐 SUG）**：原本卡拉ok扫光按 advance 框（含字形两侧 side
  bearing + 字间空隙）推进，导致锋面与字形墨水错位（字头偏慢——锋面停在左侧空白上墨水迟迟不染；
  字尾悬空——墨水早染满而锋面还在右侧空白里推进）。SUG `karaoke_preview.py` 早已用 `tightBoundingRect`
  的墨水边界走字。现把本模块**静态走字路径**对齐：新增 `_char_ink_x_ranges`（用 `QPainterPath.addText`
  的矢量包围盒取墨水边界，与 `fillPath` 绘制同源、DPR 无关）+ `_role_char_ink_ranges_by_index`（分色逐
  glyph 各自字体），`_karaoke_fill_segments` 的分段左右缘改用墨水边界（advance 仍仅用于排版定位）；普通
  行与分色行两条 layout 均接线。墨水计算只在 **layout 阶段**（按行缓存，非逐帧），无逐帧开销。回归：
  painter+renderer+layers → 160 passed，新增 `test_static_wipe_segments_use_ink_bounds_not_advance`
  （断言 fill_segment == 字形矢量墨水包围盒且严格窄于 advance 框，回退到 advance 即失败）。
- **走字对齐墨水：扩到 transition/utopia 路径**：`_LineLayout` 增 `ink_x_ranges` 字段，普通/分色两条
  layout 都填充；`_paint_line_with_character_transition` 与 `_paint_role_line_with_character_transition`
  新增 `ink_x_ranges` 形参，`_character_fill_ratio` 的 ruby 分支改用墨水边界映射逐字 ratio（绘制定位仍用
  advance 的 `char_x_ranges`）。要点：utopia 的逐字 clip 本就用变换后字形**路径**（=墨水）bbox，唯一残留的
  advance 依赖就是 `_character_fill_ratio` 的 ruby 进度→本字 ratio 映射，故只需改这一处；非 ruby 字符的
  ratio 是纯时间比例（位置无关）本就已墨水对齐。回归：painter+renderer+layers → 161 passed，新增
  `test_character_fill_ratio_honors_ink_ranges`（同一 ruby 进度下墨水与 advance 给出不同 ratio）。
  **现状范围**：静态走字 + transition（含 utopia/char_fade/spin_flip）逐字走字均已按墨水推进；竖排
  （縦書き）走字另走 `_vertical_fill_band`，未纳入本次（如需再单独对齐）。

- **2026-06-24 · 移除预览分辨率档位控件**（`2d42cb3`，用户要求）：删除 `TransportBar` 的
  `_preview_scale_combo`（FPS 右侧 1/1·1/2·1/4 下拉）+ `previewScaleChanged` 信号 + `preview_scale`
  属性 + 全链路 `set_preview_scale`（PreviewPanel / PreviewGraphicsView / SubtitleGraphicsItem /
  PreviewCanvas 的降分辨率绘制分支）+ `_normalize_preview_scale`。**预览恒按完整输出分辨率光栅化**。
  动机：该档位是「降分辨率换流畅」的权宜手段；正确的提速路线是 §9 的特效缓存方案（量子化 bake +
  复用 glow 缓存），不靠牺牲预览清晰度。回归：transport+painter 149 passed。
- **2026-06-24 · §9 A 档完成（A0–A4）**（`c95de1c` A0 / `fa9c3bb` A1 / `d645efa` A2 / `df9fc23` A3 / 本条 A4）：
  逐字入退场特效（char_fade / spin_flip / utopia）的动画/transition 路径并入 `LayerCompositor` 烘焙缓存，
  并让 utopia 复用上正 glow 烘焙缓存（`_RUN_GLOW_CACHE`）替代每帧重算高斯。详见 §9.7 A0–A3 勾选项。
  **A4 实测 A/B**（`profile_preview.py --utopia --glow --start 92000 --warmup 5 --measure 15`，A stain 工程
  1080p60、双行+ruby、本机 16 核+NVIDIA；`KROK_SUBTITLE_GLOW_CACHE=0` 旧逐帧 glow vs 默认 A3 缓存）。
  **⚠️ 单帧光栅化耗时与预览窗口的设备像素数强相关**（utopia body 的 strokePath/fillPath 与各层 blit 在
  viewport 的 device 分辨率上栅格化）→ profiler 默认 `960×600` 窗（场景 1920×1080 被缩到 ~0.5×）数据**偏乐观**；
  真实 KS 预览窗更大（且随显示器 DPR 放大），实际帧率约比小窗低 ~20fps（已用 `KROK_PROF_WINDOW=max` 复现）：

  | 单帧光栅化 median | 旧逐帧 glow | A3 缓存 | A3 改善 |
  |---|---|---|---|
  | profiler 小窗（device ~960×540） | 17.91ms（上限 ≈53fps） | 10.81ms（上限 ≈85fps） | −40% |
  | **全屏窗（device 2400×1352，DPR 1.25，≈真实预览）** | **19.95ms（上限 ≈44fps）** | **14.32ms（上限 ≈67fps）** | **−28% / 上限 +52%** |

  **⚠️ 上表的「上限」是「纯光栅化理论上限」，不是预览实际帧率**——之前一度把它当成实测帧率上报是误导。
  **预览实际帧率另有更硬的天花板：字幕 paint 在主线程上与视频呈现循环串行**，故
  `实测预览fps ≈ 1000 / (≈16ms 视频呈现基线 + 单帧字幕 paint ms)`。诊断证据（`--no-subtitle` 全屏窗）：
  **不画字幕时时钟 tick 62.1/s、视频 60.0fps**（循环本身能跑满 60）；一旦叠加 utopia+glow 字幕，
  tick 掉到 ~35/s、实际字幕 paint ~30/s。即字幕 paint 成本**直接加进每帧循环周期**。

  | full 窗（≈真实预览） | 旧逐帧 glow | A3 缓存 |
  |---|---|---|
  | 单帧字幕光栅化 median | 19.95ms | 14.32ms（−28%）|
  | 模型预测预览fps `1000/(16+paint)` | ≈28fps | ≈33fps |
  | 实测字幕 paint 速率（小窗对照） | 28.5/s | 32.2/s |

  **结论（修正）**：① A3 真实收益 = 单帧字幕 paint −28%，对应预览实际帧率约 **28→33fps**（**不是**之前误报的
  60/85fps；achieved 一直在 ~30，和真机 transport「FPS 36」一致——之前的「不一致」纯是把理论上限当成了实测）。
  ② **要把预览顶到 ~60fps，A3 远不够**：得 B 档（body 量子化缓存）把 paint 压到 ~6–8ms（→ ~40–45fps），
  并很可能需要**把字幕 paint 从视频呈现主循环上解耦**（异步/独立层合成）才可能接近 60。这是后续要立的专项，
  不在 §9 A/B 缓存范围。③ 残余 ~14ms 主体是 utopia body 逐帧矢量（B 档目标）+ 缓存 glow 的 blit。
  profiler 加 `KROK_PROF_WINDOW`（默认 `960x600`，`max`=全屏；探针打印 viewport/DPR/device）+ `--no-subtitle`
  可量循环天花板。新增 env 开关 `KROK_SUBTITLE_GLOW_CACHE`（默认开，=0 回退）。回归：subtitle_render 317 passed。

- **2026-06-24 · 预览字幕栅格化解耦到工作线程（默认开，env 可回退）**（`1016c87`；真机验证稳定后改为默认启用）：承接 A4 诊断
  「预览实际帧率受『字幕 paint 在 GUI 主线程与视频呈现串行』所限」。新增
  `frontend/preview_async.py::AsyncSubtitleRenderer`：worker 线程渲染 `QImage`，GUI 线程
  `SubtitleGraphicsItem.paint` 只 blit 最新帧（latest-wins 合并请求）。env
  默认开启；`KROK_SUBTITLE_ASYNC_PREVIEW=0` 可回退同步预览。`LayerCache` 加锁（worker 渲染 vs GUI `clear` 防竞态，
  双检锁、bake 在锁外）。**实测（A stain 全屏窗、utopia+glow、A3 on，A/B）**：

  | 全屏窗 | 同步 | 异步 |
  |---|---|---|
  | 字幕显示 blit/s | 28.0/s | **36.1/s（+29%）** |
  | 时钟 tick | 32.8/s | 41.6/s |
  | 视频呈现 | 56.7fps | **60.0fps（满帧）** |
  | 进程 CPU mean | 较高 | **~9%** |

  视频呈现回到满帧 60、字幕显示 28→36、CPU 大降、off-thread 渲染稳定不崩。**但字幕仍 ~36 非 60**：
  当时推断新瓶颈是 GUI 线程把 1920×1080 worker 位图 **smooth-scale 到 viewport device（2400×1352）** 的 blit
  开销（~7ms）→ tick 卡在 ~42Hz。据此接入 DPR-aware 渲染（`af1ad4e`）：worker 仍按输出坐标布局，
  但渲染到匹配 QGraphicsView scene→viewport 设备倍率的 `QImage`，GUI 侧等倍 blit，
  避免 1920×1080→viewport device 的额外缩放。回归：subtitle_render 322 passed。
  **2026-06-24 修正**：async worker 从普通 Python `threading.Thread` 改为 `QThread`，
  避免 glow 模糊路径里的 Qt 图形对象在无 Qt event dispatcher 的线程里启动 `QBasicTimer`，播放时刷
  `current thread's event dispatcher has already been destroyed`。

- **2026-06-24 · DPR-aware 真机复测：坐实「不是 FPS 杠杆，是画质杠杆」**（新增 env
  `KROK_SUBTITLE_PREVIEW_DPR_AWARE`，默认开，=0 回退旧 smooth-scale 做 A/B）。
  在 `KROK_PROF_WINDOW=max`（≈真实预览窗，device 2400×1352 / DPR 1.25）、utopia+glow、
  `--start 92000 --warmup 5 --measure 15`，各跑 2 次：

  | 指标 | DPR-aware ON（默认） | DPR-aware OFF（旧 smooth-scale） |
  |---|---|---|
  | 视频呈现 | 60.1 / 59.5 fps | 60.0 fps |
  | 字幕 blit/s | 38.9 / 37.5（~38） | **43.5 / 43.9（~44）** |
  | 时钟 tick/s | 38.5 / 37.7（~38） | 43.0 / 43.1（~43） |
  | 进程 CPU mean | ~28% | ~37% |

  **结论（修正上一条的推断）**：① **「GUI smooth-scale 是字幕刷新瓶颈」不成立**——把它消掉后字幕刷新不升反降
  （~44→~38，**−13%**）。真正的限速是 **worker 单线程在 device 分辨率上的矢量/glow 栅格化吞吐**：
  DPR-aware 让 worker 在 2400×1352（≈1.56× 于 1920×1080）上栅格化，像素更多→产帧更慢→ blit/tick 反而降。
  旧路径 worker 只栅 1920×1080、GUI 那次 smooth-scale 是 GPU 加速的，**并不是瓶颈**。
  ② **视频呈现两路都 60fps**——满帧目标早已由异步解耦达成，与 DPR-aware 无关。
  ③ 故 DPR-aware 的**真实价值是画质**（按 device 分辨率原生栅格化、无上采样软化，呼应 §5.4.3 一致性），
  代价是 ~13% 字幕刷新率；**不是 FPS 收益**（之前「真实 FPS 收益待复测」的预期未兑现）。
  ④ 字幕刷新 ~38（ON）/~44（OFF）均落在「预览 24–60fps」目标带内、视频恒 60、走字时钟 tick 同样 > 24，
  两者都达标。**默认保持 ON**（画质优先，且原生分辨率更贴近导出的 §4「预览=导出一致」）；
  对「重特效下要更高字幕刷新率」的场景，`KROK_SUBTITLE_PREVIEW_DPR_AWARE=0` 可换取 ~44/s（牺牲清晰度）。
  ⑤ **要把字幕刷新本身顶向 60**，得降 worker 单帧成本（B 档已搁置，见 §9.7 B2）或把 worker 多线程/分块——
  属后续专项，不在本次范围。

**强烈建议的执行顺序**：P0.0 → P0.1 →（量到收益后）P0.2/P0.3/P0.4 →
**P1.a 解耦**（其余 P1 的地基）→ P1.b → P1.b' → P1.c →（仅当导出仍嫌慢时）P2。
D 可在任何时候作为独立旁路并行推进。

> ★ 关于 GPU：因约束 4「预览可不用 GPU + 一致性优先」，**默认不上 GPU**。优先把 CPU 路径（layout/bake 缓存 +
> 多进程）做到位——libass 无 GPU 也很快即是佐证。只有当 P0+P1 后导出仍是瓶颈，才把 GPU 当作**导出端可选后端**
> （三段式下只换 paint 实现，layout/anim 不变，观感不漂移）。

---

## 7. 风险控制与验收

- **A/B 与回退**：沿用现有 `KROK_SUBTITLE_PREVIEW=raster` 的范式，新路径都加 env/配置开关与旧路径并存。
- **像素回归测试**：对一组覆盖全特效（分色+信号+glow+ruby+竖排+双行+动画）的参考帧，做
  渲染结果 diff（允许极小阈值）。**这是 P1 重构不翻车的生命线**。
- **性能门禁**：把 §2 基准纳入 CI（或本地 `bench`），每个优化前后出对比数,防止"改完更慢"。
- **多进程坑**：字体加载一致性（worker 各自 `QGuiApplication`）、取消/进度回传、异常传播、有序写盘。
- **GPU 坑（若到 P2）**：跨平台驱动差异、headless 上下文、与 ffmpeg 的零拷贝/回读策略。

---

## 8. 待澄清 / 留给接手者确认

### 已确认（2026-06-23，已并入约束与各章节）

- ✅ **特效名订正**："コドビーア" 是笔误，实为 **ユートピア(Utopia)**，**已是现有特效**，全文已订正。
- ✅ **ユートピア = 字体动态跳动（仿射变换 + 缩放过冲），视觉上可能跨行但按单行拆分** → 处理见 §5.2（bake+变换 / 超采样 / group scope）。
- ✅ **性能目标**：导出"性能允许内尽量快"，无硬倍率；预览 24–60fps + 实时帧率显示；不要求 4K。（注：原"预览输出分辨率可调"档位控件已于 2026-06-24 移除，见下。）
- ✅ **一致性**：允许差异但尽量小；**预览可不用 GPU** → 单一共享 CPU 渲染器优先，GPU 仅作导出端可选加速（差异见 §5.4）。
- ✅ **GPU vs CPU 差异**：已写入 §5.4（性能面 + 观感/正确性面 + 一致性结论）。

### 预留（产品方已说"先预留"，不阻塞 P0/P1.a/b，到对应阶段再定）

- 🅿️ **句内混排基线对齐策略**（§4.5）：底/居中/视觉中线、是否可配置 —— **预留**，P1.c 前再定。
- ⛔ **预览分辨率档位**（§ 约束 3）：~~采用 `1/1` / `1/2` / `1/4` 三档~~ —— **该控件及功能已于 2026-06-24 移除**（commit `2d42cb3`，用户要求），预览**恒按完整输出分辨率**渲染。重特效下若需提速，**走 §9 缓存方案**（量子化 bake+复用 glow 缓存），而非降分辨率。`KROK_SUBTITLE_PREVIEW=raster` 回退仍在。

### 仍待确认（ユートピア 细节，影响 P1.c 实现，不影响 P1.a 地基）

1. ユートピア **缩放过冲倍率**是否沿用现有常量（入场 1.3×、扫光 1.15×）？决定超采样 bake 目标尺寸。（§5.3）

---

## 9. 专题 · 复杂特效字幕的缓存方案（2026-06-23 联网调研）

> 触发：用户实测 **utopia + glow** 预览掉帧（详见 §9.1）。追问暴露两个本质问题——
> ① 现有 utopia 缓存是「烘焙位图 + 每帧 `QTransform`」，缩放有 AA 软化（§5.2.6），说明做法不够成熟；
> ② 动画/transition 路径绕过了 `LayerCompositor` 的统一缓存，utopia 还自带平行缓存
> `_UTOPIA_BAKE_CACHE`，**每加一个特效都要单独搞一遍缓存 → 延展性差**。
> 本节专门调研成熟字幕/文字渲染工具在「动画特效」下如何兼顾**缓存命中**与**画质（不软化）**，并给出本项目的综合方案。

### 9.1 问题复述（结合实测）

- **实测复现**（`scripts/profile_preview.py`，A stain 工程、1080p60、双行+ruby、本机 16 核+NVIDIA）：
  | 配置 | 单帧字幕光栅化 median | 字幕有效帧率 | 视频呈现 | 根因 |
  |---|---|---|---|---|
  | 仅 glow（静态路径） | 3.3ms | ~60 | 60.1fps | glow 被 `_GlyphRunAfterGlowLayer` 烘焙缓存 → 便宜 |
  | 仅 utopia | 8.0ms | ~44 | 60.1fps（卡顿13%） | transition 逐帧重建矢量，无静态烘焙 |
  | **utopia + glow** | **18.6ms** | **~31** | **57.4fps（卡顿28%，最长443ms）** | utopia 每帧重建矢量 **+ glow 高斯每帧重算**（绕过缓存）|
  | utopia+glow + `UTOPIA_BAKE=1` | 9.8ms | ~39 | 仍卡顿24% | 现有 bake 只 1.8×，glow 仍未走缓存、ruby 仍 vector |
- **瓶颈定性**：单帧 18.6ms > 60fps 预算 16.67ms，且字幕层在**主线程单核**串行光栅化（CPU 归一化才 ~1%、GPU ~24%，**资源大量闲置**）→ 主线程被占住又饿死视频呈现、触发 20 次漂移 seek。
- **两个缓存形态的短板**：
  - 静态层走 `LayerCompositor`（`layers.py`）烘焙缓存，**glow 只烘焙一次、全局复用**（延展性正确）；
  - 但**动画/transition 路径**（`_paint_line_with_character_transition` 等 → `_paint_char_karaoke_stack` → 每帧 `_paint_glow_path`）**完全绕过它**；char_fade/spin_flip 只在入退场短窗口走这条、影响小，**utopia 全程走 → glow 每帧重算 = 18ms 的主因**。
  - utopia 的 bake 是「烘焙位图 + 每帧 `QTransform`」→ 缩放 AA 软化（§5.2.6），且**另起平行缓存** `_UTOPIA_BAKE_CACHE` —— 正是「每个特效各搞一套」的低延展性反面教材。

### 9.2 业界调研

#### A) libass —— 成熟 CPU 方案的标杆（最直接可借鉴）

- **四级缓存** `Font → Outline → Bitmap → Composite`，高层引用低层，全部 LRU + 引用计数。Outline（矢量轮廓）与变换无关、可长期复用。
- **关键：PR #285「缓存键量子化」**——回答了「动画下怎么缓存又不软化」：
  - **不**对烘焙位图做仿射变换（那会软化），而是**把所有变换集中到光栅化前一处**（缩放与其余变换一起做），**始终从矢量轮廓重新光栅化 → 锐利、零软化**；
  - 对**变换 / 模糊 / 阴影**做**量子化（离散步进）**当作 Bitmap 缓存键 → 慢/连续动画里相邻帧落同一量子桶 = **缓存命中**，只有跨桶才重栅一次；误差有界、肉眼不可见（`TRANSFORM_ACCURACY` / `BLUR_ACCURACY`，步长由「可接受像素误差」的微分方程定）；
  - **主体位图**与**描边/阴影/模糊位图**分开缓存、各自量子化；
  - **跨字形对齐量子化误差**，让缓存失效同时发生 → 提升 Composite（整行合成）缓存命中。
  - 「重特征 + 慢动画」可快**数倍**。
- **核心启示**：mature answer = **「缓存与变换无关的轮廓 + 按『量子化变换』缓存重栅位图」**，而**不是**「缓存位图 + 每帧变换」。前者锐利、后者软化。**我们 utopia 当前做法恰好是被业界淘汰的后者。**
- **blur 是最贵的算子**：libass 用 SIMD 高度优化高斯；多条重叠 blur 仍可能串行/退化（Win/OSX 上有已知瓶颈与显存膨胀报告）。→ 印证 glow/blur 必须缓存、且按量子化半径独立缓存。

#### B) SDF / MSDF —— GPU 的无损答案（长线轨）

- 距离场**烘焙一次**，shader 里**任意缩放/旋转无损**（不重栅、不软化），`screenPxRange()` 按屏幕尺度控 AA。
- **描边 / 阴影 / 外发光全是同一张距离场上的 shader 阈值运算**（填充 = d>0；描边 = 0 附近的带；外发光 = 对更宽距离做 `smoothstep`；阴影 = 偏移采样）→ 我们的 **stroke + glow 近乎免费**，且天然无软化。
- 限制：极小字号变差、尖角略圆、图集显存、**需 GPU 重写**。与本项目约束（GPU 仅作导出可选后端、预览保持 CPU 一致性，§5.4）相符——**作为长线无损轨，不挤占 CPU 主线**。

#### C) Skia / 浏览器合成器 —— 量子化思想的普遍性

- GPU 字形图集 + **子像素量子化**（每字形预存几个离散子像素偏移版本，取最近的）；放大跨尺度时按阈值重栅。
- 「合成层变换缓存纹理」是廉价路径但放大会糊（与我们 bake+transform 同病）；要锐利就**按尺度阈值重栅**——和 libass 的量子化重栅同一思想。
- **线性空间混合**对 AA 正确性重要（呼应 §5.4.3 的 premultiplied 取整小色差）。

### 9.3 对本项目的综合建议（CPU 主线 + 统一可延展 + 无软化）

**核心思路：把缓存的「静态 / 动态」二分，升级为「按量子化动画状态缓存」，统一覆盖所有特效。**

- **缓存键** = `(run/layer 身份签名, 量子化变换, 量子化 blur 半径, 量子化 stroke/shadow)`。
  `bake(key)` 在**该量子化变换下从矢量重栅**（锐利）；`animate()` 只补「**子量子残差变换 + opacity + clip**」（残差极小，软化不可见）。
- **一举解决两件事**：
  1. **画质**：不再「烘焙位图 + 整帧 QTransform」→ 改为「量子化重栅」→ **消除缩放软化**（修正 §5.2.6 的取舍口径——软化不是必然代价，是当时次优实现）；
  2. **延展性**：**一套缓存服务所有特效**（含未来新特效）。新特效只产出「逐帧动画状态」，由框架量子化成键、命中或重栅——**不再写专用缓存**，退役 `_UTOPIA_BAKE_CACHE`。
- **glow 作为独立量子化缓存条目，静态与动画路径共用** → 直接干掉实测「utopia 下每帧重算高斯」的 18ms 主因。
- **映射到现有 `LayerCompositor`**（改动可控，地基已在）：
  - 把 `static_key` 从「与 t 无关」放宽为「**量子化(t 的动画状态)**」；`LayerCache` 已是 LRU、`_paint_baked_layer` 已支持 `transform/clip/opacity`，只需让 `bake` 接受量子化变换、`animate` 产出残差；
  - 把 char_fade / spin_flip / **utopia** 都并入这套 `bake + animate` 契约（它们本就该是 Layer）；
  - 量子化步长按「可接受像素误差」定（借鉴 libass `*_ACCURACY` 思路），并**跨字形对齐失效**；
  - 后续可加 **Composite 级整行缓存**（before+after+glow 合成位图按量子化状态缓存），进一步省合成。
- **并行/导出红利**：量子化让多帧共享烘焙，天然利于多进程导出与预览缓存复用。
- **长线可选**：MSDF GPU 作为**导出端无损后端**（stroke/glow → shader 阈值），预览仍用 CPU 量子化方案以保持一致性（§5.4.4）。

### 9.4 落地步骤（增量、每步像素回归）

1. **抽出统一「量子化变换 + 缓存键」工具**；先用于 utopia transition：把「bake + QTransform」改成「**按量子化变换重栅 + 缓存**」，像素对比应**比现 bake 更锐利、比逐帧 vector 快**。
2. **让 glow 缓存被 transition 路径复用**（独立量子化条目）——直接消除实测每帧高斯。
3. 把 **char_fade / spin_flip / utopia 全并入 `LayerCompositor` 的「量子化 bake+animate」契约**，退役 `_UTOPIA_BAKE_CACHE`，验证「新特效 = 只写 `animate()`，不碰缓存」。
4. 选配：Composite 级整行缓存；导出多进程共享量子化缓存。
5. 长线：MSDF GPU 导出后端单独立项调研（不阻塞上面 1–4）。

### 9.5 与既有约束/结论的关系

- 满足约束 1（不砍特效）、约束 4（预览=导出一致、CPU 主线）、以及用户强调的**延展性**（统一缓存契约，新特效零缓存代码）。
- **修正 §5.2.6**：utopia bake 的「AA 软化」**不是必然代价**——换成 libass 式「量子化重栅」即可锐利；当前 bake+transform 只是过渡实现。
- 不与 §4.6（layout/anim/paint 三段式）冲突，反而是其 paint/缓存段的正确收口。

### 9.6 原语契约（primitive-based caching contract）——延展性的根本保证

> 目的：以后**加任何特效都不写缓存代码、不进任何 dispatch 矩阵**，只描述「这帧怎么动」。
> 「按特效分类的矩阵」（§9 表格）只是**诊断快照**，证明现有特效都落进固定原语集；不是要维护的清单。

**固定的一小撮合成原语**（框架只实现一次，特效作者不碰）：

| 原语 | 缓存待遇 | 无损? | 现有用例 |
|---|---|---|---|
| `opacity` | 合成时套用，永远命中**同一张**烘焙位图 | 是 | 淡入 / 逐文字渐显 / 退场 |
| `translate(dx,dy)` | 合成时平移（整数对齐） | 是 | 滑入 / 上移 |
| `clip`（走字裁剪带） | 合成时裁剪 | 是 | 卡拉ok 扫光 |
| `affine`（缩放/旋转/skew） | 写进**量子化烘焙键**，跨桶才重栅 | 量子化后锐利 | 旋转翻转 / utopia |
| 逐帧变化的**栅格参数**（blur 半径 / 描边宽度在动） | 同样进量子化键 | 量子化后锐利 | 未来特效 |

**契约**：每个特效实现**一个** `animate(ctx, layout) -> AnimationState`，返回上述原语的本帧取值。框架自动：
1. 挑出「影响栅格的子集」（affine + blur + stroke）→ **量子化**成烘焙键 → 命中或从矢量重栅一次（锐利）；
2. 其余（opacity / translate / clip / 子量子残差变换）→ 合成时**免费**套用。

→ 新特效**自动获得**：glow/描边缓存复用、并行、保守 bounds、预览=导出一致；只用前三原语自动落「全缓存无损」档，用到 affine 自动落「量子化重栅锐利」档。
→ **唯一需要动框架**的情况：发明一个**全新栅格原语**（mesh warp / 置换贴图 / 自定义 shader，无法用 affine+blur+stroke+clip+opacity 表达）。那是**一次性**给框架加一个原语类（做一次、全特效受益），不是每个特效重复。

### 9.7 执行清单（接手指南，按依赖排序；勾选即更新）

> 冷启动接手者先读：§9.1（实测瓶颈）→ §9.2（调研）→ §9.3（方案）→ §9.6（原语契约）→ 本清单。
> 复现/验收工具：`scripts/profile_preview.py`（`--utopia --glow --start <ms>`；`KROK_SUBTITLE_UTOPIA_BAKE=1` 可 A/B）。
> 像素回归：`tests/test_subtitle_render_painter.py`（每步新增/保持参考帧；env 开关与旧路径并存做 A/B）。

#### 现状代码地图（动手前必看）

- **保持不动（已正确）**：
  - 静态路径：`_layout_line` / `_layout_plain_line` / `_layout_role_line` → `_paint_line_layers` → `_TEXT_RUN_COMPOSITOR.paint_ordered`。
  - 已迁移的层（`engine/painter.py`）：`_GlyphRunLayer`、`_GlyphRunAfterGlowLayer`（**glow 已烘焙缓存**）、`_RubyTextLayer`、`_TitleOverlayLayer`、`_SignalLitsLayer`、`_BakedPathStackLayer`（竖排）。
  - 图层框架：`engine/layers.py`（`LayerCompositor` / `LayerCache` / `SubtitleLayer` 协议 / `LayerAnimation{top_left,opacity,clip_rect,transform}` / `_paint_baked_layer`）。
  - **整行级动画（淡入/滑入/上移）**：`engine/animator.py::line_animation_state` → 在 `painter.py` `_paint_line`（约 :3139–3169）以 `setOpacity`+`translate` 套在 `_paint_line_static` 外层。**已是「缓存渲染 + 每帧 opacity/translate」理想形态，零改动。**
- **要改（动画/transition 路径）**：
  - ~~char_fade / spin_flip~~ **已于 A1/A2 迁出**：现由 `_char_transition_layer_stack` + `_TEXT_RUN_COMPOSITOR` 处理（见 `_paint_line_static` 的 `effect in ("char_fade","spin_flip")` 分支）；下列函数现仅服务 **utopia（全程）**。
  - `_paint_line_with_character_transition`（约 :4825）、`_paint_role_line_with_character_transition`（约 :4482）、`_paint_char_karaoke_stack`（约 :5421）。
  - 每帧 glow 热点：`_paint_glow_path` 调用点（约 :4388 / :4465 / :5558）。
  - transition 触发：`_line_char_transition_context`（约 :4734）——utopia 全程、char_fade/spin_flip 仅入退场窗口。
  - 逐字 ratio：`_character_fill_ratio`（约 :6010，已墨水对齐）。
- **要退役（平行缓存，低延展性反面教材）**：`_UTOPIA_BAKE_CACHE`（:81）、`_utopia_baked_run` / `_utopia_baked_glow`（约 :5287 / :5310）、`_paint_utopia_group_baked`（约 :5407）、`_utopia_bake_enabled` + env `KROK_SUBTITLE_UTOPIA_BAKE`。

#### A 档 · 动画路径并入 LayerCompositor + 复用 glow 缓存（中改动 / 低风险 / 收益大头）

- [x] **A0 框架扩展**（2026-06-24 完成，行为保持）：把 `SubtitleLayer.static_key` 语义从「与 t 无关，否则 None→逐帧」放宽为「返回 `quantize(动画状态)`」；`bake(key)` 接受量子化变换、`animate()` 产出残差（opacity/translate/clip/子量子 transform）。落地详情——
  - 复核确认 `LayerCompositor`/`LayerCache`/`bake(...,key)`/`_paint_baked_layer` 的现有机制**已支持**量子化键与 opacity/translate/clip/transform 残差，无需改造缓存层；
  - 唯一真实缺口是**残差仿射的枢轴**：给 `LayerAnimation` 加 `transform_origin: QPointF | None`（场景坐标，与 `top_left` 同空间），`_paint_baked_layer` 经新 helper `_pivoted_transform()`（Qt 行向量约定 `T(-o)·M·T(o)`）绕枢轴施加变换。默认 `None` = 现状，**152 个 painter/renderer 像素回归全绿**。这样 A2(skew)/B2(utopia affine) 接入时无需手工把 `translate(c)·M·translate(-c)` 折进矩阵；
  - 正式化 `SubtitleLayer` / `LayerAnimation` 契约 docstring 为「量子化动画状态 + 残差合成原语」（落实 §9.6）；
  - 新增单测 `tests/test_subtitle_render_layers.py`：枢轴不动点、`None` 透传、180° 绕中心翻转的像素验证。
- [x] **A1 char_fade 迁移**（2026-06-24 完成）：逐字仅 opacity → 烘焙一次（直立）+ 每帧 opacity 走 compositor。**无损**。落地——
  - 给 `_GlyphRunLayer` / `_GlyphRunAfterGlowLayer` 加 `fade_opacity: float = 1.0`，乘进 `animate().opacity`（其余字段不变 → 现有静态路径行为零变化）；
  - 新增 `_char_fade_layer_stack`（**A2 已泛化更名为 `_char_transition_layer_stack`**）：逐 glyph 复用静态路径的 before/after/glow 层（按 `glyph.index` 取 `_char_fade_opacity`，opacity≤0 整字跳过，按 glyph 顺序交错与旧逐字栈同口径），扫光带取整行 `fill_segments`；普通行与分色行同路；
  - `_paint_line_static` 在 `transition.effect == "char_fade"` 时改走 `_TEXT_RUN_COMPOSITOR.paint_ordered`，不再进 `_paint_line_with_character_transition` 的逐帧 `_paint_char_karaoke_stack`；**glow 顺带并入烘焙缓存**（char_fade 不再每帧重算高斯，等于提前吃到 A3 对 char_fade 的红利）；
  - char_fade 的 ruby 仍由 `_paint_rubies(..., transition)` 渲染，未改动；
  - 新增单测 `test_char_fade_layer_stack_applies_staggered_per_char_opacity`（逐字 fade_opacity / 跳过逻辑）；既有 char_fade 像素关系测试（vs plain / vs utopia）现已跑在新路径上。**315 个 subtitle_render 测试全绿。**
- [x] **A2 spin_flip 迁移**（2026-06-24 完成）：逐字 opacity + scale(opacity)+skew，窗口短 → 走 compositor。落地——
  - 把 A1 的 `_char_fade_layer_stack` 泛化为 **`_char_transition_layer_stack`**（同时服务 char_fade / spin_flip）；`_paint_line_static` 的 dispatch 改为 `effect in ("char_fade","spin_flip")`；
  - 给 `_GlyphRunLayer` / `_GlyphRunAfterGlowLayer` 加 `transform: QTransform | None`，在 `animate()` 作为残差返回（同一字的 before/after/glow 三层套同一变换）；
  - 新 helper `_spin_flip_char_transform` **复用既有 `_character_transform`**（与旧 `_apply_character_transform` 逐元素一致：scale(opacity)+skew、绕字心枢轴烘焙进矩阵）→ **几何与旧路径完全相同**，仅渲染基底由「逐帧矢量重栅」变为「烘焙位图 + 变换」（bitmap-transform 软化，见 D2）；
  - 注：A0 的 `LayerAnimation.transform_origin` 此处未用（枢轴已折进矩阵更省事），它留给 B 档量子化重栅；
  - 新增单测 `test_char_transition_layer_stack_spin_flip_carries_scale_skew_transform`（残差变换与旧 `_character_transform` 逐元素相等）；既有 spin_flip 状态测试不受影响。**316 个 subtitle_render 测试全绿。**
- [x] **A3 让 transition 路径复用 glow 烘焙缓存**（2026-06-24 完成；char_fade 已在 A1 顺带解决，本步收口 **utopia**）→ **消除实测「utopia 下每帧重算高斯」**。落地——
  - utopia 默认（非 bake）路径每帧在变换后的矢量 path 上 `_paint_glow_path`（高斯）= 实测 18ms 主因（utopia-only 8ms，+glow 到 18.6ms）。
  - 新增**上正 glow 烘焙缓存** `_RUN_GLOW_CACHE`（`LayerCache`）+ `_get_or_build_run_glow` / `_blit_cached_run_glow`；把 `_build_glyph_run_after_glow_layer` 泛化为 `_build_glyph_run_glow_layer(..., after)`（before/after 两态共用，按 glyph 身份缓存）。
  - `_paint_char_karaoke_stack`（A1/A2 后已是 **utopia 专用**）加 `glow_run` / `glow_transform`：给定时三个 ratio 分支的 glow 都改为「贴缓存的上正 glow 位图 + `setTransform(transform)`」、body 仍 `draw_glow=False` 逐帧矢量（**锐利**）；`glow_run=None` 退回旧逐帧 glow（可回退）。
  - 两个 caller（普通行 `_paint_line_with_character_transition` / 分色行 `_paint_role_line_with_character_transition`）传上正 `group_glyphs`/`run` + utopia `transform`。
  - **glow 是软晕、对 bitmap-transform 不敏感** → 复用无明显软化；body 锐利留给逐帧矢量（B 档再缓存 body）。device 空间 clip 在 `setTransform` 前设好，几何与旧 `_paint_glow_path` 同位（差异仅「先模糊后变换」vs「先变换后模糊」，对软晕可忽略）。
  - 新增单测 `test_utopia_glow_uses_cached_run_glow`（缓存被走到 + 同帧重画纯命中不增长）；既有 utopia 关系测试现跑在新路径上。**317 个 subtitle_render 测试全绿。**
- [x] **A4 用 `scripts/profile_preview.py --utopia --glow` 验收**（2026-06-24 完成，结论已修正）：A/B（`KROK_SUBTITLE_GLOW_CACHE=0` vs 默认，全屏窗 ≈真实预览）单帧字幕 paint median **19.95ms → 14.32ms（−28%）**。**关键修正**：预览实际帧率不是「纯光栅化上限」，而受「字幕 paint 在主线程与视频呈现串行」所限，`≈1000/(16+paint)`；`--no-subtitle` 诊断证明不画字幕时循环可跑满 60Hz、叠字幕后掉到 ~35Hz。故 A3 真实收益 = 预览 ~**28→33fps**（与真机 transport「FPS 36」一致；之前误报的 60/85fps 是理论上限）。**要顶到 ~60fps 需 B 档（paint→~6-8ms）+ 把字幕 paint 从呈现主循环解耦**，超出 §9 缓存范围、需另立专项。见 §6.1。

#### B 档 · utopia 量子化重栅（在 A 档之上，根治缩放软化 + 终态延展性）

- [x] **B1 抽「量子化变换/缓存键」工具**（2026-06-24 完成）：新增 `engine/quantize.py`——
  `quantize_linear_key`（量子化仿射线性部分为整数桶；**平移不进键**=免费残差）、
  `quantized_linear_transform`（吸附到量子格点、平移清零，bake 即按它从矢量重栅；idempotent）、
  `residual_transform`（合成残差，`quantized·residual==actual`，Qt 行向量 `quantized⁻¹·actual`）、
  `quantize_blur` / `quantize_stroke`。**D1 默认**：线性步长 = 可接受误差 `0.4px` / 参考字形半径 `96px`
  ≈ `0.0042`（保守、偏锐利、命中略低，常量可调）。纯函数、不碰热路径。单测 7 项（邻近同键 / 跨桶异键 /
  平移无关 / idempotent / 残差还原 / 误差有界 / blur·stroke 取整）全绿。
- [⛔] **B2 utopia 改量子化重栅 —— 经 B1 工具实测验证「不值得做」，搁置**（2026-06-24）：
  用 B1 的 `quantize_linear_key` 跑真实 utopia 变换曲线（`_transition_char_state`+`_character_transform`，
  step=0.0042 即 0.4px 预算）量桶命中——**入场 43 帧→31 桶（~28% 命中）、退场 46 帧→46 桶（0% 命中）**。
  根因：utopia 入场缩放(0→1.3×/400ms)、退场翻转(0→−180°/750ms)、扫光(±0.15/数帧)**都比量子步长快得多**，
  相邻帧普遍跨多个桶 → 子像素精度下几乎零复用（libass 的「量子化可快数倍」前提是**慢**动画，utopia 不满足）。
  要有复用须把步长放粗 ~10×（误差 >4px）→ 明显抖动/软化，不可接受。故量子化重栅对 utopia body 净收益
  ≈ 0（多数帧 miss 时做同样的矢量重栅 + 额外开销）。**body ~8ms 成本的正解是『搬出主线程』（已由异步预览
  解决），而非缓存**。B1 工具保留（未来若有慢动画特效仍可用）。验证脚本思路见本条，不入库。
- [x] **B3 退役 `_UTOPIA_BAKE_CACHE` 及配套**（2026-06-24 完成：删除 opt-in bitmap bake 开关与平行缓存死代码）：原依赖 B2 把 utopia 统一到量子化 bake。**B2 搁置后**，
  opt-in `KROK_SUBTITLE_UTOPIA_BAKE` 的「bitmap 软化」路径已被 A3（默认矢量锐利 + 缓存 glow，更清晰）+
  异步预览全面超越、形同死代码——**可作独立低优先清理**（删 `_utopia_baked_*` / `_paint_utopia_group_baked` /
  `_UTOPIA_BAKE_CACHE` / env），不再绑 B2。
- [ ] **B4 验证延展性（独立于 B2，仍有效）**：新增一个「示例新特效」只实现 `animate()`，确认**零缓存代码**
  即获得缓存/glow/并行/bounds（落实 §9.6 契约）。可作为 A 档框架的收尾验证。

#### 长线 · 可选（不阻塞 A/B）

- [ ] **L1 Composite 级整行缓存**：before+after+glow 合成位图按量子化状态缓存，进一步省合成。
- [ ] **L2 导出多进程共享量子化缓存**：量子化让多帧共享烘焙，配合 A3 多进程导出。
- [ ] **L3 MSDF GPU 导出后端调研立项**：距离场烘焙一次、stroke/glow 为 shader 阈值、缩放旋转无损；仅作**导出端**可选后端，预览保持 CPU 一致（§5.4.4）。单独立项。

#### 待决策（接手前/中与产品方确认）

- [~] **D1 量子化步长 / 可接受像素误差阈值**：B1 暂定 affine 误差预算 `0.4px`@参考半径 `96px`（线性步长 ≈0.0042）、blur/stroke 步长 `1px`（`engine/quantize.py` 常量）。保守偏锐利；B2 落地后据实测命中率 vs 目检画质回调。
- [~] **D2 spin_flip 是否需要无损**：A2 暂按「**bitmap-transform 软化可接受**」实现（窗口短、字符在退/入场同时缩放变小→软化幅度小）。几何与旧路径完全一致，仅基底从矢量改为位图。**若实测觉糊**，纳入 B 档量子化重栅（B1/B2 的 `quantize(transform)` 同样适用 spin_flip 的 scale+skew）即可无损，无需改 A2 结构。待产品方在真实工程上目检确认。
- [ ] **D3 utopia 缩放过冲倍率**（入场 1.3× / 扫光 1.15×，§5.3）是否沿用——决定量子化变换的取值范围。
- [ ] **D4 §5.2.6 口径更新**：B2 完成后，把「utopia bake 必然软化」的旧结论正式改写为「量子化重栅可锐利」。

#### 验收基线（每步必跑）

- [ ] 像素回归 `tests/test_subtitle_render_painter.py`（+ renderer/layers）全绿；新特效/新路径各加参考帧。
- [ ] `scripts/profile_preview.py --utopia --glow --start 92000`：单帧光栅化 < 16.67ms（60fps 预算）、视频呈现卡顿率显著下降、播放中 seek 趋零。
- [ ] A/B env 开关与旧路径并存，确保可回退（沿用 `KROK_SUBTITLE_*` 范式）。

---

## 附录 A：关键代码位置

- 导出循环 / ffmpeg 命令：[`engine/renderer.py`](../krok_helper/subtitle_render/engine/renderer.py)
- 渲染核心 / 全部特效 / 现有缓存：[`engine/painter.py`](../krok_helper/subtitle_render/engine/painter.py)
  - 已唱层逐帧热点：`_paint_line_static` / `_paint_after_path`
  - 未唱层烘焙（可参照模式）：`_get_or_build_before_layer`
  - glow 烘焙（可参照模式）：`_get_or_build_after_glow`
  - Sayatoo 信号：`_paint_signal_lits` / `_signal_lit_groups`
  - 句内字体/字号现状（§4.5）：`_paint_line_static`（单 font/metrics）、`_style_for_line`（按行/歌手覆盖）、
    `_make_font_for`（仅 jp/latin family 切换）、`_before_layer_cache_key`（单 font_sig，需扩为 per-run）
- 预览（默认 GraphicsView）：[`frontend/preview_graphics.py`](../krok_helper/subtitle_render/frontend/preview_graphics.py)
- 预览（raster 回退）/ 播放时钟：[`frontend/preview_view.py`](../krok_helper/subtitle_render/frontend/preview_view.py)
- 现有测试：`tests/test_subtitle_render_painter.py`、`tests/test_subtitle_render_renderer.py`

## 附录 B：参考链接

- libass 架构 / 多级缓存（DeepWiki）：https://deepwiki.com/libass/libass
- What Every Typesetter Should Know about Renderer Internals：https://gist.github.com/arch1t3cht/8cb1793beb72d30e334c7c46ff8c1080
- libass GPU 后端特性请求 #704：https://github.com/libass/libass/issues/704
- Aegisub Inline-fx 文档：https://aegisub.org/docs/latest/karaoke_inline-fx/
- AE 渲染与缓存（cross-render caching）专利说明：https://image-ppubs.uspto.gov/dirsearch-public/print/downloadPdf/7295208
- SDF/MSDF 文字渲染指南：https://www.redblobgames.com/x/2403-distance-field-fonts/ ，
  https://github.com/Blatko1/awesome-msdf
- Skia 文字 API / 字形图集：https://skia.org/docs/dev/design/text_overview/ ，
  skia-python：https://github.com/kyamagu/skia-python
- Qt 图形 / QRhi：https://doc.qt.io/qt-6/topics-graphics.html ，https://doc.qt.io/qt-6/qrhi.html
- moderngl headless 渲染：https://deepwiki.com/moderngl/moderngl/5.2-headless-rendering
- ffmpeg ass/subtitles 滤镜（方案 D）：https://www.baeldung.com/linux/subtitles-ffmpeg
- **§9 特效缓存调研**：
  - libass「缓存键量子化」PR #285（动画下重栅+量子化缓存的关键）：https://github.com/libass/libass/pull/285
  - libass 缓存层级（DeepWiki，Font→Outline→Bitmap→Composite）：https://deepwiki.com/libass/libass
  - libass 高斯模糊 SIMD 优化 issue #9：https://github.com/libass/libass/issues/9
  - MSDF 生成器 msdfgen（缩放/旋转无损、stroke/glow shader 化）：https://github.com/Chlumsky/msdfgen ，awesome-msdf：https://github.com/Blatko1/awesome-msdf
  - 字体渲染综述（bitmap/SDF/vector 取舍，Aras）：https://aras-p.info/blog/2017/02/15/Font-Rendering-is-Getting-Interesting/
  - Skia GPU 字形图集 + 子像素量子化：https://deepwiki.com/google/skia/4-graphite:-next-generation-gpu-backend
</content>
</invoke>
