# 字幕视频渲染（subtitle_render）模块 — 需求与界面设计

> 给接手本模块的 agent（Codex / Claude Code / 其他）的需求规格 + 设计文档。
> 本模块在 `feat/subtitle-render` 长线开发分支上推进，最终一次性 merge 回 `main`。

## 0. 当前进度（骨架已落地）

提交 `d077b3e` 已在 `feat/subtitle-render` 分支上完成最小骨架：

- 目录 `krok_helper/subtitle_render/` 已建，含 `__init__.py` / `__main__.py` / `models.py` / `settings_bridge.py` 占位、`engine/` 与 `frontend/` 子包
- `SubtitleRenderWindow(embedded=False)` + `for_embedding(parent, settings_provider, workflow_context)` 已实现（仅显示"开发中"提示）
- `KrokHelperSubtitleRenderSettingsBridge` 已实现，桥接 `AppSettings.subtitle_render: dict` 命名空间
- `gui_qt.py` 第 5 步原本的 `PlaceholderPage` 已替换为本模块的嵌入实例
- 烟测：`python -m krok_helper.subtitle_render` 能弹空窗；工作台切到第 5 步不报错

**下一步从 P0-A1（加载字幕源 .ass / .sug / .nkm）开始**。建议按 A1 → A2/A3 → A4（核心高亮）→ A7（预览）→ A8/A9（输出与取消）→ A10/A12/A11（双模式接线）的顺序推进。

## 0.1 通用工程约束（接手前必读）

- **用户面向字符串一律中文**（UI 标签 / 弹窗 / CHANGELOG / release body）。代码注释 / docstring 中英文均可。
- **不要改 `krok_helper/lyrics_timing/` 里的 submodule 源码**——要消费 SUG 的数据，从外部读 `Project` 对象或 `.ass` 导出文件即可。
- **目录布局沿用工作台惯例**：业务逻辑独立 `.py` 或子包；UI 入口统一放在子模块 `frontend/main_window.py`；标题栏/菜单栏由 `SubtitleRenderWindow` 自身管理而非塞回 `gui_qt.py`。
- **设置存储**走 `AppSettings.subtitle_render` dict（已新增字段）；模块内部自由读写嵌套 key，宿主负责整体持久化。
- **新增依赖前先看 `requirements.txt`**——已有 PyQt6 / qfluentwidgets / ffmpeg CLI 调用 / numpy / sounddevice / pedalboard / pykakasi。慎引入新包。
- **Sayatoo「基本」页复刻任务**已有专项逆向记录：见 [`docs/Sayatoo基本页逆向与实现计划.md`](Sayatoo基本页逆向与实现计划.md)。本机 Sayatoo 路径为 `C:\Program Files\Sayatoo Software\SubtitleMaker2\2.3.18.9487`，后续遇到不确定的布局/视图语义时优先参考该文档并继续逆向 Sayatoo。

---

## Context

卡拉ok工作台（krok-helper）目前已完成 6 步工作流中的 5 步（视频下载 / 波形对齐 / 歌词检索 / 歌词打轴 SUG / Hi-Res 混流），第 5 步"字幕视频渲染"在 `gui_qt.py` 中原本是 `PlaceholderPage`，骨架阶段已替换为本模块的嵌入实例。本模块是发布 1.0 前最后一个、也最复杂的模块，目标是实现 **NicoKaraMaker3 等效功能**：把带逐字时间戳的歌词，渲染为带卡拉ok高亮动画字幕的视频文件（叠加在 MV 或纯背景上）。

模块必须支持双模式：

- **standalone**：脱离工作台独立启动，用户手动选字幕 / 视频 / 音频
- **embedded**：作为工作流第 5 步，从前序步骤自动获得资源；产物交给第 6 步 Hi-Res 混流

开发策略：在 `feat/subtitle-render` 长线开发，期间 `main` 上的 bugfix 频繁 merge 进开发分支（`git merge main`），最终一次性 merge 回 main。**不要把零散 bugfix 直接提交到开发分支**——bugfix 走 main。

复刻 SUG（lyrics_timing 子模块）的双模式骨架：`MainWindow(embedded: bool)` + `for_embedding()` 工厂方法 + `KrokHelperSubtitleRenderSettingsBridge` 设置桥。

---

## A. 功能需求清单（P0 / P1 / P2）

### P0 — MVP（开发分支首个可演示版本）

| # | 需求 | 输入 | 输出 | 模式 |
|---|------|------|------|------|
| A1 | 加载字幕源 | **Nicokara 逐字 LRC**（`.lrc`，SUG `NicokaraExporter` 产物，含 `@Ruby` / `@Offset` / `@Title` / 演唱者标签） | 内部 `TimingTrack` | 通用 |
| A2 | 加载背景视频 | 文件路径 + 起始 offset_ms | 渲染底层帧源 | 通用 |
| A3 | 加载音轨 | 文件路径 / 来自波形对齐 | 预览同步 + 输出混音 | 通用 |
| A4 | 卡拉ok逐字高亮 | 字符级时间戳 + Style | 帧像素（底色→填充色） | 通用 |
| A5 | 字体配置 | family / size / weight / italic（**MVP 仅横书き**） | 渲染参数 | 通用 |
| A6 | 颜色配置（纯色） | 底色 / 填充色 / 描边色 / 阴影色 | Style 字段 | 通用 |
| A7 | 实时预览（指定时间点） | 当前 ms | QImage 显示 | 通用 |
| A8 | 输出 MP4 | 分辨率 / fps / 码率 / 硬软编 | mp4 文件 | 通用 |
| A9 | 取消渲染 | `should_cancel` 回调 | 中断 ffmpeg 子进程 | 通用 |
| A10 | 嵌入模式上游消费 | WorkflowContext：`subtitle_lrc_path`（SUG `NicokaraExporter` 落盘的 .lrc） + `aligned_video_path`（默认背景） + `alignment_offset_ms` | 写回 `subtitle_video_path` | embedded-only |
| A12 | 嵌入模式默认背景=MV | `aligned_video_path` 自动填入 `Background(kind="video")` | 素材区"背景"卡片默认就绪 | embedded-only |
| A11 | Standalone 文件 IO | 新建 / 打开 / 保存 `.krrender.json` | 项目文件 | standalone-only |

### P1 — 正式版必须

- **B1** ふりがな (ruby) 渲染：主行上方，独立字号/颜色/k 时间
- **B2** 多歌手区分：按 `singer_id` 分配 Style；合唱重叠双行
- **B3** 渐变填充色：填充色线性渐变（起色→终色 + 角度）
- **B4** 入场 / 退场动画：滑入 / 淡入 / 上移；可配 `lead_in_ms` `fade_out_ms`
- **B5** 样式预设：`.krstyle.json` 保存 / 加载，独立于项目
- **B6** 背景类型：纯色 / 静态图片 / 循环视频（无主视频场景）
- **B7** 标题字幕：曲名、艺术家叠加在指定时段
- **B8** 输出参数完整化：crf / preset / pix_fmt / 硬编探测（nvenc/qsv/amf）
- **B9** 縦書き（日文垂直排版）：字形旋转 / 标点替换 / ruby 横转纵——工作量 3-5×

### P2 — 锦上添花

- C1 发光（glow）/ 多层描边
- C2 时段图片插入
- C3 时间轴拖动连续预览（无音）
- C4 多 Style 预设模板库
- C5 导出帧序列 PNG
- C6 单句 Style 覆盖

---

## B. 双模式数据流

### Standalone
- **入口**：`python -m krok_helper.subtitle_render` / 菜单"文件 → 新建"
- **加载**：三个独立选择按钮——字幕（`.lrc`）/ 背景视频 / 音频
- **存储**：项目 → `.krrender.json`；全局首选项 → `AppSettings.subtitle_render` dict
- **装饰**：完整标题栏 + 菜单栏（文件 / 编辑 / 视图 / 帮助）+ 状态栏

### Embedded
- **构造**：`SubtitleRenderWindow.for_embedding(parent, settings_provider, workflow_context)`
- **上游数据**（自动获得，左侧素材区只读）：
  - 字幕：`context.subtitle_lrc_path`（SUG `NicokaraExporter` 落盘的 Nicokara 逐字 LRC）。SUG 完成第 4 步时自动调用 `NicokaraExporter` 落盘到工作流目录约定位置（如 `output/{slug}/subtitle.lrc`）
  - 背景视频：`context.aligned_video_path`（第 2 步对齐后的 1080p60 mp4），**默认作为背景层**
  - 音轨：与背景视频同源（mux）或独立流
  - 偏移：`context.alignment_offset_ms`
- **下游交付**：写 `context.subtitle_video_path`，发 `workflow_step_completed` 信号
- **去装饰**：参考 SUG 的 `_remove_embedded_title_bar()` / `_apply_embedded_ui_policy()` 模式（位于 `krok_helper/lyrics_timing/src/strange_uta_game/frontend/main_window.py:212` / `:236`）

### 设置桥
- 骨架已实现 `KrokHelperSubtitleRenderSettingsBridge`（`krok_helper/subtitle_render/settings_bridge.py`），仿照 SUG 的 `KrokHelperSettingsBridge`（`gui_qt.py:182-244`）
- 暴露字段示例：`default_font_family` / `default_resolution` / `default_fps` / `prefer_hwenc` / `output_dir` / `recent_style_presets`——存进 `AppSettings.subtitle_render` dict 的对应 key

---

## C. 界面布局设计

参考 Sayatoo（日系卡拉ok打轴工具）的工业标准布局——歌词列表 / 视频预览 / 样式属性
三列横排，波形与字幕轨道全宽贴底。

### 总体

```
┌─ 顶部菜单栏（standalone：文件/编辑/模板/工具/窗口/帮助；embedded 隐藏） ─┐
├─ 顶部工具栏：[选择字幕] [选择背景视频] [选择音频] + 加载状态 ──────────┤
├──────────────┬──────────────────────────┬────────────────────────┤
│ 左·歌词列表 │ 中·预览区                  │ 右·属性面板          │
│ (QTableView) │ (QGraphicsView 含字幕叠加) │ (QTabWidget)        │
│              │                            │                      │
│ 列：         │                            │ 标签页：             │
│  行号 / S /  │                            │  基本 / 字幕 / 特效 │
│  角色 / 内容 │                            │  / 装饰              │
│              ├──────────────────────────┤                      │
│              │ 播放控件 + 时间码           │                      │
├──────────────┴──────────────────────────┴────────────────────────┤
│ 底·波形（全宽，含 playhead，与预览同步）                           │
├──────────────────────────────────────────────────────────────────┤
│ 底·字幕轨道 T1 / T2 / T3...（全宽，按行/按字渲染色块，对齐 playhead）│
└──────────────────────────────────────────────────────────────────┘
```

### 各区组件（PyQt6 原生为主，qfluentwidgets 渐进引入）

- **顶部菜单栏**（standalone）：`QMenuBar` ——文件（新建/打开/保存/导入字幕/导出 MP4）/ 编辑 / 模板（样式预设管理）/ 工具 / 窗口 / 帮助
- **顶部工具栏**：`QHBoxLayout` 一行按钮 + 加载状态短文案；embedded 模式下保留但隐藏 standalone 专属项
- **左·歌词列表**：`QTableWidget`，列 `行号` / `S`（演唱者切换标志） / `角色`（演唱者名） / `内容`；行高亮跟随预览 playhead 当前行；点击行可跳转 playhead
- **中·预览区**：`QGraphicsView` + 自定义场景——背景层（`QMediaPlayer.setVideoSink` 取视频帧 / 纯色 / 静态图） + 字幕层（QPainter 离屏帧叠加）；预览路径与渲染路径共享同一 `paint_frame`
- **中·播放控件**：播放 / 暂停 / 录制（用作"渲染当前预览片段"） / 时间码（`00:06.77`） / 速度（`1.0x`） / 跳转滑块
- **右·属性面板**：`QTabWidget` 四标签 —— 基本（屏幕预设 / 像素纵横比 / 宽高 / 视图）/ 字幕（字体 / 字号 / 颜色 / 描边 / 阴影）/ 特效（动画 / 渐变 / 发光 P2）/ 装饰（标题字幕 / 时段图片 P2）；每个标签内用 `SettingCardGroup` 折叠分组
- **底·波形**：`QGraphicsScene` 自绘，复用第 2 步波形对齐模块的渲染逻辑（参考 `audio_alignment.py::extract_waveform`），横轴时间、纵轴振幅，playhead 与预览实时同步
- **底·字幕轨道**：`QGraphicsScene` 自绘——多行（T1/T2/T3）色块，每行展示一段歌词的演唱区间，块内细分到字符级；按演唱者上色；点击块可定位到对应行 / 字

### Sayatoo「基本」页专项任务

当前已确认 Sayatoo「基本」页包含「屏幕 / 视图 / 布局 / 时间」四组，内部参数可从 `SayaSub2.exe` / `CoreSuites.dll` 抽到 `screen.*`、`viewport.*`、`layout.*`、`time.*` key。实现优先级和难度不要凭截图临时判断，按 [`docs/Sayatoo基本页逆向与实现计划.md`](Sayatoo基本页逆向与实现计划.md) 执行。

近期优先做：

- P0 / 低难度：屏幕预设、宽高、fps；时间组的提前入场、延迟退场、偏移。
- P1 / 中难度：2D 视图变换（对齐、X/Y、缩放、Z 旋转）；第一行 / 第二行独立对齐和 X/Y。

明确推迟：

- P3 / 高难度：3D 旋转、竖排文字、从右到左、路径曲度、多行 / 滚动布局。

### 模式差异
- standalone：完整 `QMenuBar` + `QStatusBar` + 独立窗口标题
- embedded：隐藏 `QMenuBar`、`QStatusBar`；顶部工具栏的"选择字幕/视频/音频"改为只读卡片（数据来自 `WorkflowContext`）；最右下追加"渲染并进入下一步"按钮，替代菜单导出

---

## D. 数据模型（`krok_helper/subtitle_render/models.py`）

骨架阶段只有占位 dataclass，需按下方结构补全字段：

```
RenderProject (dataclass, JSON 序列化)
  ├ subtitle_source: SubtitleSource
  │    ├ path: str                      # 必须是 Nicokara 逐字 LRC (.lrc)
  │    └ singer_filter: list[int] | None  # 按演唱者过滤显示（None=全部）
  ├ background: Background
  │    ├ kind: Literal["solid","image","video","loop_video"]
  │    ├ color: str
  │    ├ path: str | None
  │    └ video_offset_ms: int
  ├ audio_path: str | None
  ├ global_style: Style
  ├ singer_styles: dict[int, Style]
  ├ line_style_overrides: dict[int, Style]   # P2
  ├ title_overlay: TitleOverlay | None
  ├ output: OutputConfig
  └ schema_version: int = 1

Style: font_family / font_size_px / weight / italic / vertical / fill_color
       / fill_color2 / gradient_angle_deg / base_color / stroke_color
       / stroke_width_px / shadow_color / shadow_offset / glow_radius_px (P2)
       / ruby_size_ratio / ruby_color / ruby_vertical_offset_px
       / entry_anim / entry_lead_ms / exit_anim / exit_fade_ms / line_y_position

OutputConfig: width / height / fps / video_codec / encoder / crf / preset
              / pix_fmt / audio_codec / audio_bitrate_kbps / output_path
```

- 项目文件后缀：`.krrender.json`
- 样式预设后缀：`.krstyle.json`
- `schema_version` 字段保留升级空间

---

## E. 渲染引擎技术选型

**已选定：QPainter 离屏 + ffmpeg rawvideo pipe**（不要改方案，已与用户达成一致）

- 流程：枚举每帧 t → `QPainter` 画到 `QImage(Format_RGBA8888)` → bytes → `subprocess.Popen` 写入 ffmpeg stdin (`-f rawvideo -pix_fmt rgba`) → ffmpeg 同时读 `-i background` 和 `-i audio` 做合成与编码
- **关键优势**：
  1. 卡拉ok填充逐像素精确，渐变/发光/动画完全自由
  2. **预览与渲染共用 QPainter 绘制函数**，所见即所得
  3. 直接复用 `krok_helper/ffmpeg.py` 的 `run_command` / `find_tool` / `probe_media`
  4. 硬编探测沿用 `audio_alignment.py::export_aligned_video_v2`（参考其分支判断写法）
- **缓解 CPU 占用**：多线程绘帧（producer N×QPainter → 队列 → 单 consumer 喂 ffmpeg），目标 1080p60 实时倍率 ≥ 0.5×

**预览策略**：同引擎降分辨率（`QImage(960, 540)`），定位用 `QMediaPlayer.setPosition()` 取静态背景帧，叠加 QPainter 字幕。保证"预览=输出"。

**否决方案**（避免重新讨论）：
- ❌ 生成 `.ass` + libass burn-in：渐变 / 发光 / 复杂动画表达受限；预览路径割裂。可作为 P2 "极速导出"按钮，但不是主路径。
- ❌ Pillow / Skia / Web 离屏：与现有 PyQt 基建脱节、PyInstaller 打包冲突。

---

## F. 关键文件清单

### 已建（骨架，需要扩充实现）

- `krok_helper/subtitle_render/__init__.py` — 导出 `SubtitleRenderWindow`
- `krok_helper/subtitle_render/__main__.py` — standalone 入口
- `krok_helper/subtitle_render/models.py` — D 节所有 dataclass + JSON IO（**占位**，待 A1 阶段填字段）
- `krok_helper/subtitle_render/settings_bridge.py` — `KrokHelperSubtitleRenderSettingsBridge`
- `krok_helper/subtitle_render/frontend/main_window.py` — `SubtitleRenderWindow(embedded, settings_provider, workflow_context)` + `for_embedding()`
- `krok_helper/subtitle_render/engine/painter.py` / `animator.py` / `timeline.py` / `renderer.py` / `encoder_select.py` — **均为空占位**，按 P0 优先级实装

### 待新建（按 P0 顺序）

- `krok_helper/subtitle_render/subtitle_sources.py` — Nicokara 逐字 LRC 解析器（SUG submodule 只有导出器，没有解析器，需新写；参考 `lyrics_timing/.../exporters/nicokara_exporter.py` 的格式规范反向实现）
- `krok_helper/subtitle_render/project_store.py` — `.krrender.json` 持久化
- `krok_helper/subtitle_render/style_presets.py` — `.krstyle.json` 管理（B5 时落地）
- `krok_helper/subtitle_render/workers.py` — `QThread` 包装渲染器，发 progress/log/finished/error 信号
- `krok_helper/subtitle_render/cli.py` — `python -m krok_helper.subtitle_render --project foo.krrender.json --output out.mp4`（CI 烟测用）
- `krok_helper/subtitle_render/frontend/preview_view.py` / `timeline_view.py` / `property_panel.py` / `material_panel.py` / `output_bar.py` / `menus.py`

### 修改现有
- `krok_helper/gui_qt.py` 第 2515-2533 行（**已改**：PlaceholderPage 替换为 `SubtitleRenderWindow.for_embedding`，并注入 `KrokHelperSubtitleRenderSettingsBridge`）
- `krok_helper/settings.py`：`AppSettings.subtitle_render: dict` 字段（**已加**）
- WorkflowContext：新增 `subtitle_project_obj` / `subtitle_ass_path` / `aligned_video_path` / `alignment_offset_ms` / `subtitle_video_path` 字段——**接 A10 时落地**，需先找清楚现存 WorkflowContext 类（或自己定义）
- `krok_helper/ffmpeg.py`：可选抽出 `select_video_encoder()` 公共函数
- PyInstaller spec：新模块入口加入打包白名单（接 A8 输出 MP4 完成后）

---

## G. 验证方案

- **单元测试**（建在 `tests/test_subtitle_render_*.py`）：
  - `test_painter.py` — 给定固定 Style 与时间戳，paint_frame 输出像素哈希校验
  - `test_models.py` — RenderProject JSON round-trip
  - `test_subtitle_sources.py` — 三格式解析回归（fixture 用 SUG 测试集里的 `.ass`）
- **集成测试**：用 fixtures 的 `.ass` + 5 秒纯色背景 + 5 秒静音音频跑完整 pipeline，校验产出 mp4 时长 / 编解码参数
- **CLI smoke test**：`python -m krok_helper.subtitle_render --project fixtures/demo.krrender.json --output /tmp/out.mp4`，CI 跑（无 GUI）
- **手测清单**（每次 MVP 演示）：
  1. standalone 启动、加载 SUG `.ass` + 背景 mp4 + 音频，预览能动、能渲染 10 秒切片
  2. embedded 模式从工作流第 4 步进入，能读到上游字幕和视频，渲染后第 6 步按钮亮起
  3. Ctrl+S 保存 / Ctrl+O 打开项目（standalone 独有）
  4. 渲染中点取消能立刻停止 ffmpeg

---

## H. 已确认的产品决策（不要重新讨论）

1. **嵌入模式下字幕传递**：**单一文件路径**——SUG 第 4 步完成时调用 `NicokaraExporter` 把 Project 落盘成 Nicokara 逐字 LRC，第 5 步通过 `WorkflowContext.subtitle_lrc_path` 读文件。
   - 不再走内存对象直传：实现简单、调试方便、和 standalone 模式共用同一路径
   - 模块内部只需要一个入口 `load_from_lrc(path)`
   - 若将来发现 IO 成为瓶颈再考虑内存直传补丁
2. **嵌入模式背景默认行为**：**默认叠在第 2 步对齐后的 MV 上**。
   - 素材区"背景"默认值 = `Background(kind="video", path=context.aligned_video_path)`
   - 用户仍可在属性面板切换为纯色 / 图片 / 循环视频
3. **垂直書き（縦書き）**：**推迟到 P1（B9）**，MVP 仅支持横书き。
   - 影响 Style 字段：`vertical` 标志位 MVP 保留但只允许 `False`；P1 阶段实装字形旋转 / 标点替换 / ruby 横转纵
4. **样式预设包**：**MVP 只给空白默认**。
   - `style_presets.py` MVP 阶段实现保存 / 加载 `.krstyle.json` 的能力（B5）但不附带任何内置预设包
   - 预置预设包后续作为独立工作单独排期，可能由非工程师贡献
