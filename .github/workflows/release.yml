name: Build And Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller PySide6

      - name: Build Windows package
        shell: cmd
        run: scripts\build_windows.bat

      - name: Archive Windows package
        shell: powershell
        run: |
          Compress-Archive `
            -Path "dist\windows\Karaoke Helper\*" `
            -DestinationPath "KaraokeHelper-windows.zip" `
            -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: KaraokeHelper-windows
          path: KaraokeHelper-windows.zip

  build-macos:
    name: Build macOS
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install pyinstaller PySide6

      - name: Build macOS package
        run: bash ./scripts/build_macos.command

      - name: Archive macOS package
        run: |
          cd dist/macos
          ditto -c -k --sequesterRsrc --keepParent "Karaoke Helper.app" "../../KaraokeHelper-macos.zip"

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: KaraokeHelper-macos
          path: KaraokeHelper-macos.zip

  release:
    name: Publish Release
    needs:
      - build-windows
      - build-macos
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: KaraokeHelper-windows
          path: release-assets/windows

      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: KaraokeHelper-macos
          path: release-assets/macos

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release-assets/windows/KaraokeHelper-windows.zip
            release-assets/macos/KaraokeHelper-macos.zip
