# 不参与 pip freeze hash 计算的包名前缀（不区分大小写）
# 这些包是纯开发工具，不会被 PyInstaller 打入 runtime，
# 因此不需要因为它们的版本变化而重建 runtime zip。
#
# 规则：前缀匹配（pip 里的 foo-bar 和 foo_bar 视为同一包）
# 修改后提交 git，所有开发者共享同一份排除规则。

# ── 包管理 ──
pip
setuptools
wheel
pkg_resources

# ── 打包工具 ──
pyinstaller
pyinstaller-hooks-contrib

# ── 发布工具 ──
build
twine
readme-renderer
pkginfo
requests-toolbelt
rich

# ── 测试 ──
pytest
pytest-cov
pytest-mock
coverage

# ── 代码质量 ──
black
ruff
flake8
mypy
isort
pylint
pyflakes
pycodestyle
mccabe
types-

# ── 开发流程 ──
pre-commit
nox
tox
