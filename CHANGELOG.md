# Changelog

本项目所有重大变更都记录在此。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

打轴模块（StrangeUtaGame）合并前的历史见
[`krok_helper/lyrics_timing/CHANGELOG.md`](./krok_helper/lyrics_timing/CHANGELOG.md)。

---

## [Unreleased]

### 特性改变
- Windows 字幕预览会依据主文字描边宽度选择实测更快的 Direct2D 后端：8px 以上宽描边自动使用 WARP，细描边继续使用硬件双 worker；可通过环境变量关闭自动选择或调整阈值。
- GPU 预览改为按实际 roundtrip 延迟提前渲染有界未来帧，只有媒体时钟命中时才交付，避免复杂工程的渲染结果持续落后播放进度。

### 修复项目
- 修复连续 60fps 播放时 geometry realization 预热因始终等不到 100ms 空闲窗口而永久停滞的问题；样式连续修改时后端选择保持单向稳定，不再反复重建设备缓存。

---

## [4.1.1.1] — 2026-07-20

修复 Windows 打包版启用 GPU 字幕预览时弹出命令行窗口的问题。

### 修复项目
- GPU 字幕渲染进程现在以无控制台窗口方式在后台启动，启用预览时不再弹出黑色命令行窗口；渲染通信与日志读取保持不变。

---

## [4.1.1] — 2026-07-20

修复 4.1.0 Windows 增量更新遗漏 GPU 渲染器的问题；本版本替代 4.1.0
作为稳定版本，4.1.0 全量包不受该问题影响。

### 修复项目
- 修复从旧版本通过 app 增量包更新到 4.1.0 后缺少 `krok_subtitle_renderer.exe`，导致字幕 GPU 预览提示异常并回退 Painter、GPU 导出无法启用的问题。

---

## [4.1.0] — 2026-07-20

字幕视频生成正式启用 Windows GPU 预览与导出，并集中对齐 NicoKaraMaker3 的布局、
注音和动画表现；同时完善工程打开、导出通知、窗口布局与样式编辑体验。

### 新增功能
- Windows 字幕预览与导出默认启用 GPU 渲染，CPU QPainter 路径继续作为自动回退；GPU 路径现已覆盖注音、角色与行内配色、渐变及拼色、图片填充、阴影、标题、多字幕源、导唱符、横排/竖排/RTL，以及逐行、逐字、旋转翻转和 Utopia 动画。
- 字幕渲染模块支持直接拖入 NicoKaraMaker3 `.n3proj` 项目。
- 字幕视频导出完成后播放显式通知音并显示 Fluent 完成弹窗，弹窗同时展示本次导出耗时。
- 装饰类型新增“无”，允许字幕既不使用发光也不使用阴影。
- 支持从 Windows 资源管理器双击 `.yurika` 文件启动工作台并自动进入字幕视频生成模块；将 Karaoke Studio 设置为该扩展名的默认应用后即可使用。

### 特性改变
- GPU 预览加入有界调度、缓存与停滞恢复，持续播放性能提升至稳定 60fps；GPU 导出优化发光缓存与帧回读流水线，降低复杂字幕的渲染开销。
- 全面强化 N3 布局语义兼容，按项目画布正确还原字体像素、水平度量、逻辑行框、分页显示窗口、上下轨位置、居中锚点、页首字体、负字距与注音避让。
- 注音配色新增默认勾选的“默认跟随主文字”，会同步文字、描边、二重描边、装饰、渐变方式与色号等完整配色，并将开关状态保存到角色方案。
- 编码器、视频编码、CPU preset、CRF、渲染进程数及 GPU 预览/导出开关会保存到本机，新建项目默认沿用上次设置。
- 从 N3 批量导入角色模板后不再默认全选，避免误操作一次导入大量项目角色。
- 预览/导出切换移至页面顶部中央并改为滑块式工作区切换控件，为字幕轨道腾出更多高度。
- 预览窗口最小化后改为停靠在应用中下部并持续显示“预览窗口”；默认窗口尺寸改为完整 16:9 视频画面加独立标题栏，标题栏不再遮挡视频。

### 修复项目
- 修复 Utopia 与逐文字渐隐、旋转翻转组合时退场特效失效，以及动画结束后文字主体、逐字遮罩和时间时钟未正确释放的问题。
- 修复 GPU 路径下 Utopia 进场时画面左侧出现多余发光、注音开头发光缺失及紧密字形交叠异常的问题。
- 修复分色与普通渲染路径下角色注音字体、英数字体和配色未完整生效的问题，CPU 与 GPU 结果保持一致。
- 修复 N3 注音逐字擦除时序、注音碰撞布局以及高分辨率项目字体尺寸换算偏差。
- 修复原生预览任务积压导致播放卡顿的问题，并按实际显示分辨率与缩放比例渲染预览画面。

---

## [4.0.7.2] — 2026-07-19

更新 StrangeUtaGame 至 v1.4.0，新增全文本查找替换、多项目拼接和词典搜索过滤等，@Xuan-cc
同时修复 LRC 导入时项目角色丢失和 N3 样式预设未能持久化的问题。@Myosotis11037

### 新增功能
- **打轴模块**
- **全文本编辑界面支持查找与替换**（Ctrl+F 或点击「查找和替换」按钮）
  - 支持普通查找与替换模式
  - 搜索时可选剥离演唱者标签、时间戳、注音块等结构化标签，仅匹配可见文字
  - 匹配结果高亮标记
- **多项目拼接功能**（打轴界面-文件管理-多项目拼接）
  - 将多个 SUG 文件拼接为一个完整项目（合作,串烧用）
  - 卡片拖拽排序，自由调整各项目顺序
  - 每张卡片显示项目名、音频长度，支持设置间隔时间
- **用户词典和网络词典支持搜索过滤**（设置-读音词典-编辑用户词典 / 网络词典管理）
  - 输入关键词实时筛选词条，仅显示匹配行（行号保持原词典序号不变）
  - 新增「置顶」「置底」按钮，方便快速移动词条位置
- **工作台**
- 工作台支持监听外部字幕源文件变化，外部修改后提示用户重新加载。

### 特性改变
- 打轴模块**调整原始时间戳范围放宽**至 ±300 分钟（原 ±99999 ms），适应超长时间轴调整场景。
- 更换 LRC 歌词文件时保留项目角色方案，不再丢弃已配置的角色配色。

### 修复项目
- 修复 N3 样式预设（字体模板 / 角色方案）在关闭或重开工程后未能正确持久化的问题。
- 修复打轴模块更新检测弹窗与保存文件对话框同时弹出导致 UI 死锁。
- 修复打轴模块弹窗切换应用后再切回时无法点击的问题（更新弹窗、闪退恢复弹窗）。

---

## [4.0.7.1] — 2026-07-18

本次为经确认的合并发布：修正字幕注音避让与裁剪，并同步新版 StrangeUtaGame，
完善打轴模块的自动保存、备份恢复、更新取消和窗口层级行为。

### 特性改变
- 更新 StrangeUtaGame 子模块到 `27c7112`，包含 SUGv1.3.10 与 SUGv1.3.11 的用户可见改进。
- 打轴工程备份统一使用 `.bak` 后缀，旧格式备份只在默认位置检测，减少其他目录中的误识别。
- 移除打轴模块四处窗口的系统级置顶行为，并更新部分界面翻译与自动保存说明。

### 修复项目
- 对齐 NicoKaraMaker3 的注音字符盒、均分和相邻避让算法，修复带逐字符角色的歌词未避让、汉字间距被过度拉开，以及注音描边异常截断的问题。
- 修复打轴模块自动保存与临时文件的触发方式相反、更新检测与闪退恢复时序异常，并新增明确的闪退恢复入口。
- 修复打轴模块更新器在更新过程中无法取消的问题。

---

## [4.0.7] — 2026-07-18

集中完善字幕工程安全性、SVG 导唱符与时间轴控制：加入未保存保护、自动保存和备份恢复，
支持直接导入及批量替换 SVG 导唱符，并进一步对齐 NicoKaraMaker3 的图片填充和逐字渲染表现。

### 新增功能
- 字幕工程新增完整的未保存状态与退出保护，并支持自动保存、异常恢复、定时备份及外部文件冲突检测。
- 支持把 SVG 作为行内导唱符直接参与歌词排版和逐字走字，无需再制作专用字体；可设置导唱符数量与提前时间。
- 新增批量识别导唱标记，可检测行首或任意位置的指定字符、预览候选行并选择性替换为 SVG；逐字符角色窗口也可直接替换选中字符。
- 字幕导出可选择并行进程数，方便高核心数 CPU 使用更多渲染进程。
- 时间设置新增“同步入场”，可让同段落各轨道的首句同时上屏，并保留手动上屏时间的优先级。

### 特性改变
- 字体字重菜单会根据当前字体实际支持情况动态显示；可变字体仍提供有效的连续字重选择。
- 色号输入改为输入后立即应用；百分比、像素等数值框的单位后缀不再被选中，减少编辑干扰。
- 字幕预览播放器只在预览 tab 显示和响应播放快捷键，切换到其他页面或导出时不再残留控制。
- 图片填充进一步对齐 NicoKaraMaker3：修正缩放方向和全局画布锚点，并在文字超出图片范围时循环平铺图像。
- 带注音正文可选择按注音可视字符数分配逐字进度，使注音与正文走字对应更自然。

### 修复项目
- 修复奇数句段落的末句与下一段首句错误配页，导致字幕跨段长时间挂屏、同步退场时间不一致的问题。
- 优化行内 SVG 导唱符的布局与走字缓存，修复含导唱符歌词行预览或播放时明显卡顿的问题。
- 修复 NicoKaraMaker3 图片填充导入时的缩放、透明区域和缺失图片配置处理差异。
- 修复 Utopia、主文字及注音在逐字扫光边界处的发光、阴影、渐变锚点和前后颜色交接异常。

---

## [4.0.6] — 2026-07-16

集中完善字幕视频生成的工程、样式与导出工作流：新增工程拖入、方案保存和导出目录偏好，
让字体及布局随视频分辨率自动缩放，并修复逐字走色、注音、字体匹配与 FFmpeg 导出的多项问题。

### 新增功能
- 支持直接拖入 `.yurika` 工程，并在重新打开工程时清理旧素材状态、提示缺失资源。
- 字体配色方案与布局方案新增明确的“保存为软件默认值”操作，新建项目可继续使用已保存参数。
- 角色预设支持在同名跨分组候选之间选择；未命名的 SUG 演唱者会明确映射到全局方案。
- 导出页新增输出目录偏好，并可在导出完成后选择返回工作流或继续当前操作。
- 渐变关键点支持跨控件复制粘贴，配色状态可一键交换唱前与唱后颜色。
- 工作台新增启动加载窗口并更新启动图。

### 特性改变
- 导入或替换视频时，输出宽高会自动匹配视频分辨率，字体、描边、注音与布局参数同步按高度缩放；布局示意幕布也会使用实际输出分辨率和宽高比。
- 导出预览的分辨率、编码等状态改为随设置实时刷新，无需等到开始导出；导出页设置卡片和全局设置布局同步优化。
- 标题支持与普通歌词一致的右键角色/布局操作，时间轴补充动画说明，并突出显示当前行采用的布局。
- 渐变关键点、填充控件、输入对话框及颜色按钮交互统一优化，播放/暂停按钮恢复为清晰的 SVG 图标。

### 修复项目
- 修复非日文系统区域下日文字体本地化名称无法正确匹配的问题，提高项目字体识别与回退可靠性。
- 修复 Utopia 多字退场与颜色闪烁，以及主文字、注音逐字走色边界不平滑的问题。
- 修复导唱符抬高整行注音、标题未应用布局字符间距、英数歌词联动时间丢失等排版问题。
- 修复 SUG 行内停顿附近的注音丢失，并隐藏仅供兼容使用的可配置注音停顿标记。
- 修复 FFmpeg 路径解析未正确回退系统 `PATH`，以及导出日志过多时可能发生的进程阻塞。
- 修复视频下载暗色主题格式下拉框、部分 QSS 样式解析、颜色按钮悬停描边和导出播放器按钮显示问题。
- 修复布局页在 320px 窄面板下出现横向溢出的问题，布局方案导航仍可完整操作。

---

## [4.0.5] — 2026-07-14

集中完善字幕视频生成的字体兼容、逐字走字与导出体验：对齐 NicoKaraMaker3 字体目录和排版行为，
新增实时导出监视器与 H.265 编码，并修复样式继承、注音走字及歌词补时问题。

### 新增功能
- 字体选择接入与 NicoKaraMaker3 DirectWrite 规则一致的字体目录，改善字体枚举、名称匹配与回退表现。
- 字幕导出页新增实时导出监视器，可在编码过程中查看当前画面与导出进度。
- MP4 导出新增 H.265（HEVC）视频编码选项。

### 特性改变
- 导出页改用 Fluent 卡片布局，输出路径拆分为文件夹与文件名两行输入，并提高监视画面清晰度、优化布局及停止确认提示。
- CPU 编码预设默认值由 `veryfast` 调整为 `medium`，在导出速度与成片质量之间采用更稳妥的默认平衡。
- N3 项目导入会把自动输出文件名转换为 `_yurika出力` 后缀，静默忽略本程序不支持的 N3 帧率，并按 N3 布局应用字符排版参数。
- 「允许文字咬合」移至布局卡片右下角；字幕渲染确认框、消息框和状态提示统一为工作台 Fluent 样式。

### 修复项目
- 修复字幕样式未正确继承字体的问题，避免共享样式或回退路径下出现意外字体变化。
- 修复注音走字遮罩未贴合字形几何、歌词补时与走字释放不同步，以及字符视觉层未完整释放的问题。
- 停用打轴数据自动直传字幕模块，避免未经显式载入的中间状态影响字幕工程。

---

## [4.0.4] — 2026-07-14

打轴模块更新至 SUG v1.3.9：新增间奏指引自动生成、符号时间戳强制复制和 InfoBar 报错弹窗，@Xuan-cc

### 新增功能
- 打轴界面时间戳工具新增「自动生成间奏指引」：输入间奏文本后自动搜索已打轴段落之间的空白区域，生成带时间戳的间奏指引行，文本均匀分配并支持前后边距设置。
- 「分离符号时间戳」新增强制复制开关：开启后不再检查后方字符是否已有时间戳，直接覆盖；连续句尾符号视为整体，组内均匀分配。
- 内部异常现在通过 InfoBar 弹窗提示用户，同类错误 3 秒内去重，避免连续弹窗刷屏。

### 特性改变
- 演唱者添加、编辑和删除操作增加防闪退兜底处理。

### 修复项目
- 全文本编辑界面的「更新节奏点」按钮支持中文模式（此前中文歌词下不生效）。
- 间奏指引弹窗 UI 优化。

---

## [4.0.3] — 2026-07-14

集中完善字幕视频生成的配色、角色与素材工作流：支持导入 NicoKaraMaker3 字体模板，
补齐颜色透明度、渐变和拼色兼容，并改进背景音频、更新器及批量编辑体验。

### 新增功能
- 支持从 NicoKaraMaker3 导入字体模板，可选择多个模板并导入为当前项目角色；日文、英数、注音、描边、装饰与完整 RGBA 配色会一并转换。
- 样式预设库改为独立管理界面，支持预设分组、搜索、导入、重命名、覆盖、删除及批量导入项目角色。
- 歌词支持逐字符分配角色配色，也可多选歌词行后一次批量应用角色方案，并作为单条操作撤销或重做。
- 背景支持视频、静态图片、图片序列和纯色；非视频背景可加载独立音频用于预览及 MP4 导出。
- 颜色控件支持直接输入十六进制色号、屏幕取色实时预览和 Alpha 透明度编辑。
- Windows 更新器改为独立图形界面，显示更新进度并允许取消，不再依赖控制台窗口。

### 特性改变
- NicoKaraMaker3 项目及字体模板导入进一步对齐：分别保留日文/英数/注音字号与描边，支持透明色、浮点渐变关键点、多段硬边拼色及二重描边开关。
- 内置标题方案按 NicoKaraMaker3「情報小」重新校准，英文标题不再继承歌词的英数字体；旧的错误内置标题默认值会自动迁移。
- 字体、布局、配色等样式修改现已纳入统一撤销/重做；未分配到歌词的项目角色也会随 `.yurika` 保存。
- 角色选择改为单击表格角色列弹出 Fluent 菜单，右键菜单可批量应用角色方案和布局。
- 特效、标题和颜色属性区域重新排布，常用参数更加紧凑，颜色按钮加入调色板与直接取色入口。

### 修复项目
- 修复从 4.0.1 等旧版本更新时主程序未完全退出，导致 Updater 无法替换 `_internal` 并报“拒绝访问”的问题。
- 更新检查的代理、403 限流和备用源处理与打轴模块保持一致，网络错误提示更准确。
- 修复 NicoKaraMaker3 模板 fallback 把注音字号错误解释为 100px，以及角色标签/共享字体回退导致配色或字体套用错误的问题。
- 修复暂停字符附近“走字前”描边盖住已唱字符外沿的问题。
- 视频背景不再允许另选独立音频，避免视频内嵌音轨与外部音频形成双时钟不同步。
- 修复导入项目角色后左侧歌词列表无法找到该方案，以及批量应用后表格角色名称不刷新的问题。

---

## [4.0.2] — 2026-07-12

字幕视频模块的属性面板与标题系统改版：标题外观并入配色方案与布局方案统一管理，
注音支持独立字体，并修复 N3 项目导入时注音设置丢失的问题。

### 新增功能
- 注音（ふりがな）支持独立字体设置：可单独指定注音的日文/英数字体、字号与字重，也可保持跟随主文字。
- 颜色方案新增内置「标题」方案：标题的字体与颜色和角色方案在字体页统一编辑，标题 tab 提供一键跳转。
- 布局方案内置「タイトル左上」预设（对齐 NicoKaraMaker3 出厂布局），标题与歌词行均可引用。

### 特性改变
- 标题设置大幅精简：字体/颜色/位置不再单独设置，改为引用「标题」配色方案与布局方案，标题 tab 只保留文字、布局引用与显示时段；旧工程加载时自动迁移，外观不变。
- 字幕布局设置面板重构：布局示意图居中、锚定/余白/行布局按空间语义贴边排布，角色与字体颜色面板合并。
- 属性面板多处布局优化：注音区块单行放下三个控件、视图卡片按面板宽度自适应换列。

### 修复项目
- N3 项目导入：注音的字体、字号与字重现在会正确导入（此前会跟随主文字按主字号渲染）。
- 导入 N3 项目时，标题引用的配色方案与布局分别写入「标题」方案和布局引用，与 N3 渲染一致。
- 布局页「上下配置」选居中时，字符排版区域不再整体上移。
- 「垂直与方向」的行间距输入框不再裁掉单位文字。

---

## [4.0.1] — 2026-07-12

集中完善字幕视频生成的编辑体验：加入预览音量控制和分屏比例记忆，重做歌词表格、
颜色与字体属性界面，并进一步对齐 NicoKaraMaker3 的发光表现。

### 新增功能
- 字幕预览增加音量滑块，并在播放与暂停状态间显示对应图标；新旧两种音频播放路径均会应用当前音量。
- 字体设置改为“日文 / 英数”分页，可分别设置字体、字号与字重；斜体和允许文字咬合保持为全局排版选项。
- 拼色填充支持多个颜色带和关键点，可直接制作两段以上的硬边分区配色。

### 特性改变
- 颜色设置改用“主文字 / 注音”和“走字后 / 走字前”文件夹式标签；纯色、横向渐变、纵向渐变、拼色与图像填充改为紧凑图标按钮。
- 纵向渐变与拼色使用统一的竖向预览和关键点编辑方式；颜色、位置、描边及发光参数重新排布，减少属性面板的纵向占用。
- 数值控件改为直接输入，不再弹出遮挡数值的上下步进面板；装饰设置只显示与当前装饰类型有关的参数。
- 字间距和空格宽度移至“布局”页的字符排版区域；颜色与字体卡片会按可用宽度自动横排或上下排列，横排时各自保持内容高度。
- 歌词列表的“轨 / 角色 / 特效 / 内容”列改为按字体、DPI、表头与可见内容测量；角色和特效列仍可拖动，内容列自动吸收剩余空间并使用尾部省略。
- 视频预览与歌词列表默认按 4:6 分配空间，并记住用户拖动后的分屏比例；角色卡片折叠时显示当前角色，新增、重命名、删除和预设管理改为紧凑图标操作。
- 字幕属性页顶部分类改为“字体 / 布局 / 时间 / 特效 / 标题”文字标签，含义更直观。

### 修复项目
- 修复拖宽“角色”或“特效”列时表格总宽度失控、内容列被推出可视区域，以及左右属性面板发生覆盖的问题。
- 修复窄窗口下字体卡片被挤出属性面板，以及横排时较高的颜色卡片强制拉高字体卡片的问题。
- 调整字幕发光的高斯核、半径映射和“均衡”档缩放响应，使发光范围、亮度和边缘衰减更接近 NicoKaraMaker3。

---

## [4.0.0] — 2026-07-11

工作台六步制作链路正式完整贯通：字幕视频生成模块上线，同时启用增量更新、
Updater 自更新和更可靠的跨版本更新检查。

从较旧版本直接升级时，更新弹窗可能只展示本版本说明；完整历史请查看
[CHANGELOG](https://github.com/karaoke-studio/karaoke-studio/blob/main/CHANGELOG.md)。

### 新增功能
- **字幕视频生成工作流**：第 5 步不再是占位页，可从歌词打轴项目直接载入字幕和背景视频。
- 支持导入 `.sug`、逐字 `.lrc`、`.n3proj`，并保存 / 打开 `.yurika` 字幕工程。
- 支持卡拉 OK 逐字高亮、ruby、角色 / 多歌手、多字幕源、渐变 / 图片填充、双描边、发光、竖排 / RTL、标题与逐行特效。
- 支持视频同步预览、歌词列表、时间轴、属性面板以及 60 / 120 fps MP4 导出。
- 支持 CPU / NVENC / QSV / AMF 编码选择和多进程 QPainter + FFmpeg 渲染。
- **增量更新**：Windows 发布新增 app/runtime parts 与 manifest；首次升级写入本地清单，后续只下载发生变化的 part。
- **Updater 自更新**：主程序退出前可先更新 `Updater.exe`，并提供可取消的更新准备进度窗。
- 更新检查增加 GitHub 302 跳转兜底、403 限流专属说明和跨版本更新日志聚合。

### 特性改变
- Windows 包加入 QtMultimedia 及 FFmpeg 媒体插件，支持字幕模块内的视频和音频预览。
- Windows runtime 增加构建 profile，PyInstaller 收集策略变化时强制重建，避免误复用缺组件的旧 runtime。
- 发布工具新增 `prepare` / `notes` 命令，自动同步版本号、README 和中文 Release notes。

### 修复项目
- 修复 PyInstaller 版本多进程字幕导出无法启动 worker、进度停在第一帧的问题。
- 增加 frozen multiprocessing 构建冒烟，今后 worker 启动失败会直接阻止发布包生成。

---

## [3.1.7.4] — 2026-07-11
同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程部分改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.7。@Xuan-cc
将FFmpeg路径应用至下载模块。@Myosotis11037

### 修复项目
- 分离符号时间戳功能,部分情况下出现异常前补偿 @Xuan-cc
- 通过加载文件加载内容后，无法改变窗口大小 @Xuan-cc
- 春日向双注音罗马音拗音部分情况下标注错误 @Xuan-cc
- 用户此点覆盖单字后々未能跟随用户词典 @Xuan-cc
- FFmpeg路径未应用与下载模块 @Myosotis11037

---
## [3.1.7.3] — 2026-07-07
同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.6。@Xuan-cc

### 修复项目
- 在部分场景下々被标注为くりかえし
- 按行编辑部分情况下节奏点丢失
- 仅注音模式影响句尾符号

---

## [3.1.7.2] — 2026-07-02
同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.5。@Xuan-cc

### 新增功能
- **中文拼音注音**（设置-AutoCheck-中文歌标注拼音，默认关闭）
  - 检测到中文歌词时，自动为汉字标注带声调拼音注音（如 nǐ hǎo）
  - 也可通过打轴界面工具栏「注音分析」按钮 →「中文拼音注音」手动触发
- **分离符号时间戳功能细化**（打轴界面-时间戳工具-分离符号时间戳）
  - 各符号分组新增「详情」展开按钮，可单独选择组内每个具体符号
  - 父复选框支持三态（全选 / 部分选中 / 全部不选）

### 特性改变
- **启动时自动清理上次遗留的音频缓存**（视频提取的 mp3、TSM 缓存、临时 wav 等）

### 修复项目
- **macOS 键盘映射增强**
  - 修复 macOS 上 Qt 无法识别部分符号按键（. , ; 等）的问题
  - 现在可以在 macOS 上将这些符号键设置为快捷键
- 自动补偿时间戳不再把句尾时间戳字符与后方字符混在同一补偿段
- 仅注音模式不再影响字符的句尾/行尾标记

---
## [3.1.7.1] — 2026-07-02
同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.4。@Xuan-cc

### 特性改变
- 拖动时间戳功能UI相对位移弹窗更改为鼠标右下方
- 自动补偿时间戳んン促音捨假名等不再属于平假名片假名

### 修复项目
- 同一行中间的自动补偿时间戳异常忽略了自身的句尾时间戳
- 春日项输出句尾时间戳丢失
---

## [3.1.7.0] — 2026-07-01
同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.3。@Xuan-cc

### 特性改变
- 允许在全文本编辑界面手动添加演唱者标签
- 解析演唱者时,自动过滤纯空格的同一演唱者

### 修复项目
- 导唱待办标记在全文本编辑界面丢失

---

## [3.1.6.10] — 2026-06-30

同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.2。@Xuan-cc

### 新增功能
- **波形时间标签支持拖拽编辑**（设置-打轴-波形时间标签拖拽，默认开启）
  - 直接拖拽波形上的时间标签把手即可调整时间戳
  - 单击把手定位到对应时间点，Ctrl+单击可多选同时拖拽
  - 拖拽时实时显示时间偏差值
  - 拖拽操作支持撤销/重做（Ctrl+Z / Ctrl+Y）
- **波形标签显示开关细化**（设置-打轴-波形时间标签）
  - 显示开关拆分为「显示字符」与「显示注音」两个独立开关
  - 关闭字符时注音自动跟随关闭
- **波形标签显示效果增强**
  - 标签文字之间不再互相重叠（优先显示左侧标签）
  - 字符/注音文字增加半透明背景，在波形上更容易辨认
- **新增「春日向注音」「春日向注音（带罗马音）」两种导出格式**（设置-导出-默认导出格式）
- **提供 SUG 默认保存目录设置**（设置-自动保存-SUG默认保存目录）
  - 设置后，保存未命名项目时优先使用此目录
  - 留空则不启用，沿用已有保存路径
- **批量填充导唱待办**（打轴界面-插入导唱符）

### 特性改变
- **打轴设置页重新分组**（设置-打轴）
  - 原单一「打轴设定」拆为 5 个分组：时间补正 / 波形时间标签 / 鼠标与焦点 / 预览指引 / 按键音效
  - 节拍器校准移至「时间补正」组内
- **大幅优化波形绘制性能，打轴更流畅**
  - 连续打轴时波形标签刷新不再卡顿
  - 波形隐藏时不再占用打轴性能，重新显示后自动恢复

### 修复项目
- 修复鼠标滚轮缩放时缩放滑条位置不跟随
- 修复导入带时间戳的歌词/项目时波形标签不显示

---

## [3.1.6.9] — 2026-06-22

同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程小部分改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.1。@Xuan-cc
更改打包winRT为sudachi。@Xuan-cc

### 新增功能
- 查询字典候补条目时允许删除字典
- 新增禁用点击时居中按钮（打轴-禁用点击时居中）
- 项目备份兜底功能（自动保存-自动备份项目个数）
  - 默认10个，允许（0-99999个）
  - 当用户选择放弃或者保存时保存一份备份，避免误保存或误点击
  - 默认位置.\ProjectBackup
- 添加SUG-github链接打开按钮，方便快速发起issue

### 特性改变
- 从文件加载歌词时，创建新项目（含未保存检测）  
- AutoCheck-符号节奏点相关更新：
  - 所有符号是否生成节奏点合入“生成节奏点的字符类型”
    - 涵盖所有Unicode符号
  - 额外check规则点里的符号参与节奏点移除
- 项目.temp文件与.autosave临时文件更改为存储在.\ProjectBackup\.cache
- 更改打包winRT为sudachi

### 修复项目
- 新建项目未保存时，点击取消也会新建项目
- 单字非连词组多轴点且后方有无轴点字符时，走字异常
- utaten模式下分词异常
- 自动补偿时间戳识别字符范围不足
- 自动补偿时间戳未识别居中的句尾字符

---

## [3.1.6.8] — 2026-06-22

同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程小部分改动。

更新 StrangeUtaGame 子模块到 SUGv1.3.0。@Xuan-cc
修复Hi-res设置非法字符校验不全。@Xuan-cc

### 特性改变
- 单字清除全部节奏点升级为清除所选字符的节奏点
- 优化LLM注音逻辑，减少遗漏注音，并提高反馈感

### 修复项目
- **尝试修复默认路径应用规则**
  - 优先级：（手动浏览的路径>已保存的项目路径
            - >默认导出路径>上一次加载路径）
- 补全时间戳功能中“·”符号被误判为平假名
- 全文本编辑界面中如[[T],{{}的字符误解析

---

## [3.1.6.7] — 2026-06-22

同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程小部分改动。

更新 StrangeUtaGame 子模块到 SUGv1.2.6。@Xuan-cc
修复部分场景下闪退,打轴模块显示项目名称。@Xuan-cc

### 新增功能
- 开放**SUG所有工具栏**的按钮允许通过快捷键设置打开。
- 打轴模块可显示SUG项目状态。

### 修复项目
- 若保存文件过慢时，新建项目有概率与保存项目冲突。
- 部分场景下导出时KS闪退兜底。
- Hi-res混流设置保存异常。

---

## [3.1.6.6] — 2026-06-21

同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程小部分改动。

更新 StrangeUtaGame 子模块到 SUGv1.2.5。@Xuan-cc
修复子模块音频缩放条失效，优化波形对齐缩放手感。@Xuan-cc
  
### 新增功能
- **一键修改所有注音为罗马音**
  - 自动注音管理 - 全部转为罗马字
  - **仅经过简单测试,有BUG请反馈**
- **自动注音·仅注音Mode**
  - 该方案注音保留节奏点
- **分色标签助手功能增强**
  - 允许通过**[浏览]**填写文件名
  - 允许**删除**不需要的标签
  - 允许**手动添加**非演唱者标签
- **时间戳工具增加（打轴-时间戳工具）**
  - 删除所有时间戳
  - 删除所有时间戳（保留首行）
  - 删除所选范围时间戳

### 特性改变
- **优化WIN10深色模式显示**
  - 输入框/滑条/对话窗口/按钮等
- **优化整体UI风格**
  - 对话框更换为Win11风格
  - 修正部分风格错误的按钮
  - 弹出窗口时自动暗屏
- **优化不同分辨率/多屏幕显示**
- 优化自动注音进度显示
- 优化滚动条大小
- 调整原始时间戳范围放宽（放宽至-99999~99999ms）
- 新建项目时自动清理残留的nicokara标签
- 分色助手可以读取标签内已有的emojiTag
- 更改自动注音按钮文本说明

### 修复项目
- **首次新建歌词时歌词滚动条不显示**
- 读取lrc时无法解析emoji标签
- 纯新项目先导入音频再导入歌词导入音频丢失
- backspace部分场景下焦点滚动异常

---

## [3.1.6.5] — 2026-06-19

仅同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.2.3。@Xuan-cc
  
### 特性改变
- **工具栏各窗口现在可以直接用enter确认**
- 异步保存SUG
- 优化各种显示UI动画
- 时间波形上的Ruby贴着字符显示

### 修复项目
- 修复KS来源的utaten歌词自动分析节奏点异常
- 修复部分采样率下变速音频异常
- 修复部分场景下音频渲染并发

---

## [3.1.6.4] — 2026-06-17

仅同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.2.1。@Xuan-cc

### Added

- 打轴模块新增隐藏 hitbox 高亮功能（设置 → 打轴 → 隐藏焦点高亮），含红色高亮与蓝色高亮，仅拖拽时高亮保留。
- 打轴模块波形时间轴上新增 ruby 显示。

### Changed

- 优化打轴性能。
- 优化数字注音逻辑：可按 ruby 分配节奏点，自动切分分配读音。

### Fixed

- 修复按类型删除注音窗口遗漏促音选项的问题。
- 修复直接应用模式下增减节奏点的 BUG。
- 修复删除平假名注音时误删除片假名「ッ」的注音「っ」的问题。
- 修复未给长音符号注音的问题。

---

## [3.1.6.3] — 2026-06-17

修复 YouTube 推荐清晰度显示与实际下载不一致的问题。

### Fixed

- 修复 YouTube 视频解析后显示“最佳质量”时，实际下载可能被通用 H.264 规则降级到较低清晰度的问题；现在默认下载会使用解析列表中展示的具体最高质量音视频格式组合。

---

## [3.1.6.2] — 2026-06-17

修复打轴模块导入新歌后按键音默认无声，以及打包发布版中英文歌词「按音节注音」失效的问题。

更新 StrangeUtaGame 子模块到 c47e2f9。

### Fixed

- 修复打轴模块导入新歌后，打开按键音却默认无声、需要手动切换一次音效才能恢复的问题。
- 修复打包发布版中英文歌词「按音节注音」失效的问题：补齐 pyphen 连字词典数据后，英文单词重新按音节生成节奏点（此前整词只在首字母生成一个节奏点）。

---

## [3.1.6.1] — 2026-06-17

仅同步打轴模块 StrangeUtaGame 到最新修复版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 827989d。

### Fixed

- 修复打轴模块在播放中触发自动保存时可能造成 UI 短暂停顿的问题，尤其是打一行后立即双击回放时的卡顿。
- 修复同行内回放刚打好的时间点时预览走字可能不刷新的问题。

### Changed

- 优化打轴模块 `.sug` 保存序列化路径，减少自动保存耗时。
- 增加默认关闭的打轴性能诊断日志；开启后 release 版日志位于 `logs\sug-timing-perf.log`。

---

## [3.1.6] — 2026-06-17

修复嵌入式打轴模块在跳转时间戳和长连词预览场景下的卡顿问题。

更新 StrangeUtaGame 子模块到 e9cdf47。

### Fixed

- 修复嵌入工作台时双击跳转时间戳可能触发短暂停顿的问题，减少跳转后播放 1-2 秒内按键和输入被延迟响应的情况。
- 修复长连词预览和跳转长连词时间戳时更容易卡顿的问题，避免光标、焦点和滚动状态变化反复使整句渲染缓存失效。

### Changed

- 优化打轴模块时间轴标签刷新和 checkpoint 跳转索引，降低 embedded 模式下主线程重复刷新造成的 UI 卡顿。

---

## [3.1.5.2] — 2026-06-17

仅同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.2.0。

### Added

- 打轴模块新增预览指引自定义设置，可分别控制上一个 / 正在打 / 下一个字符的提示显示与透明度。
- 打轴模块新增从 Karaoke Studio 导入配置的能力，方便复用工作台侧配置。
- 打轴模块新增界面语言设置，支持简体中文 / 日文 / 英文，并可按系统语言自动选择。

### Changed

- 打轴模块默认语言改为 Auto，会按系统 locale 自动选择界面语言。
- 打轴模块不再提前预渲染各速度音频，改为切换到对应速度后再渲染。
- 打轴模块优化变速音频渲染算法，降低部分场景下变速后音频速度突然加快的概率。

---

## [3.1.5.1] — 2026-06-16

仅同步打轴模块 StrangeUtaGame 到最新发布版，工作台主流程无改动。

更新 StrangeUtaGame 子模块到 SUGv1.1.8。

### Added

- 打轴模块新增导唱符待办标记：可在无时间戳字符前标记需要补导唱符，并可通过右键菜单或快捷键切换。
- 打轴模块新增按行 / 按所选字符范围移动原始时间戳的能力。

### Changed

- 打轴模块加载歌词改为异步并显示进度，Nicokara 格式解析速度提升。
- 打轴模块允许 ruby part 中输入空格；空 ruby part 会使用停顿符占位，避免保存或导出时丢失节奏信息。
- 打轴模块加载 / 新建项目时会清理旧音频数据，默认导出路径与上一次加载路径配置分离。
- 打轴模块自动注音会把数字标成日文读音，而不是保留纯数字。

### Fixed

- 修复打轴模块中连词组内存在无轴点字符时走字不同步的问题。
- 修复打轴模块自动删除注音后未取消连词状态的问题。
- 修复打轴模块中文 / 英文歌词场景下部分检查规则不生效的问题。
- 修复打轴模块清除当前行时间戳后 UI 未刷新的问题。
- 修复打轴模块行尾字符走字找不到闭区间时表现异常的问题。

---

## [3.1.5] — 2026-06-16

修复歌词检索与打轴嵌入路径中的若干问题，提升日文歌词导入和嵌入 SUG 关闭时的稳定性。

### Fixed

- 修复 Windows 日文 locale（cp932）下嵌入 SUG 启动 / 输出日志时遇到特殊 Unicode 字符可能触发编码错误的问题。
- 修复 UtaTen 带注音歌词导入打轴时，片假名 ruby 被转换成平假名的问题，例如 `レクイエム` 会保持片假名。
- 修复歌词预览标题过长时与右侧操作按钮重叠的问题，超长标题会省略显示。
- 修复嵌入打轴模块播放音乐后关闭工作台，音乐仍在后台继续播放的问题。

---

## [3.1.4] — 2026-06-13

修复波形对齐导出时的编码模式同步与 MP4 音频兼容性问题。

### Fixed

- 波形对齐导出视频时，编码模式以导出按钮点击时 UI 实际勾选的软编 / 硬编为准，避免内部缓存状态与单选按钮不同步。
- 波形对齐导出 MP4/MOV/M4V 时，PCM / FLAC / ALAC 等无损音轨改为转码成 320k AAC，兼容 Windows 自带播放器；需要继续保留无损音频时请导出 MKV（自动转 FLAC）。

---

## [3.1.3] — 2026-06-10

修复源码调试配置隔离、波形对齐 MP4 无损音频兼容性、工作台打开 `.sug` 项目，以及 YouTube fallback 解析只能看到 360p 的问题。

### Added

- 工作台现在可以作为 `.sug` 文件的打开方式：双击 `.sug` 后会启动工作台、自动切到「歌词打轴」模块并打开项目文件。
- 源码调试入口默认使用独立的 `Karaoke Studio Dev` 配置目录，避免直接运行 `app.py` / `python -m krok_helper` 时覆盖正式版设置；仍可通过 `KARAOKE_STUDIO_SETTINGS_DIR` / `KARAOKE_STUDIO_SETTINGS_APP_NAME` 显式指定配置位置。

### Changed

- 波形对齐导出视频时，MP4/MOV/M4V 中的 PCM / FLAC / ALAC 等无损音频会自动转为 ALAC，MKV 中转为 FLAC，避免把 PCM 音轨直接塞进 MP4 导致 Windows 播放器等无法播放，同时保持无损。

### Fixed

- 修复 YouTube 兼容 fallback 解析只剩 360p 的问题：解析阶段不再强制 `best`，fallback 客户端改用 `android_vr,web`，例如 `k5GQK_cOXcw` 可重新看到 2160p / 1440p 等分离视频流。

---

## [3.1.2] — 2026-06-09

修复 v3.1.1 引入的 Bilibili 视频解析回归。

### Fixed

- 修复 v3.1.1 为 YouTube 解析阶段加入 `best` 格式探测时误伤 Bilibili 的问题。Bilibili 多 P 链接不再被强制套用 `-f best`，解析时恢复只读取格式列表，避免已登录账号下出现「1 个链接失败」。

---

## [3.1.1] — 2026-06-09

修复 YouTube 部分 Mix / 电台链接在解析阶段直接显示「1 个链接失败」的问题。

### Fixed

- 修复部分 YouTube `watch?v=...&list=RD...&start_radio=1` 链接解析失败的问题：当默认 YouTube 客户端返回「视频不可用」或「当前清晰度不可用」时，会自动切换兼容客户端重试。
- 修复本地 YouTube Cookie 对个别公开视频产生副作用时解析失败的问题：如果带 Cookie 只能拿到缩略图或触发格式不可用，程序会仅针对当前视频自动改用无 Cookie 解析，并在后续下载中沿用该选择，避免同一任务下载阶段再次失败。
- 解析阶段改为显式使用 yt-dlp 的 `best` 格式探测，避免只想读取视频信息时被默认格式组合提前拦截。

---

## [3.1.0] — 2026-06-09

歌词检索模块新增日文带注音搜索源 **UtaTen**：搜索结果整理成带 `[tool:utaten-ruby]` 标记的 LRC，导入到打轴后会保留 ruby 注音、按字典自动分布每字读音，需要时还会自动识别 ateji 整块连词，不重新分词注音。

### Added

- 歌词检索新增 **UtaTen** 单独子模块（位于来源下拉的最后一项，与聚合分开）：按歌名搜索 utaten.com 的歌词页，结果是无时间戳的带注音 LRC（`{国道||こくどう}を行く` 这种形式）。导入到打轴后，多字 ruby 块会按字典切到每个汉字（如 世界→世=せ、界=かい），字典查不到的 ateji（如 新時代→はじまり）则均分假名到各字并保持连词整体；普通 LRC / TXT / ASS 等导入路径完全不受影响。
- 歌词预览面板在选中 UtaTen 候选时会提示「提供带注音的无时间戳 LRC；导入打轴后会按 ruby 块自动分布注音、不重新注音」，方便用户预判导入后的行为。

### Changed

- 聚合搜索范围调整为 QQ音乐 / 酷狗音乐 / 网易云音乐 / LRCLIB，**不再包含 UtaTen**——UtaTen 是日文专用通道，混在聚合里通用结果会把它压下去看不见，单列后能直接选用。

### Fixed

- 修复歌词检索面板默认勾选「省略歌曲介绍」时，会把 UtaTen 标记 LRC 头部的 `[tool:utaten-ruby]` / `[ti:]` / `[ar:]` 当成 intro credit 删掉的问题。原因是这几行都满足"短前缀冒号 + 长度 ≤ 48"的 intro 探测规则，全被去掉之后 SUG 就识别不出 utaten 格式，导致注音丢失。修复后 LRC 头部连续的 metadata 行不再参与 intro 探测，去 intro 只针对其后的正文行。

---

## [3.0.7.1] — 2026-06-09

仅 submodule 同步，主程序代码无改动。更新 StrangeUtaGame 子模块到 `ff69416`（基于 SUGv1.1.6，增加 UtaTen 标记 LRC 导入的逐字 ruby 分布能力）。

### Changed

- 更新 StrangeUtaGame 子模块到 `ff69416`：导入带 `[tool:utaten-ruby]` 标记的 LRC 时，多字 ruby 块（如 `{世界||せかい}`）会按字典自动切到每个汉字（世=せ、界=かい），ateji（如 `{新時代||はじまり}` —— 三字读音与单字读音无关）则均分到各字（新=はじ、時=ま、代=り）并保持连词。普通 LRC / TXT / ASS 等其它格式的导入路径完全不受影响。

---

## [3.0.7] — 2026-06-09

新增波形对齐导出目录选项；修复打包版点击「更新 yt-dlp」反而把 Karaoke Studio 自己的 usage 喷回状态栏的事故。

### Added

- 波形对齐模块的「命名设置」对话框新增「对齐导出位置」面板：可在「保存在字幕视频所在目录」（默认，与旧版一致）和「保存在指定目录」之间切换；选择后会持久化到 `settings.json`（`align_output_dir_mode` / `align_output_custom_dir`），波形对齐页面的「打开输出目录」按钮也会跟着跳到这里设置的目录。

### Fixed

- 修复打包版（PyInstaller exe）在视频下载模块点击「更新 yt-dlp」时，状态栏弹出 `yt-dlp 更新失败：... usage: Karaoke Studio.exe -h ...` 一整段乱码 + 主程序自己的 help 输出的问题。原因是 frozen bundle 里 `sys.executable` 是宿主 `Karaoke Studio.exe` 而不是 Python，旧逻辑直接拿它去跑 `-m pip install`，被主程序的 argparse 当成未知参数打回 usage。现在打包版会跳过 pip 路径，只尝试系统 PATH 上的独立 `yt-dlp` CLI；找不到时给出明确的中文提示，告知打包版无法热更新，请整体升级应用或单独安装独立 yt-dlp。

---

## [3.0.6] — 2026-06-09

新增顶部 6 步工作流栏的紧凑模式开关：打轴时嫌工作流栏占太高的用户，点一下就能把它折成一条窄行，6 步全部仍可点击切换，状态会持久化到 `settings.json`。

### Added

- 工作流栏右上角齿轮图标左侧新增「折叠 / 展开」按钮：折叠后整条 bar 从 80px 高缩到 44px，每一步只剩编号 + 标题（隐藏副标题与下划线），6 步全部仍可点击切换；按钮图标会在 ↑ / ↓ 之间切换并带中文 tooltip。
- 新增持久化设置 `workflow_compact`（写入 `%APPDATA%\Karaoke Studio\settings.json`），默认 `false` 保持老用户视觉不变；用户点过折叠后下次启动自动保持紧凑态。

---

## [3.0.5] — 2026-06-09

修复 v3.0.x 的一次真实数据丢失事故——升级时打轴模块的设置 / 词典 / 演唱者 / 网络词典缓存可能被全部清空。

### Fixed

- 修复 `%APPDATA%\Karaoke Studio\settings.json` 在保存时不是原子写、进程被打断会把文件 truncate 后留下空 / 半截内容的问题。改为先写 `settings.json.tmp` 再用 `os.replace` 原子替换；从此进程被杀只会留下未完成的 `.tmp` 文件，本体一定是上一次的完整版本。
- 修复启动时若 `settings.json` 损坏会被默默回落到默认值、并导致打轴模块 4 个命名空间（主配置 / 词典 / 演唱者 / 网络词典）一并消失的问题。现在解析失败会把坏文件备份到 `settings.json.corrupt-<时间戳>`，并在主窗口打开后弹红框告知备份位置与恢复入口（全局设置 → 工具 → 打轴模块数据导入）。

### 给老用户

- 上一版升级时若已经丢失了打轴模块设置 / 词典 / 演唱者，请检查 `%APPDATA%\Karaoke Studio\` 下是否仍有 `settings.json.tmp` 或系统快照（OneDrive / 历史版本）可恢复；若没有，请重新从原 StrangeUtaGame 目录导入。本次修复主要确保不再复发。

---

## [3.0.4.1] — 2026-06-09

仅 submodule 同步，主程序代码无改动。更新 StrangeUtaGame 子模块到 `SUGv1.1.6`。

### Added

- 注音分析所选字符支持跨行选中：选区内每一行的字符会被分别打包提交给分析引擎，按行排队顺序执行，避免词典 / 索引异常。
- 跨行选中时，编辑 / F3 拆分 / 批量改读音操作会被拦截并通过 InfoBar 提示「请仅在单行内操作」；跨行删除 / 复制 / 演唱者批量处理可正常使用并合并为一次 undo。
- 跨行拖动选词：数据模型、渲染高亮、右键菜单、选区边缘自动滚动全部就位。
- 双击音量 / 速度滑块可快速回到默认值。
- 导出时若检测到未打轴的行，会同步在 InfoBar 显示哪些行未完成。
- 按行设置演唱者支持多选。

### Fixed

- 修复无音频载入时 TSM 引擎仍派发任务的问题。
- 修复 nicokara 标签设置长内容显示不完整。
- 修复添加句号时右移的空格未按半个汉字字宽生成。
- 修复英文单词后紧跟的 `,` 影响英文单词后空格句尾取消的逻辑。
- 优化分色标签助手 UI 细节。

---

## [3.0.4] — 2026-06-09

修复 v3.0.3「打轴模块数据导入」的关键回归（取代 v3.0.3，建议老用户在升级后**重新点一次**导入）。

### Fixed

- 修复从旧版 StrangeUtaGame standalone 导入设置时，打轴模块的界面字号 / 注音字号与间距 / 节奏点标记大小与间距 / 行间距系数等设置被静默丢弃、退回到工作台默认值的问题。原因是 v3.0.3 的导入函数按 SUG 内部一份**不完整**的字段清单递归过滤未知项，把这些「未在清单里声明、但 SUG 运行时仍正常读取」的合法设置也当成未知项剔除了。现在导入逻辑只过滤未知的顶层 namespace（`audio` / `ui` / `timing` 等），namespace 内部整体保留交给 SUG 自己处理。
- 已经在 v3.0.3 里执行过导入的用户，升级到 v3.0.4 后请在「全局设置 → 工具 → 打轴模块数据导入」**再点一次**同样的目录，丢失的字号 / 间距 / 系数等会被补回；词典 / 演唱者按原策略合并去重，不会因为重导出现重复条目。

---

## [3.0.3] — 2026-06-09

完善打轴模块的嵌入体验与旧版用户迁移路径，主程序代码改动；submodule 未动。

### Added

- 新增「打轴模块数据导入」入口（全局设置 → 工具）：可从旧版 StrangeUtaGame standalone 的安装目录一键导入设置、词典、演唱者和网络词典缓存；主配置中未知的设置项会被忽略、缺失项使用默认值；词典与演唱者按名称合并去重，工作台已有的同名条目优先保留。
- 歌词检索输入框现在支持把歌词文件拖入：自动从 LRC 的 `[ti:]` / `[ar:]` 标签提取歌名与歌手，否则回落到文件名，免去手动输入。

### Fixed

- 修复打轴模块在工作台模式下正常退出时，autosave / 临时文件未被清理的问题——之前会导致下次启动工作台时打轴模块误触发「上次异常退出，是否恢复」提示。

---

## [3.0.2.1] — 2026-06-08

仅 submodule 同步，主程序代码无改动。更新 StrangeUtaGame 子模块从 `SUGv1.1.1+7` 到 `SUGv1.1.5`，跨越 v1.1.3 / v1.1.4 / v1.1.5 三个上游版本。

### Added

- 分色标签设置助手优化；创建分色时可以从项目里已用过的颜色直接选择。
- LLM 注音协议改用项目 inline annotated 格式；prompt 新增「单字独立读音 vs 熟字訓」拆分判定规则。
- LLM 注音日志重构：每次调用拆分为 request / response / extracted 三个文件；仅启动时清理，退出后保留便于排查。

### Fixed

- 修复 ASS 导出 roundtrip。
- 修复 nicokara 导出：多入口下 `nicokara_tags` 未加载或被污染；导出时过滤空行与空格。
- 修复节奏点 marker：矢量填充修复字形残缺，间距可配置，高亮对齐。
- 修复 LLM 注音用户词典被「单字块」穿刺：连续单字块合并为同一 morpheme，并改用多字块 + 辞典 ‐/＝ 锚点。
- 修复 phase5 复合词补全：新增 `compound_group_id` 保护，覆盖全流程注音路径。
- 修复默认演唱者判定：不再仅凭「未命名」字段判断。
- 修复导唱符过滤：排除无法被 n3 识别的变体选择符。

---

## [3.0.2] — 2026-06-06

### Fixed

- 修复从 Firefox 导入 YouTube Cookie 后生成无效 Netscape cookie 文件的问题。
- 修复部分 YouTube 播放列表链接解析失败的问题。
- 修复歌词打轴模块嵌入工作台后，横向拖动窗口时可能误拖动内部内容宽度的问题。

### Changed

- 更新弹窗现在会展示 GitHub Release 发布说明，方便用户在更新前查看本次变更。
- Windows 打包产物现在使用工作台 logo 作为 `Karaoke Studio.exe` 图标。
- 更新 StrangeUtaGame 子模块，包含注音/用户词典稳定性修复与嵌入工作台相关调整。

---

## [3.0.1] — 2026-06-06

### Changed

- 仓库迁移至 `github.com/karaoke-studio/karaoke-studio`，由 Myosotis11037 与 Xuan-cc 共同维护
- 合并 `Myosotis11037/karaoke-helper` 与 `Xuan-cc/StrangeUtaGame` 为单一仓库；StrangeUtaGame 源码重定位至 `krok_helper/lyrics_timing/`，git history 完整保留
- 仓库整体采用 GPL-3.0 协议（合并前 krok-helper 无 LICENSE，StrangeUtaGame 为 GPL-3.0）
- 将 `krok_helper/lyrics_timing` 拆为 `karaoke-studio/StrangeUtaGame` submodule，保留 SUG 社交资产与独立历史
- 增加工作台自动更新的 Windows release zip / SHA-256 生成流程

### Added

- 增加本地 updater 集成测试，覆盖全量 zip 下载、解压和安装目录替换

### 下一步计划

下一个发布版本 `v3.1.0` 将包含**打轴模块与主程序工作流的 UI 集成**（当前模块代码已合入，但主窗口第 4 步仍为占位）。

---

## [3.0.0] — 2026-06（合并前）

`Myosotis11037/karaoke-helper` 作为独立项目发布的最后一个版本。

详细变更见该版本 tag `v3.0.0` 的 commit 历史。

StrangeUtaGame 同期版本为 `SUGv1.1.1`，相关历史见
[`krok_helper/lyrics_timing/CHANGELOG.md`](./krok_helper/lyrics_timing/CHANGELOG.md)。
